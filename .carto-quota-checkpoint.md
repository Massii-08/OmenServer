# Checkpoint nuit 2026-06-06 — pipeline cartographes → bots ressources multi-quota

> État au moment de la veille Omen (01:00→06:00). Branche `feat/mc-agent-oremap-quota`
> (worktree `.claude/worktrees/feat+mc-agent-oremap-quota`). Tests **Node 450 + Python 240** verts.

## Fait (commits sur la branche)

1. **Intégration** : merge origin/main + `feat/mc-agent-resource-ores` + `feat/mc-agent-carto-ores`.
   Conflits résolus : `ores.js` = UNION (sélection cibles + détection exposés), `world_memory.py`
   add_ore unique (cap=ORE_CAP) + remove_ore + routing ore_mined/ore_gone.
2. **Scan complet** : `scanAllOres` (5 matériaux quota × stone/deepslate, exposés ET enfouis, flag
   `exposed`, **maxDistance 256** — à 128 la deepslate diamond y=-59 était invisible depuis la
   surface), event **batché** `ores_found` (1 write backend/scan), ORE_CAP 4000.
3. **Quotas + claims + mémoire live** : `quota.js` (tracker banked+inventaire, junkItems),
   `claims.js` (claims-<group>.json lockfile TTL 120s), `resource.js` mode quota (allowTypes,
   quota_done/quota_progress, busy retry 30s, reloadMemory + resource_waiting, starved 10 min,
   cleanup toss > deposit), manager (sidecar quota, wm LIVE + --wm-live + --claims pour objective
   resource), router (StartReq.quota validé, 400 si invalide), `_public` expose quota/quota_done.
4. **Viz** : carte = carrés colorés par type (exposé plein / enfoui creux) + chips légende `o:` +
   fit ; Multi-bot = option « Bot ressource » (quota mission par défaut) + barres par worker.
   i18n FR/EN/IT. Cache-bust lang/bots_module v224, sw v103.

## Décisions (autonomie — raisons)

- **Store = worlds[].ores backend** (contrat mission) ; le `oremap.js` du worktree initial sert
  pour ses primitives lock (claims.js) — son store JSON propre est abandonné (supersedé).
- **Claims hors mémoire de monde** : fichier séparé écrit par les bots (lockfile) — la mémoire
  reste single-writer backend ; claims = coordination éphémère, TTL auto-purge.
- **tossJunk au lieu de deposit en mode quota** : pas de coffre sous terre + le deposit existant
  jette TOUT (pioche comprise).
- **ores_found batché** : le fichier mémoire fait ~300 Ko à 4000 ores ; 1 write par ore aurait
  fait 1500+ réécritures complètes.
- Serveur test mis en **peaceful + day + keepInventory** (rcon, accélère le run).

## À reprendre à 06:05 (Omen réveillé — il REBOOT à 06:00, attendre ~5 min)

1. **Deploy (tâche E)** : depuis ce worktree —
   `git fetch origin && git checkout main && git pull && git merge feat/mc-agent-oremap-quota --no-edit`
   → re-run `node --test` + `pytest backend/bots/tests -q` → `git push origin main`
   → attendre ~90 s (auto-deploy cron/min) → `curl -s https://omenserver.org/api/modules/ -H "Authorization: Bearer $(cat /tmp/omen_token.txt)"` → site up ? sinon **revert immédiat**.
   → `git checkout feat/mc-agent-oremap-quota` (laisser le worktree sur sa branche).
   ⚠️ Aucune nouvelle dépendance Node/Python → auto-deploy suffit.
2. **Roster (API, token /tmp/omen_token.txt, valide jusqu'à ~00:15 le 7/6)** :
   - `POST /api/mc-agent/servers/8d91a7/bots` ×4 : `{role:'mapper', username:'MapperBot3'}`,
     `{role:'worker', username:'ResBot1'}`, `ResBot2`, `ResBot3` (comptes frais = inventaire vide).
3. **Mappers** : `POST /api/mc-agent/servers/8d91a7/mappers/start {count: 3}`.
   rcon : `op` inutile ; vérifier spawn 208,88,528. Poll `GET /servers/8d91a7/memory` →
   compter `worlds.*.ores`.
4. **Seuil** : ores ≥ ~80 dont ≥ 8 diamond → lancer les 3 workers EN PARALLÈLE :
   `POST /api/mc-agent/run {server_id:'8d91a7', bot_id:<ResBotN>, autonomous:true,
   objective:'resource', quota:{diamond:15,gold:15,redstone:64,lapis:64,iron:64}}`
   (les workers live-reload : lancer tôt est sûr).
5. **Gear** : dès qu'un ResBot est en ligne :
   `ssh omen 'docker exec omen-minecraft-trusted-test rcon-cli "give ResBotN diamond_pickaxe 2"'`
   + `"give ResBotN bread 32"`. (Durabilité 1561×2 ≈ OK pour ~220 items.)
6. **Monitorer** : `GET /api/mc-agent/active` (champ `quota` par session), `/status/{sid}`,
   `/chat/{sid}` ; problèmes par bot → logger dans le rapport ; stalls → rcon tp.
   RAM : `ssh omen free -h` (≥1.5 G dispo sinon réduire le nb de bots).
7. **Vérif UI (tâche G)** : Chrome MCP sur omenserver.org → Bots → groupe trusted-test →
   Mapping → Ouvrir la carte (ores visibles) + Multi-bot (barres quota). Screenshot.
8. **Rapports** : `.carto-quota-report.md` + CLAUDE.md historique + Daily note Obsidian.

## Pièges notés pour le rapport

- Pioche : stone pick (kit auto) ne mine PAS diamant/or/redstone (filtre tier les écarte —
  le bot minerait lapis/fer puis starved). → rcon-give diamant obligatoire pour le test rapide.
- `/memory` payload ~360 Ko à 4000 ores (poll 3 s carte ouverte) — tolérable, à optimiser un jour.
- Legacy resource sans quota + wm-live : attend 10 min (starved) au lieu de finir direct — assumé.
