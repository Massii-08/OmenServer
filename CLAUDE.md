# CLAUDE.md — OmenServer Project Context

> **Ce fichier sert de mémoire persistante pour tout agent IA (Claude, Gemini, etc.)**
> **travaillant sur ce projet. Lire intégralement avant toute modification.**

---

## 🏗️ Vue d'ensemble

**OmenServer** est un panel de gestion de serveur dédié polyvalent, inspiré de Minestrator/Pterodactyl.
Il permet de gérer des serveurs de jeux (Minecraft, etc.), des bots Python, des médias, un serveur web,
et le monitoring système multi-machines depuis une interface web premium.

- **Version** : 4.3.0
- **Auteur** : Massii_08 (Massimiliano)
- **License** : MIT
- **Hardware** : HP Omen (Ubuntu Server 26.04 en prod, cerveau) + agents sur d'autres PC (bras)
- **Stack** : Python FastAPI (backend) + Vanilla JS/CSS (frontend) — **pas de framework JS**
- **Accès** : https://omenserver.org (Cloudflare Tunnel)

---

## 📁 Structure du projet

```
Projet serveur/
├── backend/                    # API Python FastAPI
│   ├── main.py                 # Point d'entrée — monte tous les routers
│   ├── config.py               # Settings centralisés (.env → Settings class)
│   ├── database.py             # SQLAlchemy (SQLite) — engine, session, Base
│   ├── auth/                   # Authentification JWT + invitations
│   │   ├── router.py           # /api/auth/login, /register, /logout
│   │   ├── invite_router.py    # /api/auth/invite — codes d'invitation
│   │   ├── sharing_router.py   # /api/auth/share — partage de serveurs entre users
│   │   ├── models.py           # User, Invitation (SQLAlchemy)
│   │   ├── utils.py            # get_current_user(), hash/verify (bcrypt direct)
│   │   ├── access_control.py   # Contrôle d'accès par serveur
│   │   ├── permissions.py      # Système de permissions RBAC
│   │   └── rate_limiter.py     # Rate limiting par IP/user
│   ├── monitoring/             # Monitoring système + multi-machines
│   │   ├── router.py           # /api/monitoring/stats (CPU, RAM, disque combiné)
│   │   ├── system_info.py      # Collecte multi-disques via psutil
│   │   ├── diagnostic_router.py # /api/monitoring/diagnostic (système + crash nodes)
│   │   ├── container_router.py # /api/monitoring/containers (Docker)
│   │   └── nodes_router.py     # /api/nodes — PC connectés via omen_agent.py
│   ├── game_server/            # Gestion serveurs de jeux (Docker)
│   │   ├── router.py           # CRUD serveurs + start/stop/restart
│   │   ├── websocket.py        # WebSocket logs temps réel
│   │   ├── models.py           # GameServer (SQLAlchemy)
│   │   ├── backup_router.py    # Sauvegardes auto/manuelles
│   │   ├── backup_manager.py   # Logique de backup (tar.gz + rotation)
│   │   ├── docker_manager.py   # Gestion conteneurs Docker
│   │   ├── games_config.py     # Config des images Docker par jeu
│   │   ├── settings_router.py  # server.properties, JVM args
│   │   ├── players_router.py   # Whitelist, ops, bans
│   │   ├── access_router.py    # SFTP/RCON credentials
│   │   ├── sftp_manager.py     # Gestion SFTP conteneurisé
│   │   └── files_router.py     # Navigateur de fichiers serveur
│   ├── bots/                   # Module Bots Python
│   │   ├── router.py           # CRUD bots + start/stop + logs
│   │   ├── models.py           # Bot (SQLAlchemy)
│   │   └── yield_router.py     # 🏦 Bot Yield dédié (upload, run, status, download)
│   ├── scheduler/              # Tâches planifiées (APScheduler)
│   │   ├── router.py           # CRUD tâches cron
│   │   ├── engine.py           # APScheduler engine + power job
│   │   ├── models.py           # ScheduledTask
│   │   ├── power_router.py     # API extinction/redémarrage programmé + immédiat
│   │   └── power_manager.py    # Logique rtcwake/suspend/shutdown + arrêt gracieux
│   ├── mods/                   # Gestion de mods (CurseForge, Steam Workshop)
│   │   ├── router.py           # /api/mods — installation/suppression
│   │   ├── curseforge.py       # API CurseForge (Minecraft)
│   │   ├── steam_workshop.py   # Steam Workshop (ARK, CS2, etc.)
│   │   ├── plugin_router.py    # Plugins Minecraft (Spigot/Paper)
│   │   ├── plugin_manager.py   # Gestionnaire de plugins
│   │   └── datapack_manager.py # Datapacks Minecraft
│   ├── modules/                # Hub des modules
│   │   └── router.py           # /api/modules/ — liste des modules activés
│   ├── media/                  # Module Média (Jellyfin)
│   │   └── router.py
│   ├── webserver/              # Module Serveur Web (Docker)
│   │   ├── router.py
│   │   └── models.py           # Website
│   ├── network/                # Module Réseau (Wake-on-LAN, ping)
│   │   ├── router.py
│   │   └── models.py           # WolDevice, NetworkLog
│   ├── gdrive/                 # Google Drive integration
│   │   └── router.py
│   ├── notifications/          # Notifications
│   │   └── router.py
│   ├── activity/               # Historique d'activité
│   │   └── router.py
│   └── rate_limiter.py         # Rate limiter global (IP-based middleware)
├── frontend/                   # Interface web (vanilla JS/CSS)
│   ├── index.html              # Shell SPA principal
│   ├── login.html              # Page de connexion (standalone)
│   ├── css/
│   │   └── style.css           # Design system Bento Tech v5 (voir section dédiée)
│   ├── js/
│   │   ├── app.js              # Router SPA + Dashboard + App controller
│   │   ├── auth.js             # Auth.apiCall(), login/logout, token JWT
│   │   ├── lang.js             # i18n — FR/EN/IT (clés: modules.*, bots.*, yield.*, etc.)
│   │   ├── modules.js          # Hub des modules (cartes)
│   │   ├── monitoring.js       # Dashboard monitoring (stats combinées, carte Omen cerveau)
│   │   ├── toast.js            # Notifications toast
│   │   ├── bots_module.js      # Module Bots (liste + Yield Bot UI)
│   │   ├── files_module.js     # Module Fichiers (navigateur)
│   │   ├── media_module.js     # Module Média (Jellyfin)
│   │   ├── web_module.js       # Module Serveur Web
│   │   ├── network_module.js   # Module Réseau (WoL, ping)
│   │   ├── game_server.js      # Liste des serveurs de jeux
│   │   ├── server_view.js      # Vue détaillée d'un serveur (onglets)
│   │   ├── sv_files.js         # Onglet Fichiers serveur
│   │   ├── sv_settings.js      # Onglet Paramètres serveur
│   │   ├── sv_monitoring.js    # Onglet Monitoring serveur
│   │   ├── sv_players.js       # Onglet Joueurs serveur
│   │   ├── sv_access.js        # Onglet Accès serveur (SFTP/RCON)
│   │   └── sv_history.js       # Onglet Historique serveur
│   ├── sw.js                   # Service Worker (PWA)
│   ├── manifest.json           # PWA manifest
│   └── favicon.svg
├── tools/                      # Scripts utilitaires
│   ├── omen_agent.py           # 🦾 Agent monitoring à installer sur chaque PC
│   ├── omen-resume.sh          # 🌙 Script post-suspend (reboot pour RAM fraîche)
│   ├── omen-agent.service      # Fichier service systemd pour l'agent
│   ├── setup_omen.sh           # Setup automatique de l'Omen (cerveau)
│   ├── setup_omen_agent.sh     # Setup automatique d'un bras (agent + suspend/wake)
│   └── translate.py            # Script de traduction automatique
├── docs/                       # Documentation
│   ├── Guide_Installation_PC_OmenServer.md     # Guide ajout PC (FR)
│   ├── Guide_Installation_PC_OmenServer_IT.md  # Guide ajout PC (IT)
│   ├── Guide_Installation_PC_OmenServer.html   # Version HTML (FR)
│   ├── Guide_Installation_PC_OmenServer_IT.html# Version HTML (IT)
│   └── generate_pdf.py         # Générateur PDF des guides
├── data/                       # Données persistantes
│   ├── omenserver.db           # Base SQLite
│   ├── power_schedule.json     # Config extinction/réveil programmé
│   └── servers/                # Données des serveurs de jeux
├── .env                        # Variables d'environnement (non commité)
├── .env.example                # Template des variables
├── requirements.txt            # Dépendances Python
├── watchdog.sh                 # Script de surveillance (prod)
├── CLAUDE.md                   # Contexte projet pour agents IA
├── AGENTS.md                   # Règles pour agents IA
└── README.md
```

---

## ⚙️ Stack technique

### Backend
| Technologie | Usage |
|-------------|-------|
| **FastAPI** 0.115 | Framework API REST |
| **SQLAlchemy** 2.0 | ORM — SQLite en local |
| **bcrypt** | Hachage de mots de passe (direct, sans passlib) |
| **python-jose** | JWT pour l'authentification |
| **psutil** | Monitoring système (CPU, RAM, temp, multi-disques) |
| **docker** (Python SDK) | Gestion conteneurs Docker |
| **APScheduler** | Tâches planifiées (cron-like) |
| **uvicorn** | Serveur ASGI |

### Frontend
| Technologie | Usage |
|-------------|-------|
| **Vanilla JS** | Pas de React/Vue — tout est en JS natif |
| **Vanilla CSS** | Design system complet avec variables CSS |
| **Inter** (Google Fonts) | Typographie |
| **Chart.js** | (via CDN) Graphiques monitoring |

### Infrastructure de Production
| Composant | Détail |
|-----------|--------|
| **OS prod** | Ubuntu Server (HP Omen) |
| **OS dev** | macOS |
| **RAM** | **15 Go** (relevé live 2026-06-01 : ~3.4 Go utilisés au repos, ~11 Go dispo) |
| **CPU** | **12 threads** (idle downclock 800 MHz, ~2-3 % au repos) |
| **Stockage** | HDD 914 Go (`/`) + SSD NVMe 469 Go (`/mnt/ssd`) = **1.3 To** |
| **Python** | 3.9+ (venv dans `./venv/`) |
| **DB** | SQLite (`data/omenserver.db`) |
| **Docker** | Conteneurs pour les serveurs de jeux |
| **Cloudflared** | Tunnel Cloudflare → `omenserver.org` (service systemd) |
| **Service** | `omenserver.service` (systemd, démarre au boot via `start-omen.sh`) |
| **Auto-deploy** | Cron toutes les minutes → `auto-deploy.sh` (git pull + restart) |
| **Agents** | `omen_agent.py` sur chaque PC du réseau |

---

## 🎨 Architecture frontend

### Pattern SPA
Le frontend est une **Single Page Application** sans framework :
- `app.js` gère le routing (`App.navigateTo('bots')`)
- Chaque module a son fichier JS (`bots_module.js`, `files_module.js`, etc.)
- Chaque module expose un objet global avec une méthode `render(container)` et optionnellement `unload()`
- Le contenu est injecté via `container.innerHTML`

### Chrome (depuis PR26)
- **Top bar horizontale** (pas de sidebar verticale) — composant `.topbar` sticky en haut
- `.brand` à gauche (logo carré accent vert "O" + texte mono "OMENSERVER")
- `.nav-tabs` au centre (8 tabs depuis PR37/PR38 : Dashboard / Serveurs / Bots / Fichiers / Média / Web / Réseau / Diagnostic) — `flex:1; min-width:0; overflow-x:auto` avec scrollbar custom 4px
- `.topbar-right` à droite : `.lang-switcher` (pill segmenté FR/EN/IT) + `.accent-switcher-mini` (4 dots couleurs) + `.user-menu-wrap` contenant `.user-pill` (avatar+nom+rôle+caret ▾, button cliquable `aria-haspopup="menu"`) qui ouvre le dropdown `.user-menu` (Utilisateurs admin-only / Paramètres / *sep* / Se déconnecter)
- Tab active visible via `::before` dot accent vert
- Badge `.tab-badge` sur nav-tab quand jobs background tournent (PR32 — `App._pollBgJobs`)

### Background jobs poller (PR32)
- `App._activeJobs = {}` + `App._bgJobsInterval` poll `/api/bots/yield|scanner/active` toutes les 5s
- Cross-page awareness : tu lances un job Yield → tu navigues sur Dashboard → le badge `[1]` reste visible sur le tab Bot
- Toast notif quand un job transition `running → completed`
- Backend continue toujours via `subprocess.Popen` détaché — c'était purement perceptuel avant PR32

### Design System (style.css) — Bento Tech v5
- **Voir section dédiée** plus bas (`## 🎨 Design System v5 — Bento Tech`) pour les tokens, composants Bento, règles d'usage et catalogue complet.
- **Variables CSS** dans `:root` (`--bg`, `--bg-elev-1/2/3`, `--text`, `--accent`, `--danger`, etc.)
- **Theming** : 1 mode dark + 4 accent variants via `data-accent="green|blue|red|yellow"` (default green dans `:root`)
- **Composants principaux** : `.bento-overview` + `.stat-card`, `.machine-card` (+ `.brain`/`.arm`), `.bot-card-bento`, `.role-pill`, `.badge`, `.diag-strip`, `.row-list`, `.machines-grid` (responsive)
- **Legacy survivants** : `.card`, `.btn`, `.btn-primary`, `.console`, `.module-card`, `.stat-machines-list` (dashboard multi-PC) — partiellement migrés via overrides PR7
- **Responsive** : breakpoints à 768px, 480px

### Internationalisation (lang.js)
- 3 langues : **FR**, **EN**, **IT**
- Accès via `Lang.t('clé.sous_clé')`
- Changement de langue via `Lang.setLang('en')` (persisté dans localStorage)
- Structure des clés : `common.*`, `nav.*`, `modules.*`, `bots.*`, `yield.*`, `servers.*`, `dashboard.*`

### Authentification (auth.js)
- Token JWT stocké dans `localStorage`
- `Auth.apiCall(url, options)` ajoute automatiquement le header `Authorization: Bearer <token>`
- Détection automatique `FormData` → pas de `Content-Type: application/json`
- Bannière auto-reconnexion si le serveur est down

---

## 🖥️ Architecture Multi-Machines

### Concept : Cerveau / Bras
- **L'Omen** = cerveau (serveur central, dashboard, API) — **toujours visible** dans la liste des machines
- **Les autres PC** = bras (agents légers qui envoient leurs stats)

### Dashboard unifié
- **Cartes du haut** : stats **combinées** (CPU moy. pondérée par cœurs, RAM sommée, Disque sommé, Temp max)
- **Section "Réseau de machines"** : grille de cartes avec :
  - 🧠 **Omen (cerveau)** toujours en premier, avec badge violet et bordure verte
  - 🦾 **Agents (bras)** ensuite, avec badge bleu et boutons reboot/shutdown/retirer
- Boutons **reboot/shutdown** sur la carte Omen (admin, double confirmation)

### Monitoring combiné
- `system_info.py → get_disk_info()` somme **tous les disques physiques** (HDD + SSD + partitions)
- `monitoring.js → updateUI()` fusionne les stats serveur + nodes connectés (CPU pondéré, RAM sommée, Temp max)
- Les cartes CPU/RAM/Temp affichent une **mini-liste par machine** quand des nodes sont connectés

### Gestion de l'alimentation (Power Management)
- `power_manager.py` : logique d'extinction/réveil programmé
- `power_router.py` : API REST pour la configuration
- **Cycle quotidien** :
  1. **01:00** → APScheduler déclenche `execute_scheduled_shutdown()`
  2. Arrêt gracieux : backup serveurs → stop Docker → stop bots
  3. `rtcwake -m no -l -t <timestamp>` → programme le timer BIOS
  4. `systemctl suspend` → suspend-to-RAM (S3)
  5. **06:00** → BIOS réveille le PC via RTC alarm
  6. `omen-resume.sh` → détecte le flag → **reboot complet** (RAM vidée)
  7. Après reboot : cloudflared + omenserver redémarrés
- Config stockée dans `data/power_schedule.json`
- Endpoints : `POST /api/power/reboot`, `POST /api/power/shutdown`
- Double confirmation pour l'extinction du cerveau
- **Anti-boucle** : fichier flag `/tmp/omen-post-suspend-reboot` empêche les reboots infinis

### Diagnostic multi-machines
- `diagnostic_router.py` vérifie CPU, RAM, Disque, Docker, Serveurs de jeux, Réseau **et Nodes**
- **Crash détecté** : si un PC agent passe offline depuis < 5 min → CRITICAL avec dernières stats
- **Offline longue durée** : > 5 min → WARNING avec suggestion Wake-on-LAN

### Agent (`tools/omen_agent.py`)
- Script Python léger à installer sur chaque PC
- Envoie un heartbeat toutes les 10s : CPU, RAM, Disque, Temp, Uptime
- Authentification via `X-Agent-Key` (clé API auto-générée)
- Peut recevoir des commandes : `reboot`, `shutdown`
- **Installation auto** : `setup_omen_agent.sh` installe tout (agent + suspend/wake + reboot post-wake)
- Guide d'installation : `docs/Guide_Installation_PC_OmenServer.md` (FR + IT)

---

## 🔌 Modules actifs

| Module | Backend Router | Frontend JS | Description |
|--------|---------------|-------------|-------------|
| **Serveurs de jeux** | `game_server/router.py` | `game_server.js` + `server_view.js` | Gestion Docker (Minecraft, etc.) |
| **Bots** | `bots/router.py` + `yield_router.py` | `bots_module.js` | Bots Python + Yield Bot |
| **Fichiers** | `gdrive/router.py` | `files_module.js` | Google Drive |
| **Média** | `media/router.py` | `media_module.js` | Jellyfin |
| **Web** | `webserver/router.py` | `web_module.js` | Sites web Docker |
| **Réseau** | `network/router.py` | `network_module.js` | WoL, ping, scan |
| **Diagnostic** ([[🩺 Diagnostic Bot]]) | `sysdoc/router.py` + `sysdoc/ws_router.py` | `sysdoc_module.js` | Agent sur PC distant via WS (RAM, processus groupés, trousse 3 tiers) |

---

## 🩺 Module Diagnostic / sysdoc (V4 multi-machine)

Le **Diagnostic Bot** est intégré comme module SPA dans le hub. Permet de superviser le Mac et le PC Windows de l'utilisateur depuis n'importe quel browser logué.

### Architecture WS

- **Agent** (`tools/diagnostic_agent/`) tourne sur chaque PC de l'utilisateur. Connecte `/ws/sysdoc/agent/{username}/{machine}` (multi-machine par user, `machine` = hostname par défaut).
- **Viewer** (frontend SPA) connecte `/ws/sysdoc/viewer/{username}`. Reçoit les messages de TOUTES les machines (taggés `machine` par le backend), affiche un sélecteur de pills en haut quand >1 machine.
- **ConnectionManager** : `agents = Dict[str, Dict[str, WebSocket]]` (user→machine→ws). `viewers = Dict[str, Set[WebSocket]]` (broadcast à tous les onglets ouverts).

### Auto-start de l'agent

- **macOS** : LaunchAgent via `setup_macos.sh` (install/start/stop/restart/status/logs/uninstall + enable-dns-flush pour sudoers DNS).
- **Windows** : Tâche planifiée via PowerShell admin avec `pythonw.exe` (PAS `python.exe` — voir piège #19). Installable via le panel install du dashboard (bouton "+ Ajouter un PC").

### Pattern unique au sysdoc

- **Mode idle/active** : l'agent ne pousse pas de metrics au boot. Reste en idle, écoute juste `START_MONITORING` / `STOP_MONITORING` / `QUERY_STATE` / `LIST_ACTIONS` / `RUN_ACTION` / `SUSPEND_PROCESS` / `RESUME_PROCESS` / `BULK_SUSPEND`. Économie CPU/réseau quand l'utilisateur n'est pas sur la page.
- **Trousse 3 tiers** (safe/moderate/risky) : safe et moderate sont exécutées par l'agent, risky est REFUSÉE même si commandée (dead man's switch, le frontend affiche juste les instructions).
- **Persistance machines en localStorage** : `sysdoc.machines.<user>` → les pills offline restent visibles même après reload.
- **/api/sysdoc/me** : retourne `secret_key` UNIQUEMENT si `is_admin=true` → permet au panel install de pré-remplir le SECRET_KEY dans les commandes copy-pastables.

---

## 🏦 Bot Yield (intégration spéciale)

Le bot de calcul de rendement d'obligations est un **projet Python externe** intégré dans OmenServer.

### Architecture
```
OmenServer                          Bot Calcul Yield (externe)
  │                                    │
  ├── yield_router.py                  ├── main.py (CLI: --all, --recalculate)
  │   ├── POST /upload → save .xlsx    ├── bot/yield_bot.py (orchestrateur)
  │   ├── POST /run/{id} → subprocess  ├── calculator/yield_calculator.py
  │   ├── GET /status/{id} → parse logs├── excel/processor.py
  │   ├── GET /download/{id}           ├── scraper/boerse_scraper.py (Playwright)
  │   └── GET /usage                   └── bot/rate_limiter.py (5/jour max)
```

### Points clés
- Le bot est lancé via `subprocess.Popen` — **jamais importé directement** en prod
- Les logs sont capturés en temps réel via `stdout` pipe + thread
- La progression est parsée via regex sur les lignes de log
- Le fichier résultat est `*_AGGIORNATO.xlsx`
- Rate limit : max 5 scraping/jour (ricalcolo illimité)
- Variable d'environnement : `YIELD_BOT_DIR` (défaut: `~/omenserver/bots/yield-bot/`)

---

## 🔒 Sécurité

- **JWT** avec expiration configurable (défaut 24h)
- **bcrypt** direct (sans passlib, avec troncature 72 bytes manuelle)
- **CORS** restreint aux origines locales
- **Headers sécurité** : X-Frame-Options DENY, X-Content-Type-Options nosniff, XSS-Protection, CSP
- **Swagger/Redoc** désactivés en production (`docs_url=None`)
- **Upload** : validation `.xlsx` uniquement pour le Yield Bot
- **Rôles** : admin, moderator, player, spectator (via `User.role`)
- **Invitations** : inscription uniquement par code d'invitation
- **Agents** : authentification via clé API (`X-Agent-Key`)
- **Rate limiter** : middleware IP-based (exemption agents + localhost)
- **RBAC** : `access_control.py` + `permissions.py` pour contrôle d'accès par serveur
- **Clés API masquées** : les clés sont masquées dans le dashboard (toggle show/hide)
- **Partage serveurs** : `sharing_router.py` pour partager l'accès à un serveur entre users

---

## 🗄️ Base de données

**SQLite** via SQLAlchemy ORM. Tables :

| Table | Modèle | Fichier |
|-------|--------|---------|
| `users` | `User` | `auth/models.py` |
| `invitations` | `Invitation` | `auth/models.py` |
| `game_servers` | `GameServer` | `game_server/models.py` |
| `scheduled_tasks` | `ScheduledTask` | `scheduler/models.py` |
| `bots` | `Bot` | `bots/models.py` |
| `websites` | `Website` | `webserver/models.py` |
| `wol_devices` | `WolDevice` | `network/models.py` |
| `network_logs` | `NetworkLog` | `network/models.py` |

Les migrations sont faites manuellement dans `main.py` startup (ALTER TABLE avec try/except).

---

## 🚀 Commandes utiles

```bash
# Développement (macOS)
cd "Projet serveur"
source venv/bin/activate
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000

# Production (Ubuntu Server — géré par systemd)
sudo systemctl status omenserver
sudo systemctl restart omenserver

# Logs du serveur
sudo journalctl -u omenserver -f

# Auto-deploy (cron, toutes les minutes)
cat ~/deploy.log

# SSH vers l'Omen
# ⚠️ L'IP locale CHANGE via DHCP. Ne JAMAIS copier une IP d'un ancien
#    transcript / exemple — elle sera stale. Procédure obligatoire avant
#    chaque SSH :
#       1. Ouvrir https://omenserver.org (tunnel Cloudflare toujours up)
#       2. Module Réseau → carte "IP locale"
#       3. SSH avec CETTE IP
ssh massii08@<IP_LOCALE_DU_MOMENT>

# Accès distant via Cloudflare Tunnel
# Automatique via systemd : cloudflared.service
sudo systemctl status cloudflared
```

---

## 📐 Conventions de code

### Backend (Python)
- **Routers** : un fichier `router.py` par module, préfixé `/api/<module>/`
- **Modèles** : SQLAlchemy dans `models.py` de chaque module
- **Auth** : `current_user: User = Depends(get_current_user)` sur chaque endpoint protégé
- **Logging** : `logger = logging.getLogger("omenserver")` puis `logger.info/warning/error`
- **Docstrings** : en français ou italien, triple quotes

### Frontend (JS)
- **Modules** : objets globaux (`BotsModule`, `FilesModule`, etc.) avec `render(container)` + `unload()`
- **API calls** : toujours via `Auth.apiCall(url, options)` — jamais `fetch()` direct
- **i18n** : toujours `Lang.t('clé')` — jamais de texte hardcodé
- **DOM** : `innerHTML` pour le rendu, `document.getElementById()` pour les mises à jour
- **Pas de framework** : pas de React, Vue, jQuery — tout est vanilla JS

### CSS
- **Variables** : utiliser `var(--nom)` pour couleurs, bordures, transitions
- **Composants** : classes `.card`, `.btn`, `.btn-primary`, `.status-badge`, `.console`, `.module-card`
- **Nouveau module** : ajouter les styles dans `style.css` avec un commentaire séparateur

---

## ⚠️ Pièges connus

1. **Python 3.9** sur la machine de dev — attention aux syntaxes 3.10+ (`match`, `|` pour unions)
2. **SQLite** ne supporte pas `ALTER TABLE ... IF NOT EXISTS` — les migrations utilisent try/except
3. **Docker** doit être installé et le daemon actif pour les serveurs de jeux
4. **Playwright** + Chromium requis sur le serveur prod pour le scraping du Yield Bot
5. **FormData upload** : `Auth.apiCall` détecte automatiquement `FormData` et ne met pas `Content-Type: application/json`
6. **Auto-refresh des bots** : l'interval de 5s doit être clearé (`unload()`) avant de naviguer vers le Yield Bot
7. **Espace dans le chemin** : "Projet serveur" a un espace — utiliser un wrapper script pour systemd
8. **bcrypt direct** : `auth/utils.py` utilise `bcrypt` directement (pas passlib) avec troncature manuelle à 72 bytes
9. **Cache Cloudflare/SW** : bumper `?v=XX` dans `index.html` + `CACHE_NAME` dans `sw.js` après chaque modif JS/CSS
10. **Service Worker PWA** : `sw.js` met en cache les fichiers statiques — bumper `CACHE_NAME` à chaque version
11. **Cache browser stubborn** : même avec `caches.delete + sw.unregister + Ctrl+Shift+R`, le disk cache peut servir une ancienne version `?v=N` du CSS si N a été utilisé avant. Workaround : bumper à un N franc (`?v=99` au lieu de `?v=83`) pour avoir une URL jamais cachée.
12. **i18n fallback truthy trap** : `Lang.t('clé.x') || 'fallback'` ne marche PAS car `Lang.t()` retourne la clé elle-même (truthy) si non trouvée. Pattern correct : `(Lang.t('clé.x')||'').startsWith('clé.') ? 'fallback' : Lang.t('clé.x')`.
13. **Backend emoji propagation** : les Toast notifications viennent souvent de messages backend (`return {"message": "...✅"}`). Stripper les emojis i18n côté frontend ne suffit pas — auditer aussi les `message`/`detail` strings dans `backend/**/*.py` (cf. PR22 — 63 ✅ retirés dans 24 fichiers backend).
14. **Pattern reconstruction trap** : quand on strip un pattern (emoji, hex, etc.), grep aussi les contextes JS où il peut être RECONSTRUIT côté code (dicts, template literals, concatenations). PR16 a découvert `renderSettings()` avait un dict `{admin:'👑', ...}` qui reconstruisait l'emoji après PR14 RBAC strip — la sidebar montrait "Admin" mais Settings montrait "👑 Admin".
15. **Regex emoji sweep + `\s*` pitfall** : `EMOJI_RE + \s*` mange les `\n` aussi (regex `\s` inclut newline). Si un commentaire `// ─── Section ───` (où `─` est U+2500 = dans le emoji range Unicode), le strip englobe le `\n` suivant et le commentaire mange la déclaration suivante. Toujours utiliser `[ \t]*` (horizontal whitespace seulement) si on veut préserver les newlines.
16. **CSP `img-src` restrictif** : par défaut `backend/main.py` n'autorise que `'self' data: blob:` pour les images. Les vignettes CurseForge/Modrinth/Steam Workshop sont CDN externes — il faut les ajouter explicitement à la directive `img-src`. Symptôme : `<img>` avec `naturalSize: 0x0` + `display: none` via `onerror` (silent fail).
17. **Emoji-only button trap (PR35)** : les sweeps emojis (PR27-29-34) laissent des `<button>...</button>` complètement vides quand l'emoji était le SEUL contenu visible. Détection : `grep -E "class=\"btn[^\"]*\"[^>]*>\\s*</button>"` sur `frontend/js/`. Fix : toujours remplacer par un label texte i18n via `Lang.t()`. Idem pour les `::before { content: 'emoji' }` CSS qui peuvent laisser un glyph orphelin avec padding incohérent (cf. `.sharing-search-wrap::before` supprimé en PR35).
18. **`.btn-icon` trap (PR36)** : la classe `.btn-icon` force `width:38px; height:38px; padding:0` (conçue pour emojis carrés). Si on convertit un bouton emoji-only en label texte SANS retirer la classe, le texte déborde et les boutons adjacents se superposent visuellement (vu sur cartes serveur PR36 : "Arrêter Redémarrer Partager" collés). Pattern de détection : `grep -E 'class="[^"]*btn-icon[^"]*"[^>]*>\\${Lang\\.t'`. Fix : retirer `.btn-icon`, garder `.btn-sm` qui a un padding texte normal.
19. **`cond ? '' : ''` smell (PR36)** : un ternaire dont les 2 branches sont des chaînes vides (`btn.textContent = visible ? '' : ''`) est presque toujours un sweep automatique qui a mangé les 2 branches d'un toggle emoji (`'👁' : '🙈'`, `'▶' : '⏸'`). Détection post-sweep : `grep -E "\\? '' : ''"` sur les JS.
20. **Windows Task Scheduler + `python.exe` = `STATUS_CONTROL_C_EXIT`** : si on crée une tâche planifiée qui exécute `python.exe -u main.py`, Windows attache une console à la session interactive. Quand l'utilisateur ferme la PowerShell où il a fait `Start-ScheduledTask`, Windows propage `CTRL_CLOSE_EVENT` → le python reçoit SIGINT → exit avec code `3221225786` (`0xC000013A`). **Solution** : utiliser `pythonw.exe` (sans le `e`, le `w` = no console). Le venv Windows en contient un d'office. Bloc complet dans le panel install Windows du dashboard.
21. **Triple-paste du SECRET_KEY au setup** : `setup_macos.sh` et `setup_windows.bat` utilisent `read -s` pour masquer l'input du SECRET_KEY. Si l'utilisateur appuie Cmd+V/Ctrl+V plusieurs fois sans s'en rendre compte (input masqué), la valeur est concaténée 2-3× dans le `.env` → JWT invalide → close code 1008 du hub → reconnect storm. Diagnostic : `python3 -c "print(len(open('.env').read().split('OMEN_JWT_SECRET=')[1].strip()))"`. Fix : remplacer la ligne via un Python script idempotent.
22. **macOS `killall mDNSResponder` sans sudo** : Catalina+ exige sudo. L'agent LaunchAgent tourne en user → ne peut pas exécuter sans password (un service ne peut pas prompt interactif). Solution : `setup_macos.sh enable-dns-flush` qui crée `/etc/sudoers.d/omen-diagnostic-agent` avec une règle ultra-restrictive (UNIQUEMENT cette commande sans password). One-shot, demande sudo une fois pour écrire le fichier sudoers.
23. **PowerShell + `.\` prefix obligatoire** : pour exécuter un script du cwd (ex: `setup_windows.bat`), faut `.\setup_windows.bat`. Sans le `.\`, erreur "n'est pas reconnu comme nom d'applet de commande". Protection anti-PATH-hijacking. En cmd.exe le `.\` n'est pas nécessaire.
24. **PowerShell s'ouvre dans `C:\Windows\system32`** : surtout en admin. `git clone` y échoue avec "Permission denied". Faut `cd ~` avant tout pour aller dans `C:\Users\<user>` (writable).
25. **Bloc de commandes "exemples utiles" copy-pasté en bloc** : PowerShell exécute en séquence si l'utilisateur colle plusieurs commandes ensemble. Si y'a un `Unregister-ScheduledTask` ou `rm` dans le lot, la dernière commande exécute et détruit ce que les précédentes ont créé. **Toujours documenter les commandes individuelles dans des blocs SÉPARÉS** avec une note "à utiliser une par une".
26. **NSSM (`nssm.cc`) souvent down 503** : mainteneur unique. Pour Windows Service alternative : `choco install nssm` / `scoop install nssm`, OU mieux **éviter NSSM totalement** via Tâche planifiée Windows (recommandé maintenant — voir piège #20 pour la version `pythonw.exe`).
27. **Multi-machine WS path** : le sysdoc utilise `/ws/sysdoc/agent/{username}/{machine}` (pas juste `{username}`). `machine` = `socket.gethostname()` sanitized par défaut, override possible via `OMEN_AGENT_MACHINE` env var. Le ConnectionManager garde un `Dict[user, Dict[machine, WS]]`. Si 2 PCs s'installent avec le MÊME machine_id, ils se kickent (strict 1:1 par machine). Solution : machine_id unique par PC (le hostname suffit usually).
28. **Backtick PowerShell `` ` `` non échappé dans template literal JS** : quand on embarque du PowerShell dans un `data-copy="..."` à l'intérieur d'un `return \`...\`` JS, **TOUT** `` ` `` dans le HTML embarqué (utilisé en PowerShell comme escape char, ex: `` `$false ``, `` `"...`" ``) ferme prématurément le template literal JS → SyntaxError au parse → module entier non défini → onglet muet (clic = ReferenceError silencieuse en console, DOM figé sur la vue précédente). Vu en commit `871c085` ligne 476 de `sysdoc_module.js` : `` -Confirm:`$false `` cassait le parse → `SysDocModule` undefined → onglet Diagnostic non fonctionnel. **Fix** : soit échapper avec `` \` `` (produit un `` ` `` littéral en sortie), soit retirer le `` ` `` si inutile en PowerShell (cas de `$false`/`$true` qui sont des littéraux natifs et n'ont PAS besoin d'échappement). **Pourquoi pas détecté** : la prod auto-deploy live sur main, et la vue par défaut au boot est `#hub`, pas `#sysdoc` — l'erreur reste silencieuse jusqu'au premier clic sur l'onglet impacté. **Réflexe** : après tout commit qui touche un panel install / code snippets PowerShell embarqués → `node -e "new Function(require('fs').readFileSync('frontend/js/<file>.js','utf8'))"` AVANT de push pour valider le parse.

29. **Rating Bond Scanner / Yield Bot — Brave Search API single source** : les deux bots partagent la même clé `BRAVE_SEARCH_API_KEY` (free tier 1000 req/mois). Stratégie `site:fitchratings.com {issuer}` parsée sur les titres indexés. Pièges spécifiques : (a) **strip prudent des suffixes** (Inc/Corp/LLC/PLC/SA/AG/NV/GmbH/SpA uniquement — JAMAIS Worldwide/Finance/Holdings/Capital sinon faux positif type Hilton Worldwide → Hilton Grand Vacations Trust BB-) ; (b) **REJECT keywords** `trust/abs/rmbs/cmbs/grand vacations/presale/covered bond/mortgage/clo/spv` (skip les hits sur structures de securitisation) ; (c) **cache négatif obligatoire 30j** (sinon on re-burn la quota pour les issuers que Fitch ne couvre pas — typiquement 30-40% des corporates US) ; (d) Brave portal a migré `api.search.brave.com` → `api-dashboard.search.brave.com` mais l'endpoint search reste `api.search.brave.com/res/v1/web/search`. (e) **Adaptation sync→async** : Yield Bot utilise `httpx.get` sync (entry point synchrone) ; Bond Scanner utilise `httpx.AsyncClient` car `fetch_ratings()` tourne dans la loop Playwright. Si on porte du code sync vers un consommateur async, surtout pas bloquer la loop avec sync I/O. (f) **2 copies à patcher** : `bot obbligation/` (standalone) + `Projet serveur/bond-scanner/` (in-server, prod). `start()` a divergé 2026-05-27 (snap-aware Chromium fallback in-server-only) → `diff -q` sur tout le market_scraper.py FAIL même après sync ; faire un diff sur la région `fetch_ratings()` uniquement.

30. **Bond Scanner — boutons Angular page-bar ne répondent qu'au DISPATCH d'événements pointer (headless)** : sur Deutsche Börse (live.deutsche-boerse.com/bonds), les boutons de pagination (`button.page-bar-type-button` : taille "100", "Show page N") NE répondent NI à Playwright `.click()` NI au native `element.click()` en HEADLESS — les deux sont des no-ops silencieux (0 nouveau `bond_search`, données inchangées → scan plafonné à 25 bonds). Le handler Angular est bindé sur la SÉQUENCE complète `pointerdown+mousedown+pointerup+mouseup+click`. Fix : `page.evaluate` qui dispatch les 5 events sur l'element_handle (cf. `MarketScraper._click_robust`). Diag confirmé en local (Mac headless chromium) : Playwright click → 50 liens ; dispatch → 200 liens + fire bond_search. **Réflexe** : pour tout site Angular/SPA en headless où un click ne déclenche rien, tester le dispatch de la séquence pointer complète. (b) **Réponse bond_search async ≈10s** : après le click, `wait_for_load_state("networkidle")+timeout` est TROP COURT → on parsait avant l'arrivée → "Nessun nuovo bond". Fix : poll explicite de `api_responses` jusqu'à voir une clé `bond_search` (`_wait_for_bond_search`, timeout 20s). (c) **Listing riche** : la réponse `bond_search` contient déjà `name.originalValue` ("ISSUER COUPON% YY/YY") + `keyData.coupon` → on extrait émetteur+coupon+maturité (année du dernier `/YY`) SANS enrich → 97% des bonds `is_complete()` depuis le listing → l'enrich page-détail (5s/bond, LE bottleneck) est skip → ratisser le marché devient faisable. (d) **3 stores dédup** : found (livrés, permanent), seen (rejetés, 60j TTL car le prix→yield bouge), rating cache (30j). L'overflow (valides hors top-N) n'est dans AUCUN → revient concourir. (e) **Garde-fou réserve Brave** : on lit `X-RateLimit-Remaining` (mensuel) ; à ≤50 on arrête + bloque le lancement (HTTP 429) — ces 50 sont RÉSERVÉES au Yield Bot (clé partagée). (f) **Scan détaché** (`start_new_session=True` dans le `subprocess.Popen` du backend) : sinon un push pendant un scan → auto-deploy → reload uvicorn → tue le subprocess enfant. NE JAMAIS pusher pendant qu'un scan tourne (badge "Bots N").

31. **Rating Brave/Fitch — faux positifs WRONG-ISSUER (les 2 bots, fix 2026-05-29)** : la stratégie `site:fitchratings.com {issuer}` extrayait le rating du TITRE du 1er hit Fitch SANS vérifier que la page parle bien DE l'émetteur cherché. Brave renvoie TOUJOURS des résultats (même si Fitch ne note pas l'émetteur) → on récupérait le rating d'une AUTRE entité. Cas réel reproduit : `Iccrea Banca` (petite banque coopérative IT, non notée Fitch) → Brave renvoyait `"Fitch Affirms ICBC at 'A'"` → Iccrea taggé **'A'** (ICBC = Bank of China ≠ Iccrea). Pire : le score `+2` ('Affirms'/'IDR') PRÉFÉRAIT activement ces pages-autre-émetteur à la page (sans rating) du bon émetteur. Symptôme côté Massii : ratings introuvables à la main sur Fitch, "aucun s'ouvre sur Fitch". **Fix** : garde-fou d'identité `_issuer_matches_hit(issuer, title, url)` — on n'accepte un rating QUE si le **token identitaire** de l'émetteur (1er mot ≥3 lettres non générique, ex. "dominion"/"iccrea"/"bayerische") apparaît comme **TOKEN** (pas substring — "ubs" ⊄ "subsidiary") du titre OU du slug de l'URL. Émetteur 100% générique → rejet. Mirror exact dans `bond-scanner/scanner/rating_providers.py` (`BraveFitchProvider`) ET `yield-bot/scraper/rating_fetcher.py` (`_try_brave_search`). (b) **Bond Scanner = rating obligatoire** : `criteria.matches()` rejette désormais TOUT bond sans rating vérifié (avant : seulement si `min_rating` set). (c) **Lien vérifiable Excel** : `RatingInfo.source_url` + `bond.rating_url` → la cellule G de l'Excel est un **lien hypertexte** vers la page Fitch qui prouve le rating (Massii clique → vérifie). (d) **Yield Bot** : écrit déjà `?` si `(None, None)`, donc le gate fait juste tomber les faux positifs en `?` (comportement demandé). (e) **Caches empoisonnés** : `~/.cache/{bond-scanner,yield-bot}-ratings.json` contenaient les faux positifs (TTL 30j) → vidés (backup `/tmp/*.poisoned.bak`), se régénèrent corrects au prochain scan. (f) Régression verrouillée par fixtures Brave RÉELLES (`bond-scanner/tests/fixtures/brave_results.json`) : `test_iccrea_false_positive_is_now_none` + `test_dominion_true_positive_bbbplus`. (g) **Affinage mots GÉOGRAPHIQUES (2026-05-29 bis, révélé par un scan réel)** : le match sur 1 seul token échouait quand ce token était un mot géographique/national commun → `Deutschland, Bundesrepublik` (Bund AAA) taggé **BBB** via "Telefonica **Deutschland**", et `DZ BANK ... Deutsche Zentral-...` taggé A- via "**Deutsche** Bank". Fix : ajout des pays/nationalités (`deutsche`/`deutschland`/`america`/`france`/`republik`/…) à `_GENERIC_NAME_TOKENS` → le token identitaire tombe sur la VRAIE marque (`telekom`, `zentral`, `lufthansa`). Effet de bord assumé : les émetteurs dont le SEUL token distinctif est géographique (Deutsche Bank, Bank of America, Bund allemand) → ∅ (pas de rating, sûr) plutôt qu'un faux. Recall < correctness (priorité Massii). (h) **Anti-péremption + withdrawn** : on jette les pages Fitch > 8 ans (ex. GM 'D' de 2009 → re-tombe sur GM BBB- 2020) et celles dont l'URL contient `withdraw` (notation retirée, ex. Vodafone West GmbH). 101 tests ✓.

32. **Garde-fou réserve Brave — mauvaise lecture du header sur plan SANS cap mensuel (fix 2026-05-29)** : le plan Brave actuel est métré **50 req/s, AUCUN cap mensuel** → l'API renvoie `x-ratelimit-limit: 50, 0` / `x-ratelimit-remaining: 49, 0`. `_read_remaining` lisait `parts[-1]` = `0` comme "0 restant mensuel" → `0 ≤ 50` → `quota_low=True` → (a) scan stoppé après le 1er bond, (b) la valeur `0` persistée dans `~/.cache/bond-scanner-brave-remaining.json` BLOQUAIT tous les scans suivants (HTTP 429 pré-lancement, `scanner_router._brave_remaining`). **Fix** : lire AUSSI `x-ratelimit-limit` ; n'armer la réserve QUE si la **limite mensuelle > 0** (vrai cap). Limite == 0 (plan métré) → `remaining_monthly = None` (pas `0`) → rien de bloquant persisté. La détection terminale `quota_exhausted` (429 + mots-clés) reste le vrai garde-fou. Les requêtes 200 prouvent que `0` ≠ épuisé. **Note** : la "réserve 50 pour le Yield Bot" ne s'applique que sur un plan À CAP mensuel ; sur le plan métré actuel la seule contention est le 1 req/s (déjà géré par l'espacement 1.1s).

33. **Rating FITCH PAR ISIN — source unique définitive (les 2 bots, 2026-05-29, SUPERSEDE #29/#31/#32)** : Massii a tranché « **fitch only ISIN** » — Brave (recherche par NOM) est abandonné car *fondamentalement* imprécis (un nom est ambigu : Iccrea→ICBC, Bund→Telefonica, et même quand le bon émetteur matche on ne récupère qu'une note approximative, pas celle DU bond). L'ISIN est unique → zéro ambiguïté. **Comment** : on interroge l'API GraphQL publique de Fitch `POST https://api.fitchratings.com/` avec `search(term:<ISIN>, item:IDENTIFIERS){ entity{name ratings{ratingTypeDescription ratingCode}} issue{isin ratings{...}} }`. (a) **Cloudflare bloque les clients serveur au niveau TLS** (`TLSV1_ALERT_PROTOCOL_VERSION` avec httpx/requests) → on utilise **`curl_cffi`** (`Session(impersonate="chrome")`) qui imite l'empreinte TLS de Chrome → 200 OK. C'est LA clé qui débloque tout (l'ancienne investigation pensait Fitch totalement injoignable headless). (b) **Note rendue = note du TITRE exact** (`issue` dont le tableau `isin` contient l'ISIN cherché — c'est LE rating du bond ; ex. Oncor secured → **A**), avec **fallback** sur la note émetteur `entity` Long Term IDR si le titre n'a pas de note propre (décision Massii 2026-05-29 : « la note du titre exact »). La note émetteur reste dispo (`issuer_rating`) — bascule globale via `PREFER_SECURITY_RATING`/`FITCH_PREFER_SECURITY=False`. NB : pour 95% des bonds (senior unsecured) titre == émetteur ; seuls secured/subordinated diffèrent. (c) **`WD`/`NR` (retiré/non noté) → None** : un émetteur dont Fitch a RETIRÉ la note (ex. Mercedes-Benz, noté S&P/Moody's seulement) tombe en ∅ — c'est CORRECT (≠ l'ancien Brave qui ressortait une page stale "A-"). Sovereigns OK (Bund→AAA, Spain→A, EU→AAA). (d) **Retry obligatoire** : la 1ʳᵉ requête d'une session curl_cffi reçoit parfois un challenge Cloudflare HTML (200 sans JSON) → 3 essais ; flag `unreachable` SEULEMENT après 5 échecs consécutifs (vrai blocage, pas un blip ponctuel → sinon on tuerait le scan sur un bond flaky). (e) **Lien vérifiable** : cellule G Excel = hyperlien `https://www.fitchratings.com/search/?query=<ISIN>` (= la recherche manuelle exacte de Massii). (f) **Modules** : `bond-scanner/scanner/fitch_isin.py` (`FitchIsinClient`, `select_rating`) consommé par `market_scraper.fetch_ratings` (sync curl_cffi via `asyncio.to_thread`) ; `yield-bot/scraper/rating_fetcher.py` (`select_isin_rating`, `_fetch_fitch_isin`) — `fetch_rating(isin, issuer)` ignore désormais `issuer`. (g) **⚠️ DÉPLOIEMENT** : `curl_cffi` doit être pip-installé dans le venv backend de l'Omen (les bots tournent via `sys.executable`) ; l'auto-deploy ne réinstalle PAS les deps → `pip install curl_cffi` à la main sur l'Omen, sinon `ImportError` → `unreachable` → toutes notes ∅. Ajouté à `requirements.txt` (×3). (h) Contrat GraphQL extrait par rétro-ingénierie du bundle Gatsby (introspection désactivée) + découverte error-guided (`search(term,item)` → `EntityHit.ratings{ratingCode,ratingTypeDescription}`, `IssueHit.isin/ratings`). Fixtures réelles : `tests/fixtures/fitch_isin_responses.json`, tests `test_fitch_isin.py`. (i) **Dead code** : `BraveFitchProvider` (rating_providers.py) + `_try_brave_search`/`_try_*` (rating_fetcher.py) + leurs tests restent en place mais NE SONT PLUS appelés (cleanup à faire). Les helpers partagés `is_valid_rating`/`normalize_to_sp`/`RATING_SCALE`/`_Cache` de rating_providers sont TOUJOURS utilisés (par fitch_isin + criteria). (j) **⚠️ CACHE EMPOISONNÉ à la migration de source (piège majeur, vécu)** : le cache `~/.cache/bond-scanner-ratings.json` (TTL 30j) contenait 457 entrées de l'ère Brave. Au 1er scan post-migration, chaque ISIN déjà caché était servi avec sa VIEILLE note Brave (par nom, fausse) au lieu d'appeler Fitch → Excel contaminé (Orange A+ au lieu de BBB+, etc.). **Fix** : à la lecture du cache, n'honorer QUE les entrées dont `source` commence par "Fitch" — les entrées Brave sont ignorées → re-fetch Fitch → écrasées. **Règle générale** : quand on change de source de rating, soit on versionne/vide le cache, soit on filtre les hits de cache par source (le cache survit aux déploiements, lui). Vérif gold standard : recouper les notes du bot contre l'API Fitch DEPUIS LE NAVIGATEUR (in-page fetch sur l'onglet fitchratings.com, CORS OK) — chemin indépendant du code Python. (k) **UI** : encarts clé Brave retirés des 2 bots (`bots_module.js`) ; bouton **"Oublier les rejetés"** (scanner) → `POST /api/bots/scanner/reset-seen` (vide seen only ; found préservé ; seen auto-expire 60j). `/reset-found` (les deux) reste pour le nettoyage total.

34. **Auto-restart au boot doit RESPECTER l'état désiré `srv.status` (fix 2026-05-29)** : la boucle d'auto-restart du `@app.on_event("startup")` de `backend/main.py` rallumait **TOUT** conteneur ayant un `docker_id` et non-running, **sans lire `srv.status`** → les serveurs de jeu **volontairement éteints** se rallumaient à chaque restart uvicorn. Comme l'auto-deploy (cron/min) restart uvicorn à chaque `git push`, le serveur ressuscitait dans la minute → impression de « se rallume tout seul en continu » (signalé ≥2 fois par Massii, le 1er « fix » n'avait jamais marché). **Cause exacte** : le `elif container.status in ("exited","created","paused"): container.start()` était **inconditionnel** — seule la branche « si allumé, rallume » existait, la branche « si éteint, reste éteint » n'avait jamais été codée. **Fix** : `container.start()` UNIQUEMENT si `srv.status == "running"` (état désiré) ; sinon (`stopped`/`error`) laissé éteint + DB sync `stopped`. (a) **`GameServer.status` est un champ CONFLATÉ** (état *désiré* + *observé*) : `list_servers`/`get_server` (`game_server/router.py`) l'écrasent avec le statut Docker réel + `db.commit()`. Le garde-fou d'une ligne suffit car après un *Arrêter* volontaire (`stop_server` commit `stopped`, l.357-359) le statut reste fiablement `stopped`. Refonte propre future éventuelle = 2 champs séparés (`desired_state` vs `observed`). (b) **Politique Docker = `unless-stopped`** (PAS `always`) : un conteneur arrêté via `docker stop` reste stoppé au reboot du daemon, seuls les running reviennent → elle COOPÈRE avec le garde-fou ; ne JAMAIS la passer à `always`. (c) **Effet attendu post-deploy** : un serveur actuellement *en ligne* n'est PAS coupé par le fix (il EST running → branche sync) → il faut un dernier *Arrêter* manuel APRÈS le deploy pour que l'état désiré devienne `stopped` ; ensuite il reste éteint à travers deploys + reboot nocturne.

35. **Mod Fabric `mc-capture-mod/` = build-time only (Phase 1b.1)** : c'est un mod CLIENT (tourne sur la machine du joueur, PAS sur l'Omen). L'Omen ne gagne **aucune** dépendance runtime — il ne fait qu'ingérer/distiller les `.jsonl` uploadés (Python **stdlib**). Java 21 + Gradle requis SEULEMENT pour builder le `.jar` (machine de dev). **Ni JDK ni Homebrew sur le Mac de dev par défaut** (`brew` introuvable, `java` introuvable) → installé en **tarball user-space sans sudo** : Temurin JDK 21 dans `~/.local/jdk/jdk-21.0.11+10/Contents/Home`, Gradle 8.12 dans `~/.local/gradle-8.12/`, JUnit console standalone dans `~/.local/tools/junit-console.jar`. ⚠️ L'API Adoptium `.../jdk/hotspot/...` peut livrer un dossier au nom trompeur — **toujours résoudre le vrai `JAVA_HOME` via** `find ~/.local/jdk -name javac -path '*/bin/javac'` (ne pas deviner le numéro de version). **Compat loom↔Gradle** : loom `1.11+` exige Gradle ≥ 8.14 ; avec Gradle 8.12 utiliser loom **`1.10.5`** (stable, supporte MC 1.21.4). **Logique pure** (`SessionWriter`/`Recorder`/`TickRecord`) testable en JUnit **sans client MC** (8/8) via `javac -cp junit-console.jar` puis `java -jar junit-console.jar execute` → les hooks (`CaptureMod`/`RecHud`) ne font que brancher ; validés au build Gradle. **Consentement câblé** : `Recorder` OFF par défaut, toute erreur d'I/O force REC-off.
36. **Captures comportementales = admin-only + consenties (Phase 1b.1)** : `data/mc-captures/<joueur>/` (gitignored via `data/`). Le `player` vient TOUJOURS du **header** du `.jsonl` (jamais d'un champ UI) → attribution auto même si un seul admin uploade pour toute l'équipe. **Upload manuel uniquement** (le mod n'a aucune capacité réseau → rien à exfiltrer). `_safe_player()` collapse les `..` (anti path-traversal) sur le nom de dossier. Distillation `mc_capture_distill.py` → 2 sorties : `style.json` (stats, `derivedParams` calé 1:1 sur `mc-agent/humanize.js` pour la calibration 1b.2) + `clips/` (motricité segmentée par contexte pour le rejeu 1b.3). Endpoints sous le **même prefix** `/api/mc-agent` que le bot (chemins `/captures*` distincts → pas de collision) ; **router séparé** `mc_capture_router` monté dans `main.py` après `mc_agent_router`.
37. **Rôle REC-testeur (`rectester`) = capture-only (Phase 1b)** : nouveau rôle RBAC inséré entre `spectator` et `player` dans `VALID_ROLES` (`backend/auth/permissions.py`), permission **`mc_capture`**. Accès UNIQUEMENT à : télécharger le mod (2 jars), tuto d'install, déposer/voir/supprimer **SES** captures. **Tout le reste reste admin strict** (lancer le bot, clé Claude, profils, distillation, captures d'autrui) — gate **backend** (`_require_capture` vs `_require_admin`), pas seulement UI (tests 403 dédiés). **Ownership = compte OmenServer** (`current_user.username`) écrit côté serveur dans un sidecar `data/mc-captures/<pseudoMC>/session-*.jsonl.owner` à l'upload — DISTINCT du pseudo MC (qui reste la clé de regroupement pour les profils). `list_captures(owner=…)` / `delete_capture(requester=…)` : `None`=admin (voit/supprime tout), sinon filtré au compte. **Jars servis depuis `mc-capture-mod/dist/`** (committés → arrivent via auto-deploy, AUCUN build Java sur l'Omen, cohérent #35) par `GET /api/mc-agent/mod/{version}` avec **whitelist `_MOD_JARS`** (lookup dict, pas de concat de chemin → path-traversal impossible, testé). Rôle **admin-assigné uniquement** (`INVITABLE_ROLES` inchangé). Frontend : carte MC Agent **réduite** si `u.role==='rectester'` (download+tuto+mes captures), complète si admin. **Mod multi-version** 1 codebase : `options.release` + `-Pjava_version` (1.20.1=Java17/major61, 1.21.4=Java21/major65 ; loom 1.10.5 pour Gradle 8.12) — JAMAIS `sourceCompatibility` (casse `--release` injecté par Loom).

38. **MC Agent commandes serveur = texte chat, pas de skill** : le bot exécute une commande serveur (`/home`, `/tpa Bob`) en la tapant via `bot.chat` (le SERVEUR fait le boulot) — AUCUNE nouvelle skill mineflayer. Le LLM met la commande dans le champ décision `command` ; `mc-agent/commands.js` (`isAllowed`) la laisse passer SEULEMENT si elle est dans la whitelist du profil serveur (sinon event `blocked_command`). Double sécurité : prompt (`buildCommandDocs`) + filtre sortant (`replyTo` + `runCommand` dans `index.js`). La whitelist effective est résolue côté backend (`mc_agent_servers.resolve_commands` : catalogue coché + custom) et passée au subprocess via `--commands <file>` (fichier temp `data/mc_agent_runs/`, nettoyé au stop). **⚠️ `buildSystemPrompt(null)` DOIT rester `=== SYSTEM_PROMPT`** (test pinné `brain_think`) → ne pas toucher au schéma JSON du prompt sans profil ; le champ `command` est introduit par le bloc commandes injecté (`commandDocs`), pas par le schéma de base. Profils serveur (connexion + intelligence + commandes) persistés `data/mc_agent_servers.json` ; catalogue prédéfini `mc-agent/commands-catalog.json` (source unique lue par le backend ET résolue pour le bot). UI 2-onglets ▶ Lancer / ⚙ Serveurs ; `/run` accepte `server_id`. Tout admin-only.

39. **MC Agent gens de confiance — gating ordres + auto-accept TP** : la liste `trusted` (par profil serveur) gate UNIQUEMENT les ordres = `decision.action` OU `decision.command` du LLM (`gateDecision` dans `mc-agent/trust.js`, appliqué dans `index.js`, les met à `null` si l'émetteur n'est pas trusted) ; le `reply` (réponse à une question) passe toujours → tout le monde peut DISCUTER, seuls les gens de confiance commandent. **Liste vide ⇒ gating OFF** (rétro-compat : tout le monde commande ; `gateDecision` renvoie la décision inchangée). Double garde-fou : prompt (`buildTrustDocs` injecté + `De: <sender>` ajouté au message dans `think`) ET suppression dure. **Auto-accept /tpa** via `bot.on('messagestr')` + `parseTpRequest` (patterns Essentials EN/FR ancrés `^\w` → le chat joueur `<X> …` ne matche jamais) ; n'envoie `/tpaccept` que si (a) demandeur trusted ET (b) `/tpaccept` **coché** dans la whitelist commandes (synergie #38). Non-trusted /tpa = **ignoré** (pas de /tpdeny). **Trade** = opt-in (Essentials n'a pas de trade natif) : `{acceptCmd, requestPattern}` par profil, auto-accept désactivé si absent. Policy `{trusted, trade}` résolue backend (`mc_agent_servers.resolve_policy`) → passée au bot via `--policy <file>` (`data/mc_agent_runs/policy-<sid>.json`, nettoyé au stop). ⚠️ formats Essentials localisés : si la traduction FR du serveur diffère des patterns, le TP auto ne se déclenche pas (échec silencieux, jamais de crash) → fournir la ligne exacte. `buildSystemPrompt(null)` reste `=== SYSTEM_PROMPT` (3e arg `trustDocs` optionnel, cf. #38).

40. **MC Agent commandes directes — couche déterministe ZÉRO-LLM (en /msg)** : 16 commandes anglaises (`take`/`follow me`/`stop`/`craft`/`give`/`give all`/`pvp`/`tpa`/`mine down`/`come`/`deposit`/`guard`/`equip`/`eat`/`goto`/`afk`) parsées par `mc-agent/orders.js` (`parseOrder`→`{verb,args}|null`) **UNIQUEMENT** sur l'event `whisper` (/msg privé), interceptées dans `handleIncoming` **AVANT** tout appel `think()` → exécution déterministe, **0 token LLM** (but #1 : ne pas griller la clé sur des ordres). **Matrice** : (/msg=commande+trusted → exécute, 0 LLM) ; (/msg=non-commande → LLM répond en /msg) ; (public+bot **nommé** → LLM répond) ; (public non-nommé → ignoré). Gating réutilise `isTrusted` (#39 ; liste vide = tous ; non-trusted+commande → ignoré silencieux). Retours **TOUJOURS en whisper privé** à l'émetteur via `ackPrivate` (jamais public ; un whisper ≠ token → l'interdit « ne réponds pas » visait l'appel LLM) — `done`/`fait`/`fatto` localisé selon la langue, échecs whisperés. **Modules purs** : `orders.js` (parseur) ; `tools.js` (`bestToolFor` outil par matériau/`bestWeapon` arme, palier netherite>diamant>fer>…, épée>hache) ; `tasks.js` (`createTaskController` : **1 seule tâche longue active, annulable** ; `stop`/`afk`/nouvelle commande l'annulent via `token.cancelled` + cleanup `stopMotion`) ; `memory.js` (historique conversation **par joueur**, fenêtre 8 msgs + TTL 10 min, horloge injectable comme `RateLimiter` → injecté dans `think` via `history`, ⚠️ coût : grossit le prompt). **Skills** : `gather` (take = `findBlock` rayon 48 + outil **re-évalué par bloc** → terre=pelle/pierre-minerai=pioche/bois=hache + **auto-défense** si hostile ≤4 blocs puis reprise) ; `mineDown` (garde-fou lave/vide) ; `guard` (boucle `setInterval`) ; `give`/`giveAll` (toss vers l'émetteur) ; `craft` ; `deposit` (coffre ≤12) ; `equip`+`eat` (réutilise `reflexes.FOODS` exporté) ; `loiter` (**`stop` = vivant** : look G/D + petits pas ≤2.5 blocs + sneak aléatoire + pauses, intensité ∝ `movementJitter`, **anti-tell #1**). **`afk` = inerte** (annule tâche+loiter, plus de mouvement volontaire, MAIS réflexes survie ON pour pas mourir bêtement, + `/afk` serveur si coché). **Langue** au profil serveur (`language` ∈ {fr,en,it} → `_clean_server` → `--lang` → `buildLangDocs` injecté dans le system prompt ; **`buildSystemPrompt(null)===SYSTEM_PROMPT` toujours pinné**, 4e arg `langDocs` optionnel, cf. #38/#39). **Port optionnel** (placeholder UI, vide→25565). ⚠️ Si le serveur route /msg autrement que l'event `whisper` mineflayer (formats Essentials localisés) → le pré-filtre commande ne voit rien (échec silencieux, cf. #39). **Aucune nouvelle dep** (Node ou Python) → auto-deploy propre (≠ #33). Tests **Node 132 + Python 61**. Worktree `feat/mc-agent-direct-commands`, subagent-driven TDD.

43. **MC Agent phase 2 anti-xray — minage réel multi-bots (2026-06-06/07)** : (a) **`findBlocks` SYNC = tueur d'event-loop** — un scan rayon 256 sur une région à grottes (20K+ ores envoyés malgré l'anti-xray engine-mode 1 : les ores air-adjacents des grottes passent) = 10-20M lectures de blocs ; à chaque arrivée ×6 mappers → event loop bloquée → keepalive raté → « Timed out » en cascade. Fix : 1 scan/cellule 128, rayon 128. (b) **allow-flight=true obligatoire** pour les bots pathfinder (kick « Flying is not enabled » aléatoire). (c) **Worldgen multi-mappers = freeze serveur** (49 s « Running behind » → timeout simultané de TOUS les clients) : flotte ≤3-5 mappers, view-distance 6, /locate espacé 180s (fouille les region files). (d) **Un deploy ne touche PAS les sessions vivantes** — après chaque fix : RECYCLER les workers (stop+/run), sinon ils tournent sur l'ancien code indéfiniment. (e) **Self-healing complet requis** pour « zéro intervention » : respawn auto backend des sessions resource mortes (l'inventaire du compte persiste → le quota reprend) + watchdog client 90 s sans physicsTick (un « Timed out » serveur peut laisser le socket client MUET → process zombie, quota figé) + starved→process.exit (un idle éternel est invisible du self-healing) + **garde crash-on-spawn : 3 morts <15 s d'affilée → respawn_given_up** (le cap global ne couvre pas ce mode : join→crash 4 s→respawn 15 s à l'infini, et chaque relance manuelle remettait le compteur à zéro). (f) **Échelle d'outillage SANS trou** : pickaxePlan doit accepter raw_iron (la fonte est déclenchée par le plan lui-même — exiger des lingots = œuf-et-poule, ×50 mineFor('iron') stériles) ; bootstrap fer sans sticks = impasse (pas de bois sous terre) → 3 bootstraps sans gain de palier = starved ; withCraftingTable doit RE-CRAFTER la table perdue (2×2, 4 planks — kick avant reclaim = table envolée → 'no_table:unknown_item' éternel). (g) **Vec3 PARTOUT où on touche bot.blockAt** : dcd874d avait fixé branchMine mais PAS descendDiagonal → mineFor rejetait en silence ('error' sans détail). (h) Boucles d'eau : ≥4 unreachable consécutifs → relocate + cap distance des cibles mappées (200) sinon jamais de starvation (33K ores = toujours « une cible suivante » dans la même zone noyée) ; relocate vers une cellule TERRE des biomes mémoire (le quadrant hashé de V2Res4 tombait en plein océan), seedé aléatoirement par process. (i) **JWT 24 h** : poller l'API toute la nuit → le token meurt en route (« blips ») ; minter côté serveur via `create_access_token` (ssh, jamais stocké).

42. **MC Agent pipeline cartographes→ressources multi-quota (2026-06-06)** : (a) **Cap d'ores PAR TYPE obligatoire** — un scan complet souterrain est ~85% de fer ; avec un cap global FIFO, la marche des mappers évince les diamants en quelques scans (vécu : 2534 fer / 9 diamants au 1er scan). `add_ore` cape par `_ore_base` (800/type). (b) **TOUT appel mineflayer long doit être borné individuellement** — le goto l'était (240s) mais PAS `collectBlock.collect` → ResBot1 gelé 25 min (leçon #41d généralisée : `collectTimeoutMs` 90s + stopMotion + cible suivante). (c) **Ne JAMAIS tp un bot en plein goto** : l'interruption rejette la promesse pathfinder → `unreachable` → le bot RELÂCHE sa claim et part ailleurs (l'inverse du but du tp). Débloquer = restart de session, pas tp. (d) **Mode quota = nearest-first** (priority=[]) — diamant-d'abord force un tunnel de plusieurs minutes par cible alors que le quota exige TOUS les types ; le plus proche d'abord crée un effet cluster en profondeur. (e) **Claims TTL vs tunnels longs** : tryClaim au départ seulement → la claim expirait (120s) pendant l'approche → refresh/re-claim post-goto. (f) **Event batché `ores_found`** quand la cible est un gros fichier réécrit par event (1 write/scan, pas 1500). (g) **`exposed` flag** : serveur offline sans anti-xray → le cache client mineflayer voit tout le Y des chunks chargés (findBlocks maxDistance **256** — à 128 la deepslate diamond y=-59 est invisible depuis la surface). (h) **Symlink node_modules absolu committé** (vécu) : une session worktree avait committé `mc-agent/node_modules -> /Users/...` → en prod = symlink cassé → tout bot crashe au lancement, silencieux jusqu'au premier /run. Purgé ; `npm install` requis sur l'Omen après (≠ nouvelles deps, mais réinstall complète). (i) **AppleScript→Chrome n'expose PAS les globals page** (App/BotsModule invisibles, monde isolé) — localStorage/DOM oui ; piloter l'UI = clics DOM réels (`el.click()`), pas d'appels de fonctions. (j) Claims multi-bots = fichier séparé `claims-<group>.json` lockfile écrit par les bots (la mémoire de monde reste single-writer backend) — coordination éphémère TTL hors contrat `worlds[].ores`.

41. **MC Agent planner autonome MVP — robustesse skills × terrain (zéro→pioche pierre, 0 LLM)** : le moteur planner (`goals.js` chaîne + `planner.js` boucle `firstUnmet→runSkill` + `worldModel.js` persistance + `auth.js` AuthMe) était CODÉ/validé mais échouait en autonomie TOTALE (sans qu'on fournisse les matières). 4 corrections de robustesse bas-niveau (validées live, run autonome complet stone_pickaxe + `autonomous_done` + **0 LLM**, 164 tests Node) : **(a) Table de craft PORTABLE** (`withCraftingTable`/`craftSmart` dans `index.js`) — le bot GARDE 1 `crafting_table` en poche et la pose à côté de lui (`placeBlockNear`) pour CHAQUE craft 3×3 puis la **reprend** (`reclaimBlock` via collectBlock), au lieu de l'ancien `ensureNearTable` qui exigeait de REVENIR à une table fixe en surface → le bot s'enterrait en minant le cobble (y~57) → loin de la table → craft 3×3 final échoue → stall. La table « vient au bot » (surface OU sous-sol). `goals.js` : `place_table` SUPPRIMÉ, `crafting_table` ne dépend plus de `hasTable`. **(b) `gatherLog` essence-agnostique** : `gather` accepte un TABLEAU de noms (`_ids` string|array) → cherche l'arbre le + proche de N'IMPORTE quelle essence (avant : `Object.keys(blocksByName).find(_log)` = oak hardcodé → `not_found` si pas d'oak ≤64). maxDistance 48→64. **(c) `placeBlockNear` 3 passes** : Pass 1 case vide/replaceable sur sol solide ; Pass 2 creuse une paroi solide (sous-sol/shaft) ; **Pass 3 piédestal** = pose un bloc de remblai (dirt/gravier… PAS le cobblestone réservé au craft) contre la face latérale du bloc sous les pieds pour combler un sol voisin manquant (cas où `gather('stone')` a miné les blocs AUTOUR → bot sur piédestal, voisins+sols en air), puis la table dessus. Retourne `{ok,pos}` pour le reclaim. **(d) Garde-fou anti-freeze** : `withTimeout` (90s) autour de chaque skill du planner — `pathfinder`/`collectBlock` peuvent rester bloqués INDÉFINIMENT sur une cible inatteignable (gèle le planner pour toujours) → timeout → `stopMotion` + `{ok:false,timeout}` → le planner re-dérive. ⚠️ **`mineDown` (creuser droit) NE marche PAS pour le cobble** : si le bot est à une position fractionnaire il est supporté par un bloc adjacent → ne tombe pas après avoir creusé dessous → `void_below` au tour suivant (échec). Garder `gather('stone')` (collectBlock gère le pathing/dig) + Pass 3 pour le piédestal. ⚠️ **LEÇON TEST MAJEURE** : tester en boucle DÉTRUIT le terrain de spawn (déforestation → `gatherLog not_found` ; trous de minage → `mineDown void_below` ; lac drainé) → **faux échecs** non reproductibles ailleurs. Valider sur une **arène propre** (`fill` un bloc de pierre solide + `setblock` des logs) ou un serveur FRAÎCHEMENT recréé ; le code est fiable sur terrain intact (3/3 runs arène OK). **Aucune nouvelle dep** (réutilise gather/mineDown/craft/collectblock) → auto-deploy propre (≠ #33). Boucle de test : `~/mc-agent-mvp-test/` isolé (≠ repo prod auto-reset), `launch.sh [user]` (user frais = inventaire vide garanti, évite la race d'inventaire hérité + keepInventory), `rcon clear`/`save-all flush`/`tp`/`setblock`/`data get entity <bot> Inventory[{id:"minecraft:stone_pickaxe"}]`. Worktree `worktree-feat+mc-agent-planner-mvp`, subagent-driven TDD.

42. **MC capture mod — builder une version MC ≥1.21.6 = bump toolchain + rupture API (2026-06-04)** : pour produire un jar OmenCapture (`mc-capture-mod/`) en **1.21.11** (démarrage des REC), 2 blocages successifs. **(a) unpick v3** : les mappings yarn ≥1.21.6 sont en unpick v3, illisibles par fabric-loom 1.10.5 (`Failed to setup Minecraft: Unsupported unpick version`) → toolchain bumpée à **loom 1.16.3** (`build.gradle`) + **Gradle 9.4.1** (`gradle-wrapper.properties`) = combo du template Fabric officiel, compatible 1.20.1→1.21.11 (couplage loom↔Gradle, extension de #35 ; loom 1.16 exige Gradle 9.x). **(b) rupture API MC 1.21.6+** : `new KeyBinding(...)` 4ᵉ arg passé de `String`→`KeyBinding.Category` (record, champs builtin `MISC`/… ou `create(Identifier)`) ; `GameProfile.getName()` supprimé (authlib devenu record → `name()`). Corrigé en **référence DIRECTE** : `new KeyBinding(..., KeyBinding.Category.MISC)` + `params.name().getString()` (émetteur chat) + `client.player.getName().getString()` (nom local). ⚠️ **PIÈGE RUNTIME (vécu)** : ma 1ʳᵉ tentative en **réflexion** (`Class.forName("net.minecraft.client.option.KeyBinding$Category")` pour rester multi-version 1-codebase) **COMPILE mais CRASHE au lancement** (`Could not execute entrypoint stage 'client'`) — loom remappe au build les noms de classes/membres MC (yarn→intermediary) **mais PAS les String** : `Class.forName(<yarn>)` cherche un nom inexistant sur le client remappé → `ClassNotFoundException` → fallback ancienne API → `RuntimeException`. **RÈGLE FABRIC : jamais de réflexion par nom yarn — référence directe uniquement** (loom la remappe ; vérifiable en `javap -c` sur le jar remappé : 0 `Class.forName`, un `getstatic class_304$class_NNNN` pour la Category). Le multi-version 1-codebase à travers une rupture d'API est donc **impossible en réflexion** → code ciblant **MC 1.21.6+** (vrai multi-version = source sets, 1 fichier compat/génération). `RecHud`/`HudRenderCallback` = simple warning de dépréciation, pas une erreur → inchangé. **Méthode clé** : inspecter l'API EXACTE de la nouvelle version via `javap` sur le jar remappé loom (`~/.gradle/caches/fabric-loom/minecraftMaven/net/minecraft/minecraft-merged/<ver>…/…jar`) AVANT d'écrire le fix (≠ deviner = cramer un build). **Bonus** : `build.gradle` n'avait jamais de `processResources { expand }` → les 7 jars committés embarquaient `"version": "${version}"` littéral (version invalide Fabric — latent car le mod n'avait jamais été chargé en jeu, seulement testé en logique pure) ; ajouté l'expand → jar 1.21.11 ship `0.1.0`. La matrice `build-all-versions.sh` skip les jars déjà présents → bump toolchain SANS rebuild des ≤1.21.5 (zéro régression). ⚠️ Le mod n'avait **jamais été chargé en jeu** avant les REC : la version réflexion a crashé au 1ᵉʳ lancement (cf. ci-dessus), la version directe est **bytecode-vérifiée** (0 réflexion) mais le CHARGEMENT complet (F8→HUD ● REC→`.jsonl`) reste à confirmer côté client. Anecdote : MC est passé au **versioning calendaire** (template Fabric master = MC **26.1.2**).

44. **MC Agent humanisation — humanize OFF par défaut + la capture v0.1.0 ne loggue que tick/chat (2026-06-08)** : le bot « trop facile à deviner » de Massii avait 2 causes. **(a) Humanize ÉTEINT** : `index.js` `HUMANIZE = STEALTH || --humanize` = **false par défaut** → les bots tournaient vitesse-machine (réponse instantanée, texte verbatim, tête figée à l'arrêt, snap-aim, lignes droites) ; et même ON, `humanize.js` ne touchait QUE le chat (latence+typos), `movementJitter`/`reaction` distillés mais **jamais consommés**. Fix paquet 1 : `start_session humanize=True` par défaut (bot face à de vrais joueurs ; mappers déjà oui via `start_mappers` ; resource via `start_for_bot` reste machine-speed) + **boucle `setInterval` bruit-de-visée idle** (`humanize.nextLook` mode idle = micro-dérive douce, gated HUMANIZE, jamais pendant dig/path/pvp, `bot.look(...,false)` interpolé). **Vérifié live** : yaw dérive de quelques° à l'arrêt (≠ ancien : figé). **(b) Trou de données capture** : le mod OmenCapture ne loggue que `tick`/`chat_in`/`chat_out` — **AUCUN `mob_appear`/`damage`/`attack`** → `mc_capture_distill._reaction_times` renvoie TOUJOURS `[]` sur de vraies captures → **temps de réaction non calibrables** (et chat ultra-rare : solo n=5). Conséquence : la **motricité (yaw/pitch, 1,5M échantillons)** est la SEULE donnée riche → c'est elle qu'on modélise ; la **réaction** utilise un défaut humain ~300ms (`humanize.sampleReactionDelay`, plancher 120ms) câblé sur les réflexes (`reflexes.js` `reactionMs`/`schedule` injectables, **défaut 0 = synchrone** pour la rétro-compat des tests ; `index.js` passe le vrai délai, toujours actif = anti aimbot 0ms). Enrichir le mod (events combat) = prérequis d'une vraie calibration réaction (futur). **(c) Posture évasive** : `brain.SYSTEM_PROMPT` + le profil injectent « ne jamais avouer être un bot » (remplace l'ancienne clause d'honnêteté « exercice de modération » ; test pinné mis à jour) — décision Massii. L'invariant `buildSystemPrompt(null)===SYSTEM_PROMPT` (#38/#39/#40) tient. **(d) Survie sans armure** (paquet 2) : `survival.combatDecision` est armor-aware (`isArmored` slots 5-8) → SANS armure le bot décroche dès PV≤12 / 2 hostiles (anti « mort par combo » — un coup de 9PV pouvait tuer avant le seuil de fuite) ; AVEC armure = seuils courageux historiques. `fleeFrom` fuit le HOSTILE le plus proche (kind 'Hostile mobs' OU set de noms incl. creeper) au lieu de N'IMPORTE quel mob (fini la fuite d'une vache). Le timer armure-craft de `c3f4d81` reste **resource-only** (l'étendre = risque concurrence, paquet 2b). **Aucune nouvelle dépendance** → auto-deploy propre. Worktree `worktree-feat+mc-agent-humanize`, TDD (Node 557 + Python 257). Artefacts diag/data hors-repo : `~/mc-captures-work/`.

45. **MC Agent pilier/stuck + enrichissement events capture (2026-06-08)** : 3 fixes. **(a) « monte mal en pilier »** = `mineflayer-pathfinder` ne toure (`allow1by1towers`) qu'avec `Movements.scafoldingBlocks`, **défaut = dirt + cobblestone UNIQUEMENT** (lib/movements.js:75-77). Un bot qui mine de la deepslate n'a que cobbled_deepslate/tuff → AUCUN bloc utilisable → bloqué au fond d'un tunnel/trou. Fix : `moves.scafoldingBlocks = ids(cobblestone, cobbled_deepslate, tuff, stone, deepslate, dirt, gravel, granite/diorite/andesite, …)` dans le setup Movements de onSpawn. **(b) « se bloque »** = `recoverFloating`/`isFloatingStuck`/`clearSnares` (unstuck.js) étaient définis + testés mais **JAMAIS branchés** (code mort) ; le jam-watchdog ne couvre que les blocages AVEC goal pathfinder actif. Fix : watchdog `setInterval` 2 s qui échantillonne pos+sol+eau+vy → coincé-flottant (mvt horizontal ≈0 ET |vy|≤0.12, donc PAS en chute/saut) → `recoverFloating` (relâche tout + `clearSnares` lianes/toiles + laisse retomber). La **garde vy** (param ajouté à `isFloatingStuck`) évite le faux positif en pleine chute. **(c) Enrichissement mod capture** (comble le trou #44) : ajout `mob_appear`/`damage`/`attack`/`block_break`, détectés **CLIENT-side** (pas d'event serveur fiable en multijoueur) — damage = chute de PV au tick ; mob_appear = scan `client.world.getEntities()` throttlé (`instanceof HostileEntity`, ≤16 blocs, `seenMobs` anti-doublon, ré-apparition après éloignement = nouveau stimulus) ; attack = `AttackEntityCallback` (Fabric, retour `ActionResult.PASS`) ; block_break = bloc visé (`client.crosshairTarget` → `BlockHitResult`) qui passe à air pendant `attackKey`. Types JSON **alignés sur `mc_capture_distill.py`** (mob_appear/damage → `_reaction_times` ; attack → contexte clip 'combat') → les FUTURES captures permettront enfin la calibration réaction. Logique pure (SessionWriter/Recorder) JUnit 10/10 ; détection MC validée au build. APIs **version-stables** (≠ KeyBinding.Category #42) donc compileraient sur ≤1.21.5 SI la source compilait — mais elle cible 1.21.6+ → **seul le jar 1.21.11 est rebuildé** (= la version des 3 joueurs) ; les ≤1.21.5 restent figés sans events (refonte source-sets requise pour les enrichir, non fait — inutile tant que personne ne capture sur ces versions). Jar vérifié : v0.1.0 (expand OK), 4 méthodes write* présentes, **0 réflexion** (javap). Build d'UNE version : `rm dist/mc-capture-0.1.0-mc<v>.jar && JAVA_HOME=<jdk21> ./build-all-versions.sh <v>` (le script **skip les jars déjà présents**).

46. **Harvester CAPTCHA human-in-the-loop — vue live noVNC + x11vnc (2026-06-21)** : (a) **x11vnc 0.9.x — piège vicieux des flags réseau de la live-view** (root-causé LIVE le 2026-06-21 ; mon 1er fix `-localhost` issu de la revue CASSAIT la feature en prod). Deux contraintes : (1) x11vnc **EXIGE un listener TCP** — le désactiver via `-rfbport -1`/`-noipv4` le fait planter `could not obtain listening port` (crash-loop). (2) **NE JAMAIS utiliser `-localhost`** : il applique un contrôle « client == 127.0.0.1 ? » **AU PEER DU SOCKET UNIX** (qui n'a PAS d'IP) → x11vnc envoie le greeting RFB **puis ferme** la connexion → la vue noVNC ne se rend JAMAIS (`disconnect clean=false` côté noVNC ; `BrokenPipe` quand le client répond au greeting). **Fix correct = `-listen 127.0.0.1`** (borne le TCP IPv4 à loopback SANS le contrôle d'allow → le socket Unix, chemin du bridge, fonctionne). Résidu assumé : un `[::]:5900` IPv6 que `-noipv6`/`-rfbportv6 -1`/`-listen6 ::1` n'enlèvent PAS avec `-listen` → joignable seulement via le Tailscale de l'Omen (PAS internet : 5900 non tunnelé). **Diag décisif** = handshake RFB en Python DIRECTEMENT sur le socket (`recv(12)`=greeting OK mais `sendall(version)`→`BrokenPipe` = x11vnc a fermé) ; `ss -x \| grep <sock>` = nb de connexions du bridge ; le canvas noVNC s'extrait via `canvas.toDataURL`. ⚠️ **Throttling onglet background** : le poll 3s du dashboard tombe à ~60s quand l'onglet Chrome est en arrière-plan → le canvas ne se connecte pas tant que l'onglet n'est pas au premier plan (réflexe verif-UI). ⚠️ `_connectVnc` ne reconnecte pas si le 1er RFB d'un job échoue (garde `_rfb && _rfbJob===jobId` → return) → fix : listener `disconnect` qui nullifie `_rfb`. (b) **Détection de résolution = TITRE-only** pendant l'attente manuelle — PAS `is_challenge_html` : Cloudflare injecte le beacon `/cdn-cgi/challenge-platform` sur TOUTES les pages d'un site protégé (même résolues) → `is_challenge_html` faux-positive → (1) on ne sortirait jamais (stall jusqu'au timeout), (2) le `goto` de refresh, étant DANS la boucle de poll, re-naviguerait toutes les 3s et **effacerait la résolution humaine en cours via noVNC**. Règle : pendant l'attente, **lecture seule, ne JAMAIS re-naviguer** ; résolu = `not is_challenge(title)` (CF auto-navigue vers le contenu après résolution). (c) **Subprocess détaché possède la page Chrome, uvicorn est un autre process** → la vue live ne peut PAS partager l'objet page → bridge **WS FastAPI admin-gated ↔ socket Unix x11vnc** (`asyncio.open_unix_connection`, pump d'octets RFB verbatim = mini-websockify) ; gate `_vnc_authorize` = admin + job_id valide + job `awaiting_solve` (sinon close 1008). (d) **Export client strip `manual_solve`/`manual_solve_timeout`** (comme `unblocker_key`) : côté client = ni dashboard ni noVNC ni Telegram → un CAPTCHA stallerait le run jusqu'au timeout. (e) **Test WS Starlette TestClient non-discriminant** : `websocket_connect` lève `WebSocketDisconnect` pour N'IMPORTE quel chemin (même inexistant) → un test « rejette le non-autorisé » passe AVANT même que la route existe. Rendre discriminant : cas autorisé + socket indispo → **accept PUIS close 1011** (le `with` entre, `receive` lève 1011) ≠ rejet pré-accept 1008. (f) **Sudo Omen** : `apt` n'est **PAS** NOPASSWD (seuls `systemctl`/`reboot`/`shutdown`/`rtcwake`/`true` le sont) → toute install de paquet exige le mot de passe de Massii (≠ moi). **SSH = Tailscale `100.108.50.70`** (IP STABLE, `Host omen` dans `~/.ssh/config`) — ≠ l'IP LAN DHCP du piège SSH ; `_dump_block` du StealthFetcher écrit un screenshot patchright `run_dir/blocks/block-N.png` = preuve visuelle de ce que le canvas afficherait.

47. **MC Agent mode SANS-GIVE (`--no-give`) + chaînes armure — run nether 2026-07-13** : objectifs `iron_armor`/`diamond_armor` (chaînes goals.js, prédicats purs `armorNeed`/`armorWornOk`, `ctxExtra.worn` = slots 5-8 hors `inventory.items()`) + flag `no_give` (/run → `--no-give` → memo respawn ; `RESPAWN_OBJECTIVES` couvre les chaînes armure). (a) **`bot.chat` n'existe PAS synchroniquement après `createBot`** (injecté par les plugins) → tout wrapper se pose à `bot.once('inject_allowed')`, sinon TypeError au boot (vécu). Le filtre `nogive.js isForbiddenCheat` bloque `/give /effect /enchant /xp /tp\s /teleport /spreadplayers /gamemode /summon /kill /setblock /fill` (PAS `/tpa*` — pas d'espace après « tp ») ; event `cheat_blocked`. (b) **Un bot NON-OP en sans-give n'a AUCUNE béquille** : les warps historiques (spawnpoint/anti-noyade/anti-camping/relocate `/spreadplayers`) deviennent des no-ops → chaque secours doit avoir un fallback « vrai joueur » : watchdog eau (≥60 s continu → `escapeWater` forcé), abri-si-NU chaque nuit (l'ancien trigger attendait la 1re mort — trop tard en hard), décalage À PIED 30-50 blocs après un échec-eau de `descendDiagonal` (sinon re-perçage du même aquifère en boucle). (c) **Le fer de SURFACE est introuvable** (re-confirmé : 24 sessions death_loop en anneaux explore 240 stériles + noyades) → le fer des chaînes armure vient du **branch-mine Y16** (descend_y16 → iron_deep), jamais de `gather` explore. (d) **Un but nourriture BLOQUANT stalle à vie** (`no_prey` quand la zone est vidée de ses proies) → chasse = hook best-effort borné 90 s pré-descente ; sous terre, famine = une mort keepInventory (récupérable), un stall = tout le run. (e) **Ne JAMAIS pusher pendant un grind** : restart uvicorn = process bots tués ET dict `_sessions` (self-healing) perdu → les morts suivantes ne sont plus relancées ; un restart uvicorn HORS deploy a aussi été observé (06:31, cause non élucidée). (f) **Frein #1 restant = churn bois↔profondeur** (casse d'outils → remontée → zone déforestée → roaming mortel) — buffer sticks ×4 aide mais T1 n'a PAS bouclé dans la fenêtre ; prochaines pistes : 2 pioches/craft, charbon comme combustible de fonte, coffre-relais au puits de mine. Stall planner → retry 90 s (gated NO_GIVE). Rapport : `.mc-fable-nether-overnight-report.md`.

48. **MC Agent — A\* de fuite NON BORNÉ = OOM 2 Go en 38 s (crash live `world_ax4`, 2026-07-25, fix `04e9731`)** : 5 crashs `FATAL ERROR: JavaScript heap out of memory` (heap 1,98 Go / limite V8 2,09 Go), **toujours juste après `survival:flee`**, un bot mourant 3× en <2 min. **Cause = 2 morceaux inoffensifs séparément** : (a) `mineflayer-pathfinder` livre `bot.pathfinder.searchRadius = -1` = espace de recherche **ILLIMITÉ**, jamais posé de notre côté ; (b) `fleeFrom` pose un `GoalInvert(GoalNear(menace,16))` dont l'heuristique est **NÉGATIVE** (s'éloigner fait toujours baisser `f`) → tant que le but n'est pas satisfait (bot **acculé/encerclé/enfermé**), l'A\* n'a aucune raison de s'arrêter et expanse jusqu'à l'OOM. **Le `thinkTimeout` (5 s) NE PROTÈGE PAS** : il borne le TEMPS, pas la MÉMOIRE — 5 s suffisent à allouer des Go. **Fix** : `movement.applyPathfinderBounds()` → `searchRadius = 256`, posé après `setMovements` dans `onSpawn` (propriété au niveau du bot ⇒ s'applique à TOUTES les recherches). **Pourquoi ça ne casse pas les longs trajets** (lire `lib/astar.js`) : `maxCost = searchRadius < 0 ? -1 : startNode.h + searchRadius` ⇒ ce n'est **PAS un rayon absolu** mais un **BUDGET DE DÉTOUR relatif à l'estimation directe** — un but à 1500 blocs (cartographes) reste atteignable, seule l'errance est coupée. **Règle générale : tout `GoalInvert` (fuite/éloignement) exige un `searchRadius` fini** — c'est la classe de buts insatisfiables par construction. **Ceinture+bretelles** : `NODE_HEAP_MB = 640` → `--max-old-space-size` ⚠️ **AVANT `index.js`** (une option node placée après le script part au script, pas à node) ; le défaut V8 ≈ 2 Go/process × 5 bots = 10 Go réclamables sur une machine qui en donne déjà 10 au serveur MC → un emballement pouvait emporter le serveur ; plafonné, le fautif meurt vite et le self-healing relance (régime normal 174-290 Mo). **Leçon méthodo** : l'audit statique (Node 960 ✓ / Python 637 ✓ + revue) disait « rien à ajouter » — **c'est le LANCEMENT qui a dit la vérité** ; sans les `session-*.jsonl` persistés (`f19341c`) le crash restait invisible (les rings mémoire meurent avec le process).

49. **Anti-xray : la métrique honnête est « cachés SECS sous Y64 »** (run `world_ax4`) — un monde neuf avec `anti-xray enabled/engine-mode 1` chargé AU BOOT donne **0,8 %** de minerais `exposed:false` **secs** sous Y64 (réf. « fuite ≈ 0 » = 2,7 % du run water-wall ; **20 % = obfuscation MORTE**). Compter les cachés bruts trompe : ici 9,8 % bruts dont **58/63 `wet`** — Paper envoie légitimement les minerais adjacents à l'eau (l'anti-xray ne masque que l'entouré-de-solide), plus la lave (`lava-obscures:false`) et l'update-radius 2 autour des minages. **Toujours ventiler wet/sec avant de conclure.** Rappel opératoire : la config Paper n'est lue qu'AU BOOT du conteneur (piège vécu : écrite 21 s après le démarrage = jamais chargée) — un monde NEUF créé sur une config déjà en place hérite correctement, sans restart supplémentaire.

50. **Sonder le terrain par RCON — le bon compteur est `clone … filtered`, PAS `fill X replace X`** (⚠️ **corrigé le 2026-07-26 : la version précédente de ce piège était FAUSSE**). `fill <box> X replace X` ne compte **que les blocs dont l'état diffère de l'état par défaut du bloc posé** — un bloc identique n'est pas « rempli », donc pas compté. Preuves mesurées sur `world_ax5` : `dirt`/`stone`/`grass_block` rendent « No blocks were filled » dans une boîte qui en contient des milliers ; `oak_log` rend **9** au 1ᵉʳ passage (les seules **branches horizontales**, `axis≠y`) puis **0** au 2ᵉ (elles ont été normalisées) ; `oak_leaves` rend 840 parce que les feuilles portent un `distance` variable. ⇒ le fameux « 0 bûche pour 960 feuilles » n'était **pas** une boîte Y mal placée, c'était ce mécanisme. **Compteur correct, non destructif à la source** : `clone <x1 y1 z1> <x2 y2 z2> <dest> filtered minecraft:<bloc>` → « Successfully cloned N block(s) » ; destination = même emprise XZ ~150 blocs plus haut (air pur, chunks déjà chargés, zéro worldgen), puis `fill … air` pour nettoyer. Témoins sur 21×21 y50-119 : grass 428 / stone 17548 / dirt 1934 / **oak_log 72** / leaves 840 / diamond_block 0 (le compteur discrimine). Contraintes qui restent vraies : (a) plafond **32768 blocs** par commande (21×21×70 = 30870 passe) ; (b) **la boîte Y doit couvrir le sol** — et sur un monde montagneux le sol peut être **au-dessus de y119** (mesuré : grass=8 à (128,128)) ; (c) chunks non chargés → `forceload add` puis `forceload remove all` ; (d) `locate biome` mesure **depuis le world-spawn** et rend le point d'ENTRÉE du biome → `execute positioned <x> <y> <z> run locate …` pour mesurer depuis un point précis ; (e) **Essentials intercepte `/time`** → `minecraft:time set day`. **Règle : valider toute méthode de sondage avec un témoin ABONDANT (grass/stone) ET un témoin ABSENT (diamond_block) avant de conclure à une absence.**
51. **`f19341c` « un deploy ne tue plus la flotte » était INCOMPLET — il manquait `KillMode=process`** (trouvé + corrigé live 2026-07-25, mon push d'un simple `CLAUDE.md` a tué 5 bots en plein grind). `start_new_session=True` change la **session/pgid**, PAS le **cgroup systemd** : or `omenserver.service` n'avait aucune directive `KillMode` → défaut `control-group` → à chaque `systemctl restart` (= à chaque auto-deploy, toutes les minutes si nouveau commit) systemd SIGKILL **tout le cgroup**, subprocess Node compris. Le registre + `adopt_orphan_sessions()` ne servaient donc jamais : il n'y avait plus rien à adopter. **Fix (côté machine, exige sudo — action Massii)** : `sudo systemctl edit --force omenserver` → drop-in `/etc/systemd/system/omenserver.service.d/override.conf` contenant `[Service]` + `KillMode=process` (⚠️ nano : écrire AU-DESSUS de la ligne « Edits below this comment will be discarded », et l'en-tête `[Service]` est obligatoire) ; `systemctl edit` fait le `daemon-reload` seul, **aucun restart nécessaire** (la flotte en cours survit). **Vérif** : `systemctl show omenserver -p KillMode` = `KillMode=process`. **PROUVÉ live** : `systemctl restart omenserver` → PIDs des bots **identiques** avant/après, 5 joueurs toujours connectés, et les 5 sessions **ré-adoptées** par le backend (`/active` les remonte `spawned`). Conséquence pratique : la règle « ne JAMAIS pusher pendant un grind » (#47e) **tombe** une fois ce drop-in en place. ⚠️ Contrepartie assumée de `KillMode=process` : si uvicorn meurt brutalement, les bots deviennent orphelins vivants — c'est VOULU (l'adoption les récupère), le nettoyage reste à `stop_session` (killpg).

52. **Carte MC « pleine de trous » = ÉCHANTILLONNAGE, pas affichage (diagnostic + fix 2026-07-25, `001aaaa`)** : retour Massii « il manque des biomes ». Mesure sur `world_ax4` : **68 cellules 128² peintes pour une emprise de 21×18 = 378 → 18 %**, 310 trous **dont 7 seulement entourés** (≥5 voisins peints) ⇒ ce n'est pas de l'oubli, c'est du terrain jamais échantillonné. Débit : ~19 cellules/h à 2 mappeurs (**1 case / 6 min / bot**) parce qu'ils patinent — MapBot2 = 36 `mapper_blocked` / 24 `unstuck cause:water` contre **11 `biome_seen` sur toute sa vie**. (a) **Hypothèse plausible RÉFUTÉE par la donnée** (à ne pas re-supposer) : je croyais l'échantillonnage des voisins à ±128 blocs mort à cause de `view-distance=6` (96 blocs) → en fait **73-78 % des cellules émises venaient de là**. **Signature d'analyse** (avant fix) : une cellule échantillonnée était émise au **centre exact** (`x,z ≡ 64 mod 128`), une cellule lue sous le bot porte sa position réelle → on trie les deux dans n'importe quel `session-*.jsonl` (depuis le fix, une cellule échantillonnée porte un **bord** : `x ou z ≡ 0 ou 127 mod 128`). ⇒ **le levier est la LECTURE, pas la marche.** (b) **PORTÉE RÉELLE DU CACHE CLIENT = 96 BLOCS, mesurée** (bot sonde qui se connecte, attend 12 s et lit `blockAt` par paliers : **6/6 lisible à 32/64/96 b, 0/6 à 128 b et au-delà** = exactement `view-distance=6`). Corollaire majeur : **viser le CENTRE d'une cellule voisine (64-192 b) ne répond qu'en bord de cellule** — d'où la couverture en traînée. Fix : on vise le **POINT LE PLUS PROCHE** de la cellule voisine (son bord, 0-128 b) — une cellule ne porte qu'UN biome quantifié, n'importe quel point dedans fait l'affaire. Les anneaux **≥2 ne répondront jamais** à view-distance 6 (bord à ≥128 b) : l'élargissement 1→3 anneaux est gratuit (arrêt dès qu'un anneau ne rend rien) et **prêt** si Massii monte la view-distance — c'est LE levier ×5-10 restant (⚠️ #43c : worldgen × N mappeurs = freeze serveur, monter par paliers). Autres fixes `mapper.js` : **l'eau est cartographiée** (« terre-only » borne le DÉPLACEMENT, pas la carte — le filtre `/ocean|river|water/` faisait un trou permanent à chaque rivière ET, ces cellules n'étant jamais persistées, `isOceanCell` n'avait **jamais** de données → l'évitement de l'océan, codé depuis #5, ne s'était jamais exercé) ; cellule marquée « faite » **APRÈS** lecture réussie (un `blockAt` null au spawn/TP la condamnait à vie) ; `frontierSkip` Set définitif → **TTL 10 min**. (c) **Prérequis backend découvert en chemin** : `world_memory.save` réécrit le fichier du groupe (**8 Mo**, 35 k minerais) et coûte **~300 ms mesurées sur l'Omen** — or il était appelé **à CHAQUE event sous verrou global** (donc aussi à chaque `ore_mined` des mineurs) ; 48 cellules/arrivée = 14 s de blocage. → **debounce 1 écriture / 2 s** (`WM_SAVE_MIN_INTERVAL_S`) + `flush_world_memory()` appelé sur fin de session, `stop_session`, `start_session` (le bot lit ce fichier) et **la lecture de la carte** (`GET /servers/{sid}/memory` — sinon le dashboard afficherait une carte en retard). **Règle : tout point qui LIT le fichier depuis le disque doit flusher d'abord.** (d) **Artefact connu et assumé** : l'échantillonnage lit au Y du bot → sous terre il peint des biomes de GROTTE (`dripstone_caves` vus dans la carte) sur les cellules voisines ; 3 anneaux amplifient ça. Non corrigé (le garde-fou « remonter à la surface » limite le cas ; `add_biome` autorise plusieurs noms par cellule → un passage en surface repeint par-dessus).


52. **Analyse du run `world_ax4` (26/07) — 3 causes racines, et ce que 406k events apprennent** (rapport : `.mc-ax4-report.md`, correctifs `002a06c`). Run de 6 h 36 : **T1 franchi pour la 1re fois** (3 pièces d'armure fer PORTÉES + bouclier, après la 1re fonte réussie du projet) mais plafonné. **(a) Le goulot n'était NI le combat NI la fonte, mais le BOIS** : `logs` = 2001 tentatives / **1853 échecs (93 %)**, soit 46 % de tous les buts du run, pendant que `iron_deep` (minage) n'échouait qu'à 8 %. Cause exacte : `explore_directed` a visé la cellule (256,128) **2036 fois sur 2585** — celle que les bots avaient eux-mêmes rasée. **L'exclusion des cibles épuisées vivait dans un `Set` PAR PROCESS** (`bot._mcaExhausted`) → vidé à chaque session, et il y en a eu **134** : chaque session réapprenait que la zone était pelée, puis mourait. Pire, `directed_exhausted` n'était persisté que via `remove_find` (un *find* ponctuel) alors que la cellule revenait par le **tier BIOME** de `directedTarget`. Fix : `add_depleted` backend (persistant, partagé, dédupliqué, cap 400 en gardant les PLUS RÉCENTES — une zone pelée repousse) + `directedTarget` saute toute cible à ≤64 blocs d'une cellule épuisée, tous tiers confondus. **Règle générale : toute mémoire d'échec doit vivre dans la carte partagée, jamais dans le process — un bot qui meurt toutes les 3 min ne capitalise rien.** **(b) Boucle du bateau = 87 % de TOUS les events** : `mapper_boat_cross` 115 384 / `mapper_boat_failed` 115 342 dont **115 138 `no_crossable_water`** (40 490 dans une seule session), chaque échec relançant la même décision au même endroit, sans temporisation → CPU brûlé + **206 Mo de logs**. Fix : `boat.shouldRetryBoat` (bougé ≥32 blocs OU attendu 60 s). **Réflexe : un échec dont la cause est POSITIONNELLE ne doit jamais être retenté sans avoir bougé.** **(c) `no_table:no_space` ×71** : les 3 passes de `placeBlockNear` ne savaient poser que **SUR UN SOL** ; en jeu un bloc s'accroche à n'importe quelle face pleine → Pass 4 (6 faces d'un voisin plein) débloque le craft en tunnel. **(d) Acquis mesurés** : mortalité 53→20-34 morts/h (−45 %, et les heures basses sont de NUIT) ; **5 `Timed out` en 6 h 36** (le churn de charge historique est mort) ; les 161 déconnexions sont des cycles mort/réapparition, pas du réseau. **(e) La carte n'était PAS en cause** : 246 cellules de biome dont **55 boisées**, forêt la plus proche à 34 blocs, 53 071 minerais — le bot savait où était le bois et y retournait quand même à vide.

53. **Lire l'inventaire d'un bot MC en ligne : 3 méthodes sur 4 MENTENT** (vécu 3 fausses alertes « les items ont disparu » en une soirée, 25/07 — récidive du piège documenté en juin). (a) `data get entity <joueur> Inventory` = **TRONQUÉ** sur Paper (rend 1 item là où il y en a 20) ; (b) `data get entity <joueur> Inventory[{id:"minecraft:X"}]` marche mais **rend vide si le bot est déconnecté** (une session en cours de respawn ⇒ « tout a disparu ») ; (c) `clear <joueur> <item> 0` (comptage à blanc) **ne renvoie RIEN via rcon-cli** ; (d) le NBT playerdata brut **mélange l'inventaire et le LIVRE DE RECETTES** (`iron_helmet` peut n'être qu'une recette débloquée). **La seule source fiable = ce que le bot publie lui-même** (`kit_equipped.worn`, `teamStatus`, events de craft). Corollaire : **ne jamais annoncer une régression d'inventaire sur la foi de RCON** — vérifier d'abord que le bot est en ligne, puis croiser avec ses propres events. Les slots confirment le port : 100 bottes · 101 jambières · 102 plastron · 103 casque · 106 main secondaire.

54. **Écarts au JEU HUMAIN (recherche 26/07, 128 règles vs audit code)** — 4 confirmés + 2 livrés ensuite, tous corrigés. **(a) Le tunnel T1 se creusait DANS LE NOIR sans survie proactive** : `branchMine` sait poser des torches (`torchEvery`) et faire tourner un tick de survie (`onSurvivalTick`), la chaîne ne les lui demandait JAMAIS — or c'est la phase la plus longue de T1 (15 min), block-light 0 ⇒ les mobs apparaissent DANS le tunnel et le bot ne réagissait qu'après avoir encaissé. **(b) Une SEULE botte désactivait l'abri nocturne** (`naked = size === 0` → `< 4`) : en hard un bot à 3 pièces meurt presque aussi vite qu'un bot nu. **(c) Aucune ARME dans la chaîne T1** — 235 combats menés au POING (1 dégât vs 5) ; l'épée existait mais dans la chaîne du kit cartographe. But `t1_sword` (2 cobble + 1 bâton), satisfiable-ou-sautable. **(d) `defendIfNeeded` CHARGEAIT les creepers** pendant la récolte (s'approcher l'amorce ; ~64 dégâts à bout portant = 3× les PV d'un bot nu) → set `NO_MELEE`. **(e) AUCUNE notion de DURABILITÉ** = cause probable de la perte de T1 (un bot à 3 pièces + bouclier s'est retrouvé nu, l'équipement avait CASSÉ) → `gear.isNearlyBroken` (85 %) et, câblage minimal, une pièce à bout de course **ne compte plus comme portée** (`_wornArmor`) ni équipée (`ctxExtra.offhand`) ⇒ `armorNeed` la recompte manquante et la chaîne la reforge pendant qu'elle protège encore. **(f) Les BÛCHES étaient brûlées telles quelles** : 1,5 fonte contre 6 pour leurs 4 planches = **75 % du bois gaspillé**, alors que le manque de combustible bloquait la fonte → `smelt.logsToConvert` (convertit avant d'allumer, seulement sans charbon ni planches suffisantes). ⚠️ **Faux positifs de l'audit automatique, à connaître** : « le bouclier n'est jamais maintenu entre les coups » (mineflayer-pvp le fait déjà) et « le charbon n'est jamais ramassé » (`branchMine.js:191` le collecte ; ce n'était vrai que pour la descente diagonale). **Méthode : un audit de code produit par agent DOIT être re-vérifié à la main avant qu'on code quoi que ce soit.**

55. **Run `world_ax5` (26/07) — 14 correctifs en 2 h, dont un revert : ce que ça apprend** (passation complète : `.mc-ax5-report.md`). Run clôturé après 2 h 13 (87 morts ≈ 39/h, comparable aux 36/h d'ax4 ; anti-xray **1,7 %** cachés-et-secs sous Y64 = efficace ; carte 134 cellules dont 71 boisées) mais **0 pièce d'armure** — 6 recyclages de flotte ont mangé la progression. **(a) Le silence est un bug.** « Mourir de faim avec 64 steaks en poche » ne dit pas « il manque à manger », ça dit « MANGER ÉCHOUE » : `installReflexes` ne branche `tryEat` que sur l'event `health`, et l'action `equip`+`consume` a son échec **avalé** par un `.catch(() => {})` — or `equip` échoue quand le bot mine, ce qu'un ouvrier fait en permanence. Filet 5 s ajouté qui **émet** `ate`/`eat_failed`. Corollaire : `skills/shelter.js` creusait à **mains nues** (3 × `bot.dig` sans `bestToolFor`) → abri nocturne en minutes au lieu de secondes, à découvert. **Tout `catch` silencieux autour d'une action vitale finit par cacher un bug pendant des runs.** **(b) Quand un retour utilisateur nomme un COMPORTEMENT, identifier QUEL mécanisme le produit avant de couper le plus évident.** « Pas de pilier » m'a fait couper `allow1by1towers` du pathfinder : c'est de la **traversée** (franchir un ressaut), pas le pilier visible. Coupé partout → 1-22 blocs parcourus en 240 s ; coupé en surface seulement → **3 bots FIGÉS**, `unjam` en boucle à y=86-88 (« il place des blocs et les casse sans avancer »). Le pilier que voyait Massii était le pilier **délibéré** (`secureTactic`/`pillarUp`), lui seul devait partir. **C'est le LANCEMENT qui l'a dit, pas les 1153 tests.** **(c) Deux mesures FAUSSES à connaître** : le log Docker du serveur **n'a pas de dates** (filtrer `^\[12:` additionne des semaines → j'ai annoncé 133 morts pour 87 réelles ; compter depuis le n° de ligne du dernier `Starting minecraft server version`) ; et sur un monde **montagneux le sol peut être AU-DESSUS de y119**, une boîte de sondage y50-119 rend alors `grass=8` et fait conclure « désert ». **(d) `pkill -f "mc-agent/index.js"` tue les bots des AUTRES runs** (vécu : supprimé un bot de la flotte Minestrator en nettoyant celle de l'Omen) — les process ne se distinguent que par `--host`, filtrer dessus ; et ce motif écrit tel quel dans une commande SSH inline **tue la session SSH elle-même** (utiliser `inde[x].js`). **(e) `/locate` RETIRÉ des bots** (décision Massii) : commande d'op qui révèle des positions qu'aucun joueur ne peut connaître, même nature que le x-ray. Conséquence assumée : une ancienne cité sous un bot de surface n'est pas cartographiée (signature à 64 blocs, cité à y≈-50). **(f) Squad** : `regroup.squadLeader`/`squadTarget` (chef déterministe = nom le plus petit, seuil 64 blocs) — mais **c'est la FRÉQUENCE du contrôle qui fait la squad** : branché sur la boucle d'équipe à 90 s, les bots dérivaient de 300-485 blocs entre deux contrôles alors que le `/tpa` partait bien. Sorti en intervalle dédié 20 s. **Reste ouvert** : les suiveurs re-divergent parce qu'ils poursuivent leur propre exploration longue distance entre deux téléportations (piste : ancrer les suiveurs via `confine.js`).

56. **Migration du banc d'essai MC sur Minestrator + le bug de portée qui tuait les workers (2026-07-26)**. Le serveur de test n'est plus le conteneur Docker de l'Omen mais un serveur **Minestrator** (`91.197.6.81:29890`, MyBox 14 = **4 vCPU / 14 Go**), piloté par le **MCP officiel** `https://mcp.sttr.io/minestrator` (68 outils, inclus dans l'offre, clé dédiée « IA / MCP »). **(a) Ce qu'on gagne** : la RAM de l'Omen passe de **4 à 12 Go dispo** (le conteneur MC en prenait 10,5) ; et surtout **spark est embarqué dans Paper** et exposé par `get_spark_report` → profilage CPU du serveur, impossible sur l'Omen. **(b) RCON inutile** : pour Minecraft la console du panel atteint le jeu, `send_console_command` suffit (`fill`, `clone … filtered`, `forceload`, `locate biome`) — un mot de passe de moins à gérer. **(c) Contraintes de version** : `change_version` **refuse de redescendre** (les chunks d'une version récente deviennent illisibles) → passer de 1.21.11 à 1.21.4 impose `reinstall=true`, contenu effacé, snapshot obligatoire. Et l'anti-xray est une feature **Paper** : sur Fabric il n'existe pas. **(d) Config à reproduire** : `online-mode=false` + `enforce-secure-profile=false` (bots non-premium), `allow-flight=true` (#43b), `white-list=true`+`enforce-whitelist=true` (offline + IP publique = n'importe qui peut usurper un op), `difficulty=hard`, `view/simulation-distance=6`, `spawn-protection=0`, anti-xray `engine-mode 1` dans `config/paper-world-defaults.yml` **puis restart** (#49), `connection-throttle=-1` dans `bukkit.yml` (sinon `kicked: Connection throttled` dès qu'on lance une flotte ou qu'elle respawn). **(e) EssentialsX sans op** : les bots ont besoin de `/home`,`/sethome`,`/tpa` mais les opper leur rendrait les triches que `--no-give` interdit → écrire un `/permissions.yml` Bukkit qui met `essentials.home`/`sethome`/`tpa`/`tpaccept`/`back`/`spawn`/`afk`/`msg` en `default: true`. Sans ça : « `[Essentials] MapBot1 was denied access to command.` ». **(f) ⚠️ NE PAS redémarrer `omenserver`** (consigne Massii) : pour purger des sessions, `POST /api/mc-agent/stop/<sid>`, jamais `systemctl restart` — un restart perd le registre `_sessions` en mémoire et coupe le site. **(g) Piège shell** : `pkill -f "mc-agent/index.js"` lancé via SSH **tue son propre shell** (le motif matche la commande distante) → écrire `mc-agent/inde[x].js`. **(h) LE BUG** : `panicInFlight` était déclaré en `let` **dans `onSpawn()`** alors que les trois gates de survie qui le lisent vivent **au niveau module** → `ReferenceError: panicInFlight is not defined` au 1er tick du timer → **le process MEURT**. Les mappeurs survivaient (ils ne passent pas par ce chemin), les 3 workers non : quelques minutes de vie puis crash, en boucle, **sans erreur applicative enregistrée** (le registre les marquait juste `stopped`, `last_error: None`). Signature : 27 sessions `stopped` sur 29, `alive=2` sur 5. **Règle : une garde de ré-entrée lue par plusieurs fonctions doit vivre au niveau module comme ses sœurs (`_imminentBusy`, `_smeltOppBusy`, `_baseBusy`, `_armorBusy`) — un `let` dans `onSpawn` compile, passe les 1153 tests, et tue le bot en production.** Corrigé `775d4bc`.

57. **Observabilité autonome des runs MC — `~/mc-digest.py` et `~/mc-watch.py` (hors repo, sur l'Omen)**. Née d'une question de Massii posée trois fois : « comment tu analyses les bots sans que je te dise leurs erreurs ? ». **L'écart réel** : les events disent ce que les bots *pensent*, Massii voit ce qu'ils *font*. Les deux outils comblent ça. **(a) `python3 ~/mc-digest.py [minutes] [group]`** agrège les `session-*.jsonl` en : section **TERRAIN** (position, `armor`/`ingots`/`need` par worker, et **verdict de cohésion automatique** GROUPÉS/ÉTIRÉS/DISPERSÉS calculé sur l'écart max) — ces données viennent de `data/mc_agent_runs/positions-<group>.json`, qui contient déjà tout ; puis sessions + morts/h, **taux d'échec par but** avec marquage `<== GOULOT`, causes d'échec, **détection d'emballement** (`<== EMBALLEMENT` à ≥20 % des events), erreurs. **(b) ⚠️ FILTRER PAR GROUPE, sinon les compteurs mentent** : le filtre naïf par `mtime` embarque les sessions d'un run précédent encore fraîches — j'ai annoncé à tort « 16 800 échecs de bateau », puis « non, c'était les zombies », avant de trouver que c'était vrai. Le discriminant fiable est `sessions-registry.json` → `server_id`. **Ne jamais conclure d'un compteur d'events sans avoir vérifié à quel run il appartient.** **(c) `python3 ~/mc-watch.py [group]`** sort UNE ligne (`alive= sess= spread= armor= ingots= workers=`) faite pour un watchdog : un `Monitor` persistant la relit toutes les 2 min et n'émet que si `alive`/`armor`/`ingots` changent ou si un seuil casse (flotte incomplète, dispersion >128) — couverture des échecs ET des succès, pas seulement du bonheur.

58. **Sonder un monde de test : un seul point de mesure ne suffit pas, et `locate biome` rend une LISIERE (26/07)**. Trois runs ont ete perdus sur des mondes noyes ; le sondage est devenu obligatoire AVANT tout lancement. Methode : lire le spawn REEL dans la ligne `logged in with entity id ... at (x,y,z)` du 1er bot connecte (jamais (0,0)), puis compter les blocs par `clone <box> <dest> filtered minecraft:<bloc>` (piege #50), toujours avec un **temoin abondant** (`grass_block`) et un **temoin absent** (`diamond_block`). **(a) `locate biome` renvoie le point d'ENTREE du biome** = sa lisiere, pas son coeur : sur `world_mn2` la « foret la plus proche » (flower_forest, 257 blocs) s'est revelee **noyee** (3042 eau, 5 buches) et le point vise 25 blocs plus loin dans le biome etait un lac (2309 eau, 9 buches). Un seul sondage aurait fait retenir un monde mortel. **(b) Le vrai discriminant est le BOIS, pas l'eau** : mn2 etait sec au spawn (161 eau) mais n'avait que **5 buches sur 51x51** — un seul arbre — avec l'ocean a 45 blocs ; c'est exactement le goulot qui a tue ax4 (93 % d'echecs sur `logs`). `world_mn3`, retenu : spawn **dans** une foret (`is_forest` a 0 bloc), ocean a 647, **0 eau**, **183 buches sur 45x45**, 28 fer. **(c) Regenerer coute 2 min** (stop -> `level-name` -> start) : ne jamais s'entêter a rafistoler un mauvais monde en deplacant le spawn. **(d) Ordre efficace** : `locate #minecraft:is_forest` + `is_ocean` d'abord (2 commandes, auto-identifiantes donc batchables) — si la foret est loin ou l'ocean proche, regenerer sans depenser la batterie de clones. **(e) Essentials intercepte `/give`** et ne gere pas le selecteur `@a` (« Player not found ») -> `minecraft:give @a ...` (meme famille que `/time`, #50e).


60. **⚠️ SONDER NE DOIT RIEN LAISSER DERRIERE — le `clone … filtered` POSE reellement les blocs comptes (erreur commise DEUX fois, 26/07)**. Massii, photo a l'appui : « arrete de placer l'eau, c'est pas la premiere fois ». Ce n'etaient pas les bots — c'etait MOI. `clone <src> <dest> filtered minecraft:water` ne compte pas l'eau : il la **RECOPIE** dans la zone temoin. Poser 925 blocs d'eau a y=190 au-dessus du spawn, c'est une cascade qui retombe et inonde le terrain qu'on est justement en train d'evaluer (fait sur `world_mn2` : 161+3042+2309 blocs, puis `world_mn4` : 925). **Regle : tout sondage se termine par un `fill <dest> minecraft:air`** — les bûches et minerais laissent sinon des blocs flottants, et les fluides coulent. **Pour les FLUIDES specifiquement, l'air ne suffit pas** (l'ecoulement demarre au tick suivant le clone, avant tout nettoyage) : **remplir d'abord la zone temoin de `minecraft:stone`**, puis cloner le fluide dedans — il se retrouve enferme dans une matrice solide et ne peut pas s'ecouler — lire le compteur, puis `fill … air` qui emporte pierre et fluide d'un coup. Verification que le nettoyage a bien eu lieu : le `fill` final renvoie exactement le nombre de blocs poses (mesure `world_mn5` : clone 248+34 bûches → fill 282 ; clone 64 fer → fill 64). Corollaire : **choisir une zone temoin en AIR PUR bien au-dessus du relief** (meme emprise XZ, y+120 ou plus) et **verifier que la boite tient sous le plafond de construction** (y max 320 : une boite de 71 de haut ne peut pas commencer au-dessus de y=249).

61. **⚠️ UN CHAMP LU AU MAUVAIS NIVEAU NE PLANTE JAMAIS — il rend juste la fonctionnalite MORTE (trois cas le meme soir, 26/07)**. Classe de bug la plus couteuse du projet : le code s'execute, les tests passent, aucun event d'erreur — la branche est simplement toujours fausse. **(a) `loadPolicy` strippait `group_bots`** : le backend l'ecrit bien dans `policy-<sid>.json` (le roster du groupe), mais le parseur Node ne renvoyait que `{trusted, trade, kit_command}`. Or l'auto-accept du TP est `isTrusted(who, policy.trusted) || policy.group_bots.includes(who)` — et `isTrusted` renvoie **false quand `trusted` est vide** (tous nos profils). Les deux branches fausses ⇒ **aucun bot n'a JAMAIS accepte le /tpa d'un coequipier**. Symptome cote Massii : « les 5 bots ressources ne sont pas ensemble ». Mesure : **0 `/tpaccept` dans tout le log serveur**, 38 `squad_result:False` sur 47, un worker abandonne a 236 blocs. Casse en cascade : regroupement, entraide, TP-au-mappeur, livraison d'armure. **(b) `nextObjectiveAfter` lisait `m.status.need`** alors que `presence.beat()` **APLATIT** le statut dans l'entree (`{x,z,role,at,armor,ingots,need}`) : toujours `undefined` ⇒ la branche `iron_help` ne s'est jamais declenchee depuis son ajout. **(c) les CARTOGRAPHES ne publiaient aucun statut** (`st = role === 'worker' ? teamStatus(...) : null`) ⇒ pas de champ `armor` sur leur entree, un mappeur equipe indistinguable d'un mappeur nu. **METHODE qui les a tous trouves : ne pas lire le code, lire le FICHIER que le code produit** (`policy-<sid>.json`, `positions-<group>.json`) et le comparer a ce que le consommateur attend. **REFLEXE : un champ de config ou de presence traverse 4 couches (Pydantic -> `_clean_server` -> `resolve_policy` -> `loadPolicy`) et CHAQUE couche peut le stripper en silence (#15a) — apres tout ajout de champ, verifier le fichier reel ET un test qui fige la forme COMPLETE du retour, pas seulement le champ ajoute.**

62. **MC Agent — armurer les cartographes (26/07)**. Demande Massii : « les bots ressources se coordonnent pour preparer une armure pour chacun et apres il se tp au mappeur pour lui donner, comme ca les mappeurs continuent sans jamais s'arreter », et **avant** le diamant. Un mappeur ne mine pas : il meurt nu, donc il n'ose pas s'eloigner, donc il cartographie peu — c'etait meme ecrit dans le code (`pickDonation` ecarte `role === 'mapper'` : « les cartographes ne montent pas d'armure »). Cette regle est INVERSEE. **Enchainement** : `iron_armor` -> `iron_help` (coequipiers en manque) -> `mapper_armor` -> `diamond`, et `mapper_armor` reboucle tant qu'il reste un mappeur nu (un set ne couvre qu'un bot). **Chaine gouvernee par `ctx.mapperTarget`** : sans cible, TOUS ses buts sont satisfaits d'office et le bot enchaine — aucun etat a maintenir. On livre des **pieces FORGEES**, pas des lingots (un mappeur n'a ni four ni table). **Le worker fait le deplacement** (`/tpa` VERS le mappeur, jamais l'inverse) ; le mappeur s'equipe seul (`armorUp(0)` deja dans son `onPeriodic`). **Coordination** : la cible etant DETERMINISTE, les 5 workers viseraient le meme mappeur → reservation via le lockfile de claims partage, desormais passe a TOUS les bots du groupe (il ne servait qu'a l'anti-collision minerai) ; cache 15 s pour ne pas marteler le lock ; `_giftDone` TTL 5 min parce que le heartbeat publie encore le mappeur « nu » pendant 60 s apres la livraison ; un TP rate RELACHE la reservation pour qu'un autre worker prenne le relais.
59. **Deux invariants trouves au lancement du run `world_mn3` (26/07, `cf693c9`)**. **(a) La memoire de monde n'est PAS indexee par monde** : elle est rangee sous une cle de DIMENSION (`worlds.overworld`). En regenerant le monde de test, **78 025 minerais et 557 cellules de biome du monde mort survivaient** (fichier de 17,4 Mo) -> les workers marchent vers des filons inexistants, les mappeurs croient avoir deja cartographie. Meme classe que le cache de notations a la migration de source (#33j) : *le cache survit au deploiement, lui*. Fix : `POST /api/mc-agent/servers/{sid}/memory/reset` (admin) — **supprimer le fichier ne suffit pas**, le manager garde un cache RAM (`_wm_cache`) qui le ressusciterait au flush, il faut passer par `forget_group`. **Reflexe : purger AVANT de lancer la flotte a chaque changement de monde.** Piste de fond : indexer par identifiant de monde pour auto-invalidation. **(b) Le semis de tables de craft** (2 photos Massii) : **28 -> 44 tables** au sol dans une boite 45x45 en 9 min, pour seulement **7 `reclaim_failed`** — le compteur d'echecs disculpait a tort la reprise. Mecanisme reel : `withCraftingTable` marchait bien jusqu'a une table existante (`TABLE_SEEK` 48), mais le craft echouait quand meme **faute de MATERIAUX, pas faute de table**, et on retombait dans la branche « fabrique + pose une table neuve » — a cote de celle sur laquelle le bot se tenait. Une table semee par craft rate ; plus les tables abandonnees quand une mort (hard) ou un timeout coupait le cycle, ce que `reclaimBlock` avale en silence (il suppose « deja repris » quand il ne retrouve pas le bloc). Fix : decision pure `tablePlan` (`use_existing` si une table est a portee -> **ne rien poser** / `recycle` = aller REPRENDRE une table abandonnee / `place` / `craft_then_place`) + event `table_abandoned`. Verifie live : **+16 tables/9 min avant, +0 apres**. **Lecon transposable : quand la mesure au sol contredit les events, c'est le sol qui a raison — un compteur d'echecs bas ne prouve pas que le chemin est sain, seulement que le chemin fautif ne parle pas.**

63. **MC Agent — 3 homes (safe/work/death) + migration de zone (27/07, `fd0bd35` + `18b7b8b`)**. Deux demandes de Massii, dont la premiere s'est revelee etre un **BUGFIX silencieux**. **(a) LE SERVEUR PLAFONNE A 3 HOMES ET LE CODE EN POSAIT 4.** `sethome-multiple: default: 3` dans `/plugins/Essentials/config.yml` ; le code posait `safe`, `canchor`, `death`, `wsite`. Le 4ᵉ `/sethome` echoue **EN SILENCE** (aucune erreur, aucun event) et **lequel manque depend de l'ordre de pose** → chaque bot avait un filet de securite DIFFERENT troue. Preuve mesuree sur `world_mn5` : NethBot2 n'avait PAS de home `safe` — sa roue de secours anti-noyade (`/home safe`, le chemin le plus emprunte du projet : 47 `water_rescue_home_safe` en 8 min de run) etait un **no-op complet** ; NethBot5 n'avait pas de `wsite`. **Comment on le voit** : Essentials repond a un `/home <nom inexistant>` en LISTANT les homes existants — ces reponses sont dans les `session-*.jsonl`. Schema cible : `safe` = LA BASE (depot + repli + **ancre de confine**, `canchor` supprime : c'etait le meme endroit), `work` = le chantier (pose AVANT toute excursion, retour par `/home work`), `death` = la dette. 3 noms pour 3 slots, plus aucun echec silencieux. **⚠️ Menage obligatoire au boot** (`/delhome canchor` + `/delhome wsite`) : sans lui les 3 slots des comptes EXISTANTS restent pris par les vieux noms et les nouveaux `sethome` echouent, toujours en silence. **(b) La dette de mort doit etre PERSISTEE, pas un drapeau de process** : le self-healing relance le bot a chaque mort, or c'est precisement la qu'elle sert — un `_deathArmed` en memoire etait perdu au seul moment utile. Rangee dans le memo base (`data/mc_agent_base_<user>.json`). Borne : le despawn vanilla 5 min, ce qui rend « il revient encore et encore » **fini par construction**. **(c) `deliverMapperArmor` ne revenait JAMAIS** : `/tpa` vers le mappeur puis reprise de la chaine a l'autre bout de la carte, son puits de mine abandonne. C'est ce que le home `work` corrige (`markWorkBeforeTrip` / `returnToWork`) — verifie live : `work_bookmarked (33,16,61)` → `work_return (33,16,61)`, memes coordonnees. **(d) Migration de zone** : `zone.js` (pur) juge la zone COURANTE sur ses compteurs (eau / rendement fer / cellules epuisees / bois introuvable) avec **hysteresis 15 min + cooldown 20 min** — sans eux la flotte devient nomade et ne produit plus rien. Cible **DETERMINISTE** (tous les ouvriers calculent la meme → l'escouade migre ensemble) + claim partage. **Distance minimale selon le MOTIF** (demande Massii en cours de route) : 200 blocs pour une nappe d'eau ou une foret rasee (problemes LOCAUX), **600 pour un epuisement** — la cellule d'a cote est le meme sous-sol deja fouille par la meme flotte ; migrer de 200 blocs pour cause d'epuisement, c'est demenager dans la piece d'a cote. **(e) A l'arrivee, SUPPRIMER l'ancien `work`** (« il enleve leur vieux home et il le remet au nouveau safe place ») : il pointe sur le chantier de la zone abandonnee, un `/home work` y re-teleporterait des centaines de blocs en arriere. **(f) `effectiveConfine` — le point qui a mordu DEUX fois** : apres migration, le self-healing respawn avec le memo `--confine` de BOOTSTRAP et le keeper garde le sien → chaque mort ramenait le bot a l'ANCIENNE zone et l'enforcement l'y clouait. La **base persistee prime** desormais sur `--confine`, qui ne fixe plus que le rayon. **(g) `_migrating` DOIT compter comme `busy`** dans l'enforcement confine, sinon il yank le marcheur vers l'ancre qu'on essaie justement de quitter (meme classe que les autres gardes busy). **(h) 3a — bannir le chantier noye, pas seulement l'oublier** : l'oubli seul laissait la re-descente re-percer le MEME aquifere quelques blocs plus loin (118 sauvetages, 0 minerai). `noteDrownedSite`/`isDrownedNear` (rayon 16, TTL 30 min) + decalage 30-50 blocs sur un **cap qui TOURNE** a chaque essai — l'angle aleatoire retombait dans la nappe une fois sur deux. Perimetre strict respecte : `descendDiagonal` n'est pas touche.

64. **⚠️ `new Function(src)` NE VALIDE QU'UN PARSE — il ne charge rien (garde-fou ajoute le 27/07)**. Le reflexe « `node -e "new Function(readFileSync('index.js'))"` avant de pusher » (#28) a laisse passer les DEUX bugs qui ont tue la prod : (1) `index.js` requerait `./wsiteDrown`, jamais `git add` → **MODULE_NOT_FOUND avant `onSpawn`**, chaque spawn frais crashait, la flotte ne pouvait plus se self-heal — et le registre marquait juste `stopped`, `last_error: None` ; (2) piege #56, `panicInFlight` declare dans `onSpawn` mais lu au niveau module → ReferenceError au 1er tick, 3 workers morts, **1153 tests verts**. Un parse ne resout aucun `require` et n'evalue aucune reference. Fix : **`index.smoke.test.js`** charge REELLEMENT `index.js` dans un process enfant, avec `mineflayer` stubbe pour que `createBot` leve tout de suite — si le module s'evalue jusque-la, tous les `require` existent et toutes les references de niveau module resolvent. Verifie dans LES DEUX SENS (il echoue bien sur un require casse, il passe une fois repare). **Regle : pour tout gros point d'entree a effets de bord, le test qui compte est celui qui CHARGE le module, pas celui qui le parse.**

65. **La veille nocturne de l'Omen COMMITTE mais ne peut pas POUSSER — moissonner AVANT toute autre chose (27/07)**. L'Omen n'a aucun identifiant git : `git push` rend « could not read Username ». La veille corrige donc en local et son travail n'existe que dans son arbre — il a fallu le moissonner a la main **4 fois en 24 h** (`3bfd982`, `de1b3d9`, `e23216e`, `f5b0e04`). ⚠️ **`git status --porcelain` PROPRE NE VEUT PAS DIRE « rien a moissonner »** : ce jour-la l'arbre etait propre mais HEAD portait un commit local en avance sur `origin/main` — et ce commit contenait le correctif du MODULE_NOT_FOUND, donc `origin/main` etait **casse** pendant ce temps. **Toujours verifier `git log origin/main..HEAD`, pas seulement le porcelain.** Moisson : `git format-patch -1 <sha> --stdout` cote Omen → `git am` cote Mac (preserve auteur et message) → suites → push → puis **realigner l'Omen** (`git reset --hard origin/main` : meme arbre, hash different, sinon le `git pull` de l'auto-deploy diverge). Voie de sortie durable = **cle de deploiement SSH** (jamais un PAT : un jeton personnel ouvre tout le compte, une deploy key n'ouvre qu'un depot et se revoque d'un clic). La cle privee ne quitte JAMAIS l'Omen ; seule la publique se colle dans Settings → Deploy keys avec **Allow write access**.

66. **MC Agent — la soiree du 27/07 au soir : 4 pieges de diagnostic, tous vecus la meme nuit** (commits `d2b8ddf` → `1e87685`, dont 3 de la veille nocturne qui POUSSE desormais toute seule). **(a) ⚠️ UNE MEMOIRE D ETAT PAR PROCESS EST INUTILE QUAND LES SESSIONS REDEMARRENT SANS CESSE — et je l avais documente le matin meme.** La migration de zone (#63d) exige 15 min d hysteresis avant tout verdict ; son horloge vivait en variable de module. Or le self-healing relance un bot toutes les quelques minutes → l horloge repartait a ZERO a chaque respawn → les 15 min n etaient JAMAIS atteintes, la porte `too_soon` restait fermee, et **la migration n a pas tire une seule fois de la journee** alors que la flotte etait visiblement noyee. Aucune erreur, aucun test rouge, aucun event : la branche etait juste toujours fausse (meme classe que #52/#61). Fix : `zoneStateInit/Load/AfterMigration` (purs) + persistance dans le memo base, a cote de la dette de mort. Le `Load` refuse une horloge venue du FUTUR (memo herite d un autre monde) qui gelerait la zone a vie. **Reflexe : a chaque etat ajoute, se demander « qu est-ce qui arrive a cette variable au prochain respawn ? ».** **(b) Un goto rate passait pour une migration reussie** : `zone_migration_start` vers une foret a 236 blocs puis `zone_migrated dist:2 took_s:3` — le pathfinder rend `NoPath` en quelques secondes sur une cible lointaine (chunks non charges), on tombait dans la branche « arrivee », on re-ancrait la base 2 blocs plus loin et on BRULAIT le cooldown, bot toujours dans la zone morte. Garde `MIGRATE_MIN_PROGRESS` (64 blocs) + repli sur la marche par jambes de 128 (que le pathfinder sait faire). **(c) L anti-nomadisme clouait les bots 20 min dans une zone PROUVEE morte** : `logsNotFound:24` pour un seuil de 8, `depletedNear:10` pour 3, `ironMined:0` → verdict `stay`. Les garde-fous (15 min + cooldown 20 min) sont faits pour une preuve FAIBLE ; face a une preuve ecrasante (3x les seuils) ils passent a 4 min / 5 min. **Invisible sur un monde SONDE** (les bots n avaient jamais besoin de migrer — d ou les « 45 min pour une armure » de Massii) et mortel sur un monde brut. **(d) Un seuil de distance ne peut pas exprimer une regle CATEGORIELLE** : « les bots restent en surface » alors que le chef d escouade etait bien le mineur du fond — la distance etait mesuree en X/Z, donc un bot 44 blocs AU-DESSUS a 17 blocs a plat etait « assez pres » ; meme en 3D (47) on restait sous le seuil de 64. La regle voulue est categorielle : *chef sous terre + moi en surface ⇒ je descends*, quelle que soit la distance. **(e) Le meme parametre doit parfois s INVERSER selon le motif** (trouve par la veille, `5db0192`) : `minDistFor` valait 200 pour le bois comme pour l eau → `pickMigrationTarget` sautait la foret a 18-110 blocs pour viser une a 238-374, injoignable depuis le fond de la mine ; 13 migrations bois sur 16 echouaient. Pour un EPUISEMENT il faut aller loin (600 : la cellule d a cote est le meme sous-sol) ; pour le BOIS il faut la foret la PLUS PROCHE. **(f) Le chef de squad doit etre CELUI QUI TRAVAILLE** : `squadLeader` prenait le nom le plus petit — deterministe mais aveugle ; si ce bot trainait en surface toute la squad remontait le rejoindre. Le determinisme est conserve, seule la cle de tri change (mineur souterrain le plus productif, nom en departage). Prerequis : le heartbeat ne publiait NI `y` NI la productivite (piege #61 encore). **(g) Ne JAMAIS miner avec le mauvais outil** : `equipCached` faisait `if (!tool) return;` → sans pioche le bot gardait l EPEE de son dernier combat (plus lent que les mains nues sur la pierre) ; et rien ne verifiait que l outil RECOLTE le bloc — sans l outil requis le bloc casse et ne donne RIEN. `canHarvestWith` (tools.js, pur) gate tout `safeDig`. **(h) Ne pas casser a travers une paroi** : le ramassage opportuniste minait hors ligne de vue, le minerai tombait de l autre cote du mur — `branchMine` avait la garde `canSeeBlock`, pas lui. **(i) Une VRAIE reserve de combustible** (demande Massii) : `t1_coal` exigeait 3 charbons et en ramassait 5, `armor_fuel` se contentait du besoin EXACT de l instant → a chaque fonte le bot repartait en surface (churn bois↔profondeur). Passe a 16 charbons / `smeltNeed + 48` unites, ramassage de 24 — avec l echappatoire `smeltNeed === 0` (rien a fondre ⇒ aucun combustible exige), **rattrapee par un test EXISTANT**. **(j) Deux points de mesure ne font pas une tendance** : j ai annonce « la fonte prend le dessus » sur deux releves, dementi 7 min plus tard (286 minerais pour 4 lingots). **(k) LES OBSERVATIONS EN JEU DE MASSII ONT TROUVE LA MAJORITE DES CAUSES**, pas les mesures : « raison fausse » (ma cause racine « pas de pioche » etait fausse — c etait le rayon de scan a 5 pour une marche autorisee a 12), « il tape avec les mains », « ils sont toujours en surface », « il casse le fer a travers les blocs ». Les events disent ce que l agent CROIT faire ; l ecran dit ce qu il FAIT.


67. **Market Pulse (3ᵉ bot finance) — quatre pièges de SOURCE, tous trouvés par la sonde, aucun par le code** (2026-07-28, phases B+C). **(a) Une bougie à moitié écrite fausse la variation du jour.** L'API chart Yahoo renvoie la séance du jour avec `open` rempli et **`close: null`** tant que la clôture quotidienne n'est pas consolidée (observé sur ^N225 alors que la séance était finie ET que `regularMarketPrice` était à jour). Le parseur jetait la bougie entière (`o is None or c is None`) → la référence de clôture reculait d'une ou plusieurs séances → **Nikkei affiché -6,11 % au lieu de -3,95 %**, Shanghai -1,64 % au lieu de -1,16 %, et le gap du jour introuvable. Règle : **garder la bougie dès qu'UN côté est connu**, n'écarter que le point entièrement nul (férié/trou), et remonter jusqu'à la dernière clôture DISPONIBLE. ⚠️ `high if h is not None else max(o, c)` lève un TypeError dès qu'un côté est None — borner sur le côté connu. **(b) `misfire_grace_time` et `coalesce` ne rattrapent RIEN quand la machine était éteinte.** Le `BackgroundScheduler()` du dépôt n'a aucun jobstore persistant : au redémarrage il ignore tout des déclenchements manqués, or l'Omen dort de 01:00 à 06:00. Ces deux réglages ne couvrent qu'un retard du process VIVANT. Le seul vrai rattrapage est une décision explicite au démarrage (`should_catch_up(cfg, last_run_date, now)`, pure et testable) arbitrée par la date du dernier run persistée sur disque — avec une borne de retard, un instantané « d'ouverture » lancé à 23 h n'ayant plus de sens. Et le trigger doit porter un **`timezone=` explicite** : sans lui, « 07:30 » part à l'heure système, pas à celle de Rome. **(c) « HTTP 200 » ne prouve pas qu'un flux RSS est vivant.** `feeds.marketwatch.com/marketwatch/marketpulse/` répond 200 avec 30 `<item>` parfaitement valides… dont le plus récent a **treize mois** (9 367 h, mesuré). Un flux abandonné est indiscernable d'un flux sain sans **regarder les dates** → filtre de fraîcheur obligatoire + liste `stale_sources` qui SIGNALE la source périmée au lieu de recopier ses titres. Même famille : Reddit `.json` = **403** (mort, le plan initial en dépendait), Milano Finanza = 404, Reuters = injoignable, et un « Teleborsa » annoncé vivant par une recherche s'est révélé 404 à la sonde. **Ne jamais coder un parseur avant d'avoir sondé l'URL exacte.** **(d) Un titre de presse peut ÊTRE un conseil d'investissement** — « We're upgrading our rating on Boeing », « 3 stocks to buy right now », « Le 5 azioni da comprare » (vus en une de CNBC/MarketWatch). Un bot qui s'interdit toute recommandation ne peut pas les relayer : recopiés dans SON rapport, ils passent pour SON avis auprès d'un lecteur âgé. Filtre bilingue `is_advice` + **comptage** (`filtered_advice`) — on écarte, on ne masque pas. Idem `is_offtopic` pour le courrier des lecteurs que charrient les fils « top stories » (« My stepdad is dying from cancer… »). **(e) Trier par récence efface la langue du lecteur** : les fils américains sont horodatés plus tard que les dépêches italiennes → une simple troncature par date supprimait toute la presse italienne. Partage équitable par source AVANT de tronquer (mesuré : 14 titres it / 11 en). **(g) Une place peut ne pas publier de VRAI prix d'ouverture** — et le gap est alors nul par construction, pas par calme du marché. Mesuré sur 2 ans : **`^FTSE` = 94,8 % de gaps exactement nuls** (|gap| moyen 0,002 %, beta contre le DAX **0,006**), là où DAX/CAC/MIB/S&P sont à 0,6-1,6 %. Le bot affichait donc « FTSE 100 — gap ap. 0,00 % » **tous les matins** : un fait FAUX, même famille que (a). `gaps.open_is_degenerate` (pur, seuil 60 % sur ≥5 points) coupe le gap ET les statistiques historiques bâties dessus, et le rapport DIT pourquoi (« gap non calcolabile: questa borsa non pubblica un vero prezzo di apertura ») au lieu d'omettre en silence. ⚠️ Cas voisin NON supprimé car le gap y reste réel, seulement comprimé : **`^STOXX50E`** (beta 0,244 contre 0,79 pour le CAC) — son gap est systématiquement sous-estimé, à garder en tête avant d'en tirer une comparaison entre places. **Réflexe : avant de publier une métrique dérivée d'un champ, mesurer que le champ VARIE.** **(f) Il n'existe AUCUN future européen sur Yahoo** (`FESX=F`, `STXE=F` → 404) : l'ouverture européenne ne peut pas être « prévue » honnêtement, le rapport aligne les faits qui la précèdent et le dit explicitement.

68. **Market Pulse phase D — deux BRANCHES MORTES dans un moteur annonce complet, et cinq pieges de source (2026-07-30)**. Le moteur etait livre « COMPLET, 292 tests verts ». En lisant non pas le code mais **le fichier qu'il produit**, deux morceaux entiers n'existaient pas. **(a) L'agenda n'etait JAMAIS rempli** : `pulse/agenda.py` n'avait jamais ete ecrit et `main.py` n'a jamais passe `agenda=` a `build_briefing` → la section « appuntamenti » du contrat sortait vide a chaque briefing, **en ayant l'air normale**. **(b) Les sources sociales n'etaient JAMAIS appelees** : `social.py` savait *lire* Reddit/Bluesky/X (parseurs testes, fixtures reelles) mais **rien ne construisait les URL**, et les quatre options `reddit`/`bluesky`/`x`/`x_account` de `prefs.json` ne faisaient rien. Meme classe que #61 et #52a : le code s'execute, les tests passent, la branche est toujours fausse. **Regle : un test qui INJECTE une dependance ne prouve pas qu'on la BRANCHE — le test qui compte verifie l'URL DEMANDEE** (`Recorder.urls == []` quand rien n'est coche). **(c) Les calendriers de banque centrale : deux sur quatre sont en JavaScript.** Sondes a la main : **Fed** (`fomccalendars.htm`, 8 reunions 2026 + 8 en 2027, panneaux `<h4>2026 FOMC Meetings</h4>`) et **BoJ** (`mpmsche_minu/index.htm`, annee en legende `<caption>Table : 2026</caption>`, 1re `<td>` de chaque ligne) sont en clair ; **BNS** publie le seul RSS de banque centrale reellement date dans le FUTUR (58 items jusqu'en 2028, `<date>` ISO). **BCE et BoE repondent 200 mais leur calendrier est construit en JS** — la page BCE ne contient qu'UNE date, celle du jour. Elles passent par un fichier cure (`data/market_pulse/agenda.json`, gitignore), jamais par une date inventee : le lecteur n'a aucun moyen de la verifier. ⚠️ Le flux BNS est servi **du plus lointain au plus proche** (2028 en premier) : un parseur qui fait confiance a `items[0]` annonce 2028 comme prochain rendez-vous. **(d) Une date de reunion sur deux jours se date au DERNIER jour** (la decision) : « October 27-28 » se conclut le 28. Et un evenement date « au jour » doit rester valable **jusqu'a la fin de sa journee**, sinon la reunion du jour disparait de l'agenda a 00:00:01 — le jour meme ou elle compte. Horizon 7 jours et pas 48 h : a 48 h la section est vide cinq jours sur sept, et une section vide se lit « rien ne se passe », ce qui est faux. **(e) ⚠️ `chartPreviousClose` de Yahoo DEPEND DE LA FENETRE DEMANDEE** — piege de VERIFICATION, pas de production, et il m'a fait accuser le bot a tort. Mesure sur ^N225 : `range=5d` → 64 611,15 ; `range=1mo` → 70 062,32. Ce champ est la cloture *precedant la fenetre*, pas la seance precedente (et `previousClose` est `None` sur cet endpoint). Recouper une variation quotidienne avec lui donne des ecarts de plusieurs pourcents sur des chiffres justes. **La seule reference honnete est la serie de bougies** : les 7 valeurs du briefing recoupees contre `close[-2] → regularMarketPrice` collent a 0,01 point pres. **(f) Le CLI Claude n'est pas au meme endroit sur les deux machines** (`~/.local/bin/claude` sur l'Omen, dans le nvm sur le Mac) : code en dur, la synthese tombait en mode degrade **silencieux** sur le Mac — une fonctionnalite morte qui a l'air de marcher. `claude_bin()` cherche `CLAUDE_BIN`, puis l'Omen, puis le `PATH`. **(g) Ne pas donner de devise a un indice** : Yahoo etiquette `^N100` en EUR et le LLM a ecrit « l'indice Euronext 100 a 1907,2 **euro** ». Un indice est un niveau. On ne lui donne plus l'occasion de se tromper. **(h) Trois defauts trouves en LISANT la sortie, pas le code** : le fil general de Reuters mettait « Detained Ugandan opposition figure is unconscious » sous le nom de la Borsa di Milano (→ comptes X finance seulement) ; les posts sociaux faisaient « decouvrir » le titre `NEXT` extrait de « NIKKEI NEWS NEXT » (→ la decouverte de titres tourne sur la PRESSE seulement, une depeche est de la prose editee, un post est une soupe de hashtags) ; un post Bluesky de 300 caracteres avec son URL recopiee au milieu ecrasait les cinq titres suivants (→ URL nue retiree, coupe a 190 caracteres sur un mot). **(i) L'analyse graphique est une PREVISION** : « Il supporto del 38,2% di Fibonacci … potrebbero preparare un rimbalzo » (mesure sur Bluesky) n'est pas un « achetez » mais recopie sous le nom de la bourse ca se lit comme la prevision DU BOT → `is_advice` ecarte fibonacci / analisi tecnica / « potrebbe salire|scendere » / canale ribassista. **(j) Dix operateurs ne font que CINQ ouvertures** : la cle d'un groupe doit etre **l'instant UTC**, pas la liste des bourses — retirer Londres de la selection renommerait le groupe, le rattrapage croirait qu'il n'a jamais tourne et relancerait un run. Calculee sur une date de reference fixe : c'est un identifiant, l'heure d'ete ne doit pas le renommer en novembre. Et **decocher une bourse doit RETIRER son job** (`get_jobs()` + prefixe), sinon l'ancien reveil reste arme et produit un briefing pour une place qu'on ne suit plus. **(k) La traduction des titres etrangers, et deux choses vues seulement A L'ECRAN (2026-07-30 bis).** Couvrir Tokyo avec de la presse japonaise etait un choix de conception ; le prix a payer etait un briefing rempli de titres illisibles. Les titres etrangers sont traduits vers une langue de LECTURE reglable (`opzioni.lingua`, italien par defaut) et la traduction voyage dans le **MEME appel LLM que la synthese** — zero requete de plus. Trois regles : le titre d'origine n'est jamais ecrase (`title_display` s'ajoute, l'original reste affiche dessous — une traduction est une transformation, on doit pouvoir verifier) ; sans traduction on montre l'original et on le DIT (« non tradotto ») plutot que de masquer la news ; et **`is_advice` tourne AUSSI sur la traduction** — il ne connait que l'italien et l'anglais, donc un titre chinois qui dit « les 5 actions a acheter » traverse la collecte sans encombre et ne se revele qu'une fois traduit (le garde-fou en a attrape un des le premier run reel). ⚠️ **La traduction se demande APRES l'assemblage** : c'est `build_briefing` qui reclasse les news (les faits d'abord), donc c'est seulement la que les index correspondent a ce que le lecteur verra — traduire la liste de collecte donnerait les bonnes phrases sur les mauvais titres. **Deux defauts que seul l'ECRAN a montres** : (1) la requete Bluesky « nikkei » ramenait « SJ Nikkei Resisters » et un post personnel sur la bulle IA sous le nom de la Bourse de Tokyo — le spec l'avait ecrit (« une requete d'un seul mot commun est presque toujours un piege », « borsa » rendait 50 % de posts turcs) mais le catalogue avait quand meme quatre requetes d'un mot ; toutes font desormais >= 2 mots, un test le verrouille. (2) Au screenshot, le titre d'origine s'affichait **en colonne d'UN MOT de large** : **`.row` du design system est un conteneur `display:grid` a quatre colonnes** (`grid-template-columns: auto 1fr auto auto`), donc tout enfant supplementaire s'y range en colonne. Il faut `display:block` en inline sur la ligne des que le contenu est vertical. **Aucun compteur, aucun test DOM ne l'aurait vu — il fallait regarder le pixel.** **(l) Detecter l'arrivee d'un deploiement : l'URL NON versionnee ne marche qu'UNE FOIS (mesure 2026-07-30).** Pour ne pas empoisonner le cache d'une URL versionnee avec un 404 (le piege connu, commit `5c8102f`), j'avais pris l'habitude de sonder l'URL **sans** `?v=` en boucle jusqu'a y voir le nouveau code. Ca marche au premier deploiement — et ca ment a tous les suivants : le premier appel fait mettre cette URL exacte en cache Cloudflare, et les requetes d'apres rendent la vieille copie. Mesure : `/js/market_module.js` = `cf-cache-status: HIT`, `last-modified` du MATIN, 64 591 octets sans le nouveau code, pendant que `/js/market_module.js?v=4` rendait `last-modified` du soir, 71 976 octets, avec. **Le seul signal fiable est `index.html`** (jamais garde longtemps, et c'est LUI qui porte les numeros de version que le navigateur va charger) ou `sw.js`. Regle : on attend le deploiement en lisant `curl https://omenserver.org/ | grep -oE '\?v=[0-9]+'`, et c'est SEULEMENT une fois que l'index annonce la nouvelle version qu'on a le droit de sonder l'asset versionne.


---

## 🎨 Design System v6 « Ion » (sur base Bento Tech v5)

> **Depuis le 12 juillet 2026, le frontend est en Ion v6** : mêmes NOMS de tokens et mêmes
> composants que v5 (tout ce qui suit reste valable), mais nouvelles VALEURS (bleu-nuit +
> néon) + couche motion. Spec : `docs/superpowers/specs/2026-07-12-omenserver-ion-redesign-design.md`.
> Mockup validé : `docs/superpowers/mockups/2026-07-12-ion-directions.html` (direction B).
>
> **2 règles Ion** : (1) le **néon = information vivante** (online/LIVE/données temps réel),
> jamais décoratif ; (2) le **mouvement = physique** (ressorts, transform/opacity only).
>
> **Tokens Ion** (remplacent les valeurs v5 listées plus bas — le reste de la section v5 fait foi
> pour les composants) : `--bg #050810` · `--bg-elev-1 #0A101E` · `--bg-elev-2 #0E1526` ·
> `--bg-elev-3 #131C33` · `--border #1C2947` · `--border-strong #2C4066` · `--text #EDF2FA` ·
> `--text-muted #8FA3C4` · `--text-dim #5A6C90` · `--accent #00FFB0` (vert électrique) +
> **nouveaux** `--accent-glow` (halo du vivant) et `--top-light` (lumière haute des cartes).
> Fond body = micro-grille pointillée bleue. **Typo : Geist** (Inter retiré) + Geist Mono.
> Les vars legacy (`--bg-primary`…) sont des **alias** des tokens Bento → re-thème atomique.
>
> **5 accents** via `data-accent` : `green` (défaut) / `cyan #00D2FF` / `violet #8B5CFF` /
> `magenta #FF3DA6` / `amber #FFB020` — migration auto localStorage (blue→cyan, red→magenta,
> yellow→amber) dans `App._loadAccent()` (étape « 1bis », APRÈS la migration omen-theme). 5 dots topbar.
>
> **Couche motion** (fin de style.css, bloc « ION v6 » + `anim.js` v2) : cascade d'entrée 70 ms
> (rejouée par navigation, jamais par les polls), count-up, **nav morphing** (pill `.nav-ind`
> créé par `Anim.navInit()`, le pill EST le fond de l'onglet actif via `:has()`), hover ressort
> + glow accent (pas sur `.offline`), sweep radar, **orbite conique** sur `.stat-card.main`
> (Dashboard uniquement, `@property --omen-ang`), glow vivant (`.badge.online`,
> `.status-badge.online` en `!important`), logo shine one-shot, reduced-motion complet.
>
> **⚠️ Pièges Ion appris à la review/vérif** : (a) le nullifieur PR7
> `.bento-overview .stat-card::before { content:none !important }` (L~2185) doit rester en
> `:not(.main)` sinon il tue l'orbite ; (b) le `::before` legacy L~635 pose `height:3px` →
> l'orbite DOIT déclarer `height:auto` (sinon anneau écrasé en lamelle) ; (c) la topbar porte
> `nav-tab` ET `nav-item` → les règles sidebar legacy (`.nav-item.active`) s'y appliquent ;
> (d) le grid inline du Dashboard bat les breakpoints → overrides `[style]` + `!important`
> en media query ; (e) `getComputedStyle` via le pane Claude peut mentir (monde isolé) —
> trancher au screenshot.

### 🌗 Mode clair « Givre » (2026-08-17)

> **Toggle noir ↔ blanc** : bouton ◐ (SVG inline — JAMAIS le caractère ◐, les sweeps emoji
> strippent les formes géométriques, cf. #17) dans la topbar + login. Mécanisme :
> `data-mode="light"` sur `<html>` (absence = dark, défaut pour tous), localStorage
> **`omen-mode`** (`'light'` | absente — on ne stocke jamais 'dark'), script inline
> **anti-flash** dans le `<head>` des 2 pages AVANT les `<link>`, `App._loadMode()/toggleMode()/
> _applyMode()` (miroir de `_loadAccent`/`setAccent`), meta theme-color dynamique
> `#050810 ↔ #EBF0FA`. Spec : `docs/superpowers/specs/2026-08-17-omenserver-light-mode-design.md`.
>
> **Principes Givre** : un bloc `html[data-mode="light"]` redéfinit les tokens (surfaces
> `#EBF0FA/#F7FAFF/#FFF/#E3EAF6` — l'élévation ÉCLAIRCIT, inverse du dark) ; accents =
> **déclinaisons foncées** (`green #00885C · cyan #0077A8 · violet #6A3FE0 · magenta #D01C7C ·
> amber #A96A00`, les néons purs font <1.5:1 sur blanc) ; glows conservés à ~22 % ;
> **sémantiques « fixes PAR MODE »** (`--danger #B91C1C · --warning #92400E (amber-800, le 700
> mesurait 4.3:1 = sous AA) · --info #1D4ED8 · --violet #7C3AED · --orange #C2570A`), les FONDS
> pastel rgba restent ; les **alias legacy chromatiques** (`--accent-green/-blue/-cyan/-red/
> -purple/-yellow`) ont leurs pendants foncés dans le bloc light ; pastilles d'accent = tokens
> pairés `--dot-*` (néon en dark, foncé WYSIWYG en light) ; voiles/ombres = `--surface-hint(-strong)`
> / `--shadow-drop` (blanc alpha en dark, encre bleu-nuit en light) ; les scrims de modale restent
> sombres dans les 2 modes.
>
> **Console = INVARIANTE au mode** (signature Givre) : `.console`, `.events-feed`,
> `.yield-terminal`, `.log-output` etc. sur tokens `--console-*` (bg `#0A101E` bleu-nuit) ;
> dedans, l'accent redevient **néon** via `--console-accent: var(--accent-neon)` et warn/err
> gardent les teintes claires (`--console-warn/-err`).
>
> **⚠️ Pièges Givre** : (a) le **fallback `<style>` inline** d'index/login DOIT rester AVANT les
> `<link>` — placé après, il gagnait la cascade à spécificité égale et écrasait `body` (la
> micro-grille Ion n'avait JAMAIS été rendue à cause de ça, invisible car #050810==#050810) ;
> (b) son **miroir light** dans style.css utilise `html:where([data-mode="light"]) …` = spécificité
> (0,0,2) calibrée : bat les type-selectors du fallback (0,0,1), perd contre toute classe (0,1,0) ;
> (c) `color-scheme: light` dans le bloc light (form controls/scrollbars natifs — la propriété CSS
> bat la meta) ; (d) **vérif via le Browser pane : l'onglet en arrière-plan GÈLE les transitions
> CSS** (`getAnimations()` → `running` à `currentTime: 0` pour toujours) → l'écran ET les computed
> montrent la valeur de DÉPART ; trancher avec un élément sonde sans transition ou
> `el.getAnimations().forEach(a => a.finish())` — extension du piège #46 throttling ;
> (e) `.btn-danger` (override PR7) utilise `color: var(--bg)` comme encre — lisible dans les 2
> modes mais sémantiquement tordu, à savoir avant de « corriger » ; (f) Chart.js lit ses couleurs
> au draw → un graphe déjà peint se rethème à la navigation suivante (même sémantique que
> `setAccent`, assumé).

### Tokens canoniques v5 (HISTORIQUE — valeurs remplacées par Ion, noms inchangés)

```css
--bg          #0E0E10    /* page background */
--bg-elev-1   #161618    /* default surface (cards) */
--bg-elev-2   #18181B    /* hover state, big cards */
--bg-elev-3   #1F1F23    /* inputs, avatars, code blocks */
--border      #27272A    /* hairline */
--border-strong #3F3F46  /* emphasized */
--text        #F4F4F5    /* primary */
--text-muted  #A1A1AA    /* secondary */
--text-dim    #71717A    /* tertiary, timestamps */
--accent      #4ADE80    /* CHANGEABLE via data-accent */
--accent-dim  rgba(74,222,128,.14)
--danger      #F87171    /* FIXED — semantic only */
--warning     #FBBF24
--info        #60A5FA
--violet      #C084FC    /* developer role */
--orange      #FB923C    /* money role */
--font-ui     'Inter'
--font-mono   'Geist Mono'  /* ALL numbers via font-feature-settings:"tnum" */
--r-sm/md/lg  8 / 10 / 14 px
--s-1 .. --s-12  4 .. 48 px (scale 4px base)
```

### Accent variants (changent uniquement `--accent`)

```css
[data-accent="blue"]   { --accent: #60A5FA; ... }
[data-accent="red"]    { --accent: #FB7185; ... }
[data-accent="yellow"] { --accent: #FACC15; ... }
/* default green stays in :root */
```

L'accent est persisté en localStorage (`omen-accent`). Migration auto depuis les anciens
thèmes (midnight→blue, crimson→red, emerald/default→green) au boot via `App._loadAccent()`.

### Composants Bento (à utiliser en priorité)

| Class | Usage |
|---|---|
| `.bento-overview` + `.stat-card` | Grille de stats Dashboard / Network / Tasks |
| `.bento-overview .stat-card.big` | Card 2× plus grosse (span 2 rows) |
| `.bot-card-bento` (+ `.b-head/.b-name/.b-actions/.b-desc`) | Bot cards module |
| `.diag-strip` + `.diag-grid` + `.diag-item` (`.warn`/`.err`) | System health checks |
| `.badge` (+ `.online`/`.warn`/`.danger`) | Status pills universel |
| `.role-pill` (`.admin`/`.developer`/`.moderator`/`.money`/`.player`/`.spectator`) | Rôles RBAC |
| `.access-pill` (`.owner`/`.manage`/`.start`/`.view`) | Per-resource sharing level |
| `.mod-chip` | `User.allowed_modules` indicator |
| `.row` + `.row-list` | Compact list items (servers, bots, etc.) |
| `.events-feed` + `.ev` (+ `.typ.ok/.warn/.err`) | Activity log mono |
| `.sparkline` | Aréa chart subtil sous big stat |
| `.machines-grid` + `.machine-card` (+ `.brain`/`.arm`/`.offline`) | Network of machines (omen + agents) |
| `.users-table` (+ `.u-head/.u-row/.u-name`) | Tableau utilisateurs |
| `.tasks-table` | Scheduler global |
| `.mod-grid` + `.mod-card` | Plugin/Mod/Workshop browser |
| `.dropzone` | Upload zone |
| `.accent-switcher-mini` + `.accent-dot` | Switcher 4 couleurs (topbar right) |
| `.sv-layout` + `.sv-sidebar` + `.sv-tab` (+ `.active/.share/.danger`) | Server view sidebar |
| `.topbar` + `.brand .logo` + `.nav-tabs` + `.nav-tab` | Top bar navigation (PR26) |
| `.lang-switcher` + `.lang` (`.active`) | Pill segmenté FR/EN/IT (PR26) |
| `.user-pill` + `.user-avatar` + `.user-meta` + `.user-caret` | Avatar + nom + rôle (topbar right) — cliquable depuis PR37, ouvre `.user-menu` |
| `.user-menu-wrap` + `.user-menu` + `.user-menu-item` (+ `.danger`) + `.user-menu-sep` | Dropdown sous le profil avec actions account-scope (Utilisateurs admin-only, Paramètres, Logout) — PR37/PR38 |
| `.tab-badge` | Badge count sur nav-tab (PR32 — background jobs) |
| `.b-ticker` | Mono text ticker chip (replace emoji avatars, PR27) |
| `.game-ico` | Idem pour game server cards (`MC`/`ARK`/`CS2`, PR24) |
| `.login-brand` + `.login-logo` + `.login-brand-text` | Login page brand (matches topbar, PR33) |

### Règles d'usage

- **Tous les chiffres** : `font-family: var(--font-mono); font-feature-settings: "tnum"`
- **Couleurs sémantiques** (`--danger`/`--warning`/`--info`) : **FIXES**, ne suivent JAMAIS l'accent
- **`--accent`** uniquement pour : status "online", deltas positifs, primary CTA active,
  nav item actif, role-pill `.admin` (signature owner)
- **Surfaces** : 3 niveaux (`--bg-elev-1/2/3`), JAMAIS d'ombre, hairline border 1px
- **Border-radius** : échelle `--r-sm` (8) / `--r-md` (10) / `--r-lg` (14) / `--r-pill` (999) uniquement
- **Anti-patterns** : pas de gradients (sauf sparkline), pas de glassmorphism, pas d'emoji UI,
  pas de `box-shadow` flou, pas de hex hardcodé dans les composants

### Coexistence avec le legacy

PR 1-6 ont ajouté les nouveaux tokens **sans supprimer** les anciens (`--bg-primary`, `--accent-green`, etc.).
PR 7 a appliqué des **overrides `!important`** sur les classes legacy (`.sidebar`, `.module-card`,
`.btn-primary`, `.status-badge`, etc.) pour les rendre Bento Tech sans toucher au HTML.

**Évolution session 26 mai PM** :
- PR12 : purge complète du système de thèmes legacy (`_themes`, `cycleTheme`, `toggleLightMode`, `_loadTheme`)
- PR17/18 : sweep JS automatique de TOUS les `var(--legacy)` et hex hardcodés (~490 substitutions)
- PR24 : `modules-grid` (6 module-cards emoji) **supprimé du Hub** — la nav passe par la topbar
- PR26 : sidebar verticale **remplacée** par topbar horizontale
- PR27-29-34 : sweep nucléaire emojis (lang.js + tous JS modules) — UI 100% text/iconography mono
- PR31 (backend) : CSP `img-src` étendu pour autoriser `forgecdn.net` + `cdn.modrinth.com` + Steam Workshop + Spigot CDN (modpack thumbnails)
- PR33 : login.html aligné sur topbar Bento (brand `O OMENSERVER` + pill FR/EN/IT)

**Évolution session 27 mai (aftermath sweep nucléaire)** :
- PR35 : 4 boutons devenus vides après PR34 (boutons emoji-only → emoji strippé → contenu vide). Fix : labels texte i18n via `Lang.t()` partout, créer les clés manquantes (`sharing.share_btn`, `sv.files.rename`). Aussi : supprimé `.sharing-search-wrap::before { content: '🔍' }` + migré vars CSS legacy (`--bg-primary`, `--accent-blue`) vers tokens Bento.
- PR35-bis : oubli cache-bust JS individuels — bumper seulement `style.css?v=` + `sw CACHE_NAME` ne propage pas les fixes JS car le disk cache browser sert encore `lang.js?v=N` (URL inchangée). Toujours bumper le `?v=` de chaque JS modifié dans `index.html`.
- PR36 : 2 nouveaux symptômes post-PR35 (Massii spotted) — boutons serveur "Arrêter/Redémarrer/Partager" superposés (`.btn-icon` force 38×38px, texte déborde) + boutons Clé API Agents vides (👁/📋 strippés + code `cond ? '' : ''` smell). Fix : retirer `.btn-icon` quand on met du texte + labels i18n sur toggle/copy + placeholder masqué = 40 bullets `•`.

**Évolution session 28 mai (topbar → dropdown profil)** :
- PR37 : tab "Paramètres" inaccessible (caché derrière `.topbar-right` par overflow-x: auto). Solution > déplacer Paramètres + Déconnexion dans un dropdown sous le `.user-pill` (avatar+nom+rôle+caret ▾). Nouveau composant `.user-menu` : position absolute top:100%+8px right:0, animation `user-menu-in` 140ms (fade+translate), shadow `rgba(0,0,0,0.35)`. JS : `App._toggleUserMenu()` + click-outside (capture phase, armé/désarmé à l'ouverture) + Esc handler + restore focus. `Lang.set()` appelle maintenant `App.updateUserInfo()` pour rafraîchir les labels du dropdown au switch FR/EN/IT (les éléments statiques de la topbar n'étaient pas re-traduits avant). Aussi : `min-width: 0` sur `.nav-tabs` (flex children scrollables nécessitent ça pour shrink correctement).
- PR38 : extension de PR37 — "Utilisateurs" (auparavant tab admin-only `display:none`) rejoint le même dropdown au-dessus de Paramètres. Visibility via `item.hidden = !user.is_admin` (toggle propre, pas `style.display` inline). Ordre menu : **admin > self > danger** (séparateur hairline avant le danger). Pattern à généraliser : *toute action account-scope future* (changer mdp, switch user, sessions actives, etc.) doit atterrir dans ce même dropdown.

**Pour migrer un composant non-encore-refactoré** :
1. Identifie son HTML render dans `frontend/js/*_module.js` ou `app.js`
2. Remplace ses classes legacy par les classes Bento ci-dessus
3. Supprime les inline `style="..."` (les classes font le boulot)
4. Supprime les overrides `!important` correspondants en bas de `style.css`
5. Bump `style.css?v=X` dans `index.html` + `CACHE_NAME` dans `sw.js`

### Référence design

- **Mockups interactifs** : `frontend/design-explorations/proposals.html` (5 directions comparison)
  et `frontend/design-explorations/proposals-v2.html` (Bento Tech 10-page mockup complet)
- **MASTER.md** : design system formel avec tokens, composants, plan migration
  (dans la branche `design/bento-tech-mockup`, pas sur `main`)
- **Backend RBAC** (source de vérité pour rôles, quotas, permissions) :
  [backend/auth/permissions.py:29](backend/auth/permissions.py:29)
- **Server view tabs logic** (conditionnels Plugins/Mods/Workshop par type) :
  [frontend/js/server_view.js:71](frontend/js/server_view.js:71)

---

## 📝 Historique récent

| Date | Changement |
|------|-----------|
| 2026-08-17 | 🌗 **Mode clair « Givre » — toggle noir ↔ blanc sur tout le site** (branche `feat/frontend-light-mode`, brainstorming par mockup 3 directions le 08/08 — Massii a choisi **A « Givre »** —, spec+plan+audit, subagent-driven : 2 Sonnet + 1 Opus, contrôles Fable). `data-mode="light"` sur `<html>` + localStorage `omen-mode` (dark reste le défaut), bouton ◐ SVG topbar+login, anti-flash inline, meta theme-color dynamique. Tokens : surfaces givrées, **accents/sémantiques/alias legacy en déclinaisons foncées** (les néons font <1.5:1 sur blanc ; `--warning` **amber-800** après mesure 4.3:1=sous-AA du 700), pastilles `--dot-*` WYSIWYG, voiles `--surface-hint*`/`--shadow-drop`, **console INVARIANTE bleu-nuit** (`--console-*`, l'accent y redevient néon). **2 bugs préexistants corrigés en chemin** : le fallback `<style>` inline placé APRÈS le `<link>` écrasait `body` → la **micro-grille Ion n'avait jamais été rendue** (fix : fallback déplacé avant les link) ; audit Opus = parité dark **mesurée** (31 sondes Playwright, 34 hardcodés tokenisés, 19 dark-only commentés, contrastes chiffrés). **Piège de vérif découvert** : le Browser pane en arrière-plan **gèle les transitions CSS** (`currentTime: 0` à jamais) → computed ET rendu montrent la valeur de départ ; sonder sans transition ou `getAnimations().forEach(a=>a.finish())`. Détails : sous-section « Mode clair Givre » du Design System. |
| 2026-07-30 | 📈🗓️ **Market Pulse phase D — un briefing par bourse, declenche a son ouverture** (`8819a57` → `62d2351`, deploye + verifie en prod). Massii coche les places qu'il suit ; chacune produit son briefing **15 minutes avant sa cloche**, tout sous le nom de la bourse : etat → indice → comparaison → **agenda** → notizie (les faits d'abord) → titoli seguiti → nuovi titoli → sintesi italienne. **Le premier acte a encore ete de reparer, pas d'ajouter** : le moteur annonce « complet, 292 tests » avait **deux branches mortes** que seule la lecture de sa SORTIE revele — l'agenda n'etait jamais rempli (`pulse/agenda.py` n'existait pas, `main.py` ne passait pas `agenda=`) et les collecteurs sociaux n'etaient jamais appeles (les 4 options de `prefs.json` ne faisaient rien). Agenda : **Fed** (16 reunions 2026-2027) + **BoJ** + **BNS** sondes a la main ; BCE et BoE construisent leurs dates en JavaScript → fichier cure, **jamais une date inventee**. Garde-fou central : une date passee ne peut pas sortir comme prochain rendez-vous. Backend : `GET/POST /prefs` (lecture `money`, ecriture `admin` strict, catalogue servi avec), `GET /briefings`, et **un reveil par GROUPE d'ouverture** (5 pour 10 operateurs — Londres sonne avec Paris, le NYSE avec le Nasdaq) avec **rattrapage par groupe** pour les ouvertures asiatiques qui tombent pendant la veille de la machine. `--borse` limite le run a la place qui ouvre : sans lui, chaque ouverture regenerait les autres et autant d'appels au LLM. Les 7 chiffres du briefing recoupes contre la serie de bougies brute — ⚠️ pas contre `chartPreviousClose`, qui depend de la fenetre demandee et m'a fait accuser le bot a tort. **Zero nouvelle dependance.** Tests : moteur 292→367, backend 939→999. Piege #68. |
| 2026-07-28 | 📈 **Market Pulse — 3ᵉ bot de la suite finance (phases B+C), ouvertures des marchés mondiaux** (`90fef8e` → `e3341e5`). Horloge des places, gaps d'ouverture, rapport matinal **en italien**, Excel, stats 1 an et revue de presse — pour le grand-père, même audience que Yield Bot et Bond Scanner (`admin` + `money`). **Le premier acte a été un bugfix, pas une feature** : le run réel a montré le Nikkei à **-6,11 % au lieu de -3,95 %** (et Shanghai, et Hang Seng) parce que Yahoo livre la séance du jour avec `close: null` et que le parseur jetait la bougie entière — exactement le genre de chiffre faux qui n'a rien à faire sous les yeux d'un particulier. Moteur : watchlist 16→20 (ajout `^TNX`, qui parle directement à un porteur d'obligations, `^VIX`, `^IBEX`, `^SSMI`), taux rendus en **points de base** (dire qu'un rendement 4,641→4,592 « baisse de 1,06 % » est trompeur), nombres au format italien. Backend : router sur le patron du Harvester (disque = vérité, subprocess détaché) + **rattrapage matinal explicite** — `misfire_grace_time`/`coalesce` ne rattrapent rien quand la machine dormait, ce qui est précisément le cas ici toutes les nuits. Presse : 7 flux RSS sondés un par un, **Reddit abandonné (403, le plan en dépendait)**, filtre de fraîcheur qui a démasqué un flux MarketWatch répondant 200 avec des titres vieux de treize mois, et un filtre qui écarte les titres de presse qui sont eux-mêmes des conseils d'achat. **Zéro nouvelle dépendance.** Tests : moteur 26→134, backend 939. Piège #67. |
| 2026-07-27 | 🏠🧭 **MC Agent — 3 homes (safe/work/death) + migration de zone autonome** (`832eda4` → `fd0bd35` → `18b7b8b`, Node 1274→1347 ✓, Python 659 ✓, 2 livraisons séparées + recyclage flotte à chaque fois). **Le « 3 homes » de Massii était un BUGFIX** : le serveur plafonne à 3 (`sethome-multiple: default: 3`) et le code en posait 4 — le 4ᵉ `/sethome` échoue **en silence** et lequel manque dépend de l'ordre → chaque bot avait un filet de sécurité différent troué (preuve `world_mn5` : NethBot2 **sans home `safe`**, sa roue de secours anti-noyade était un no-op). `canchor` absorbé par `safe` (même endroit), `wsite` → `work`, ménage `/delhome` des vieux noms au boot. Dette de mort **persistée** (le self-healing relance à chaque mort, un drapeau mémoire était perdu au seul moment utile), bornée par le despawn 5 min. **Le retour qui manquait** : `deliverMapperArmor` faisait un `/tpa` et ne revenait jamais — vérifié live `work_bookmarked (33,16,61)` → `work_return (33,16,61)`. **Migration de zone** (`zone.js` pur, 37 tests) : verdict eau/épuisée/bois avec hystérésis 15 min + cooldown 20 min, cible **déterministe** (l'escouade migre ensemble), **600 blocs minimum quand la zone est vidée** vs 200 pour un problème local, suppression du vieux `work` à l'arrivée, et `effectiveConfine` qui fait primer la base persistée sur `--confine` (le split-brain qui avait mordu 2×). 3a : chantier noyé **banni** (rayon 16, TTL 30 min) avec décalage sur cap tournant — `descendDiagonal` non touché. Nouveau **`index.smoke.test.js`** : charge réellement index.js (un parse ne résout aucun `require` — c'est ce qui a laissé passer les 2 crashs prod). Pièges #63/#64/#65. |
| 2026-07-26 | 🎮 **Run MC `world_ax5` + 14 correctifs live** (`638bb8f`→`6db2ff3`, Node 1103→1153 ✓). Monde neuf anti-xray **validé à 1,7 %** de minerais cachés-et-secs sous Y64, carte 134 cellules dont 71 boisées. **11 retours live de Massii corrigés en 2 h** : base commune (`basecamp.js`) · squad à chef déterministe (`regroup.squadTarget`) · nourriture de détresse + ramassage du butin · **filet anti-faim** (le réflexe `equip`+`consume` échouait en silence pendant le minage) · abri qui **équipe sa pioche** (il creusait à mains nues) · noyade = nager, pas se murer · minerai ramassé en passant (`oregrab.js`) · stalactites · torches ×2 plus espacées · saut avant de creuser · **`/locate` retiré** (commande d'op qui révèle ce qu'un joueur ne peut pas savoir). **Dont un REVERT de ma propre régression** : couper le pilier du pathfinder a figé les 3 bots (c'est de la traversée, pas le pilier visible). Run clôturé sans progression d'armure — 6 recyclages l'ont mangée. Piège #55, passation `.mc-ax5-report.md`. |
| 2026-07-25 | 🕳️ **Carte MC — les trous étaient de l'échantillonnage, pas de l'affichage** (`001aaaa`, TDD, déployé + mappeurs recyclés). Diagnostic chiffré sur `world_ax4` : **18 % de couverture** (68 cellules / 378), et surtout **9-11 `biome_seen` par mappeur sur toute sa vie** contre 36 `mapper_blocked` / 24 `unstuck:water` → ils patinent, ils ne lisent pas. Mesure décisive : **73-78 % des cellules émises venaient déjà de l'échantillonnage des voisins** (le cache client mineflayer porte bien au-delà de `view-distance=6`) ⇒ le levier est la LECTURE. Fixes : anneaux **1→3** (8→48 cellules/arrivée, arrêt au bord de la zone chargée) · **eau peinte** (le « terre-only » ne concerne que le déplacement ; effet de bord : `isOceanCell` a enfin des données, l'anti-océan de #5 s'exerce pour la 1ʳᵉ fois) · cellule marquée « faite » **après** lecture réussie · `frontierSkip` **TTL 10 min**. Prérequis trouvé en chemin : `world_memory.save` = **8 Mo / ~300 ms** appelé **à chaque event** sous verrou global (chaque `ore_mined` compris) → **debounce 2 s + `flush_world_memory()`** à tout point de lecture disque. Node 1060 ✓ · Python 956 ✓. Piège #52. |
| 2026-07-19 | 🗺️ **Carte MC — anti-chevauchement icônes/étiquettes + vrai sprite chambre des épreuves** (`b4ad26c` main, bots_module v235 + sw v138, vérifié servi). Retour Massii « la carte bug et affiche mal + pas d'image trial chamber » : (1) **piles de sprites illisibles** — structures souterraines (donjons/mines) dessinées 32 px quel que soit le zoom → rendu structures en **2 passes** : icônes greedy (rares d'abord via `_MCA_STRUCT_RANK` ; recouvertes → pastille 3 px, tooltip conservé) PUIS étiquettes (collision-testées entre elles ET contre les icônes — avant, l'étiquette peinte juste après chaque sprite était recouverte par le sprite suivant) ; icônes 1 px/pixel si cellule <20 px. (2) Sprite `trial_chamber` (cf51867) ressemblait à la caisse de la mine + typo espace dans la grille (`..TtccccccccT t.`) → **redessiné** (tuf + angles cuivre oxydé + trial spawner flamboyant). **Méthode de vérif réutilisable** (JWT dashboard expiré, pane Browser timeout sur file:// volumineux) : harness single-file = vrai `bots_module.js` inliné + fixture réelle (SSH Omen, ores tronqués — non lus par le dessin) + stubs Lang/Auth, vue pilotée par hash `#cx,cz,scale`, **readout dans `document.title`** (le monde isolé AppleScript ne voit pas les globals page, piège #42i) et capture par `canvas.toDataURL` → décodage local. Validé fit/1.1/4. |
| 2026-07-15 | ⛵🛡️ **Mappeur MC — terre-only + traversée d'eau réelle + survie Hard (3 chantiers, déployés `main` `9593968`)**. (1) **Fix warp-spam** (`7d2a7f6`) : `/spreadplayers` échoué (océan) rebouclait à l'infini sur la même cellule frontière → garde-fou miroir goto (`frontierSkip` si pas arrivé). (2) **Feature terre-only + bateau** (`32c4c45` puis complétée `9593968`) : `nextLandLeg` (frontière TERRE bornée, jamais l'océan), skip émission biome océan (carte terre-only), warp RETIRÉ du mapping ; `boat.js` (`outwardHeading` fan-out secteur, `landAhead`, `waterEdgeAlong`, `boatStuck`, `ensureBoat` craft, `sailToLand`) ; **cross réel** : repère l'eau au cap (±offsets), marche au bord, pose+embarque le bateau, navigue — **débarquement accepté SEULEMENT après passage au-dessus de l'eau** (sinon « atterrit sur sa propre côte » → boat-spam ×66 vécu) ; sans bateau → nage forward+jump ; garde-fou mapper `movedCross>24` sinon marche. (3) **Survie Hard** (`8431251`) : mappers nus death-loopaient (13 morts/20 min, zombies) → **abri AUTO-SUFFISANT** (`shelterUntilDawn` mine son propre bloc-toit ; `roofPlan`/`shouldShelter` purs, trigger **obscurité/hostiles** — le faux « y<45=safe » tué), buffer terre (`dirt.js`), bouffe (`huntCookGoal(6)`), **armure via `/kit`** : champ profil `kit_command` (UI+i18n+Pydantic+`_clean_server`+`resolve_policy`+**`loadPolicy` bot**) lancé au spawn/respawn (`kit.js` cooldown local 5 min) puis `armorUp`+`eat` — minage fer obligatoire ABANDONNÉ. **Validé live** : death-loop éteint, carte 32→64 cellules, `mapper_boat_cross` s'exerce. ⚠️ **Pièges** : (a) un champ de config profil traverse 4 couches (Pydantic `ServerPayload` + `_clean_server` + `resolve_policy` + `loadPolicy` Node) — chaque couche STRIPPE silencieusement ; (b) events mapper = `/api/mc-agent/events/{sid}` (ring 500), PAS `/chat` ; (c) l'eau est `boundingBox:'empty'` → tester WATER AVANT le check empty ; (d) serveur test : kits Essentials dans `kits.yml` (docker cp + `essentials reload`) — kit `mapper` créé (armure fer+bouffe+2 bateaux+terre+pioche), profil `kit_command="/kit mapper"` ; (e) chaque push = restart uvicorn = bots tués → recycler après CHAQUE push d'une série. **Retours live Massii (même soir, fixés)** : bateau posé en mini-caverne → `waterCrossMode` (bateau=OCÉAN only, rivière=NAGE, autre eau=pas de traversée) ; monté en bateau **mineflayer n'émet plus `physicsTick`** → le connection_watchdog tuait le bot en pleine traversée (fix : nourrir `_lastTick` sur l'event `move`) ; pilier : **jump TENU pendant le placeBlock** (le couper avant = retombe pendant le roundtrip) + apex par critère hauteur (dy≥0.9) ; combat mappeur : **fuite par défaut, riposte UNIQUEMENT à portée de coup** (`preferFlee` dans combatDecision/survivalTick/reflexes, meleeRadius 3, canardé→fuit). Tests **540 Node + 88 pytest**. Specs/plans sous `docs/superpowers/`. |
| 2026-07-14 | 🗺️ **Carte du mappeur MC Agent — restyle « item carte » Minecraft, déployé + vérifié prod** (branche `feat/mc-map-minecraft-style` → main `401f28e`, 100 % frontend + 1 mount statique). Brainstorming par mockup Artifact 3 directions (pattern Ion) → Massii a choisi **A « item carte »** : fond **parchemin** `#d4bb8d` + bord déchiré pixel (`_mcaFrame`, crans hashés), biomes en **map colors officielles MC** (`_MCA_BIOME_RULES` regex conservées → hex ; badland scindé de desert ; fallback palette terre pour biomes datapack) + **grain dithéré déterministe** (`_mcaHash2` numérique, sous-tuiles `max(6, cell/16)` ≤256/cellule, OFF sous cell<12 — stable au redraw, prouvé par hash imageData), spawn = **curseur de map MC** (losange blanc), **13 sprites pixel-art 16×16** dessinés par code (`_MCA_SPRITES`+`_mcaDrawSprite`, ombre dure ; village/donjon-spawner/monument/warden/œil-Ender/mine/portail/pyramides/avant-poste/épave/forteresse/grotte — remplacent pastilles+initiales ET triangles grottes ; kind inconnu → fallback pastille conservé), **nom sur étiquette parchemin** dès cellule ≥45 px (police pixel **Press Start 2P** self-hostée 4,7 Ko `frontend/fonts/`, chargée via `document.fonts.load('8px PSP')`+redraw — `ctx.font` ne lit pas les vars CSS) + **tooltip** structure/grotte <14 px dans le bandeau coords (`Grotte y -12`), légende = mini-icônes canvas + swatches recolorés. **« Trouvailles » RETIRÉES de la carte** (dessin+légende+fit+empty-state ; `_MCA_MAT_COLORS`/`_mcaMatColor` purgés — donnée backend intacte, demande Massii « la mémoire du bot ne sert à rien à l'écran »). 13 clés i18n `mcagent.map.struct.*` ×3 langues. ⚠️ **Piège découvert : `main.py` ne monte QUE `/css` et `/js`** → la police 404 en prod (échec silencieux → monospace) ; fix `app.mount("/fonts", …)`. Vérif : harness scratchpad (vrai bots_module.js + stubs Lang/Auth + fixture format réel avec finds/kind-inconnu) → 11 checks OK, puis prod via Chrome MCP (v232/244/121 + sw v135 servis, pixels canvas = parchemin/cadre, empty-state propre — le groupe test n'a pas de world data). Bumps css v121 · lang v244 · bots_module v232 · sw v135. Spec+plan+mockup sous `docs/superpowers/`. |
| 2026-07-13 | 🎨 **Ion — restes spec soldés** (`ab6c3d4`) : (1) **count-up générique** `Anim.countUpIn(root)` — anime le 1er nœud texte purement numérique des `.stat-card .value` (IP/labels/`--` ignorés, idempotent `data-cu`), branché module Réseau (statut + speedtest) ; (2) **morphing sidebar vue serveur** `Anim.svNav` — `switchTab` re-render TOUT le DOM → astuce 2 frames (mesurer l'onglet actif AVANT re-render, replacer le pill `.sv-ind` sans transition, puis animer vers le nouveau ; la barre accent gauche voyage avec) ; ⚠️ garde mobile : `flexDirection` calcule `'row'` même sur un bloc → tester `display==='flex'` AUSSI ; (3) **Historique serveur en `.events-feed`** (composant Bento, `ts/typ/msg`, typ ok/warn/err par mots-clés d'action, cascade `.ev-in` staggerée, timestamp court `13/07 01:13`). Décision : PAS de panneau Activité sur le hub (store en MÉMOIRE par serveur, vidé à chaque restart → panneau vide en permanence ; il faudrait un store persistant global d'abord). Bumps css v114 · anim v4 · server_view v102 · network v94 · sv_history v90 · sw v127. Vérifié local (serveur factice + activités seedées par API) + prod (pill ambre sous Historique, empty-state propre). ⚠️ SW : après deploy, l'onglet sert l'ANCIEN index jusqu'à 2 reloads (activation du nouveau SW). |
| 2026-07-13 | 🎨 **Ion — pack finitions déployé** (`7472b92`) : **sparkline CPU réelle** sur la carte maîtresse (`Monitoring._cpuHistory` 40 pts @poll 2 s → `Anim.sparkline(svg, values)` ; SVG dans `renderHub`, draw-in CSS `.view-enter`) + **valeur néon + badge LIVE** (⚠️ le fallback inline de index.html colore TOUS les `<span>` → règle dédiée `.stat-card.main .value span`) + **favicon/icon-512/manifest recolorés Ion** (`?v=2`, theme/bg `#050810`). Bumps css v113 · anim v3 · app v221 · monitoring v96 · sw v126. Vérifié prod (session relogin — JWT 24 h expiré entre-temps). Restes connus : count-up hors Dashboard, morphing sidebar sv, `.ev-in` inutilisé. |
| 2026-07-12 | 🎨⚡ **Refonte visuelle v6 « Ion » — déployée + vérifiée en prod** (branche `feat/frontend-ion` → `main` `fdd0387`, 14 commits, subagent-driven, brainstorming via mockup Artifact interactif à 3 directions : Massii a choisi **Ion** = premium fluide + néon dosé, vert électrique par défaut, style unique). **Re-thème atomique** : mêmes noms de tokens, nouvelles valeurs (bleu-nuit `#050810`, accent `#00FFB0`, Geist remplace Inter, micro-grille body) + vars legacy aliasées sur les tokens Bento → tout le site suit, y compris les overrides PR7. **5 accents néon** (`green/cyan/violet/magenta/amber` + migration auto localStorage des 4 anciens). **Couche motion** : cascade 70 ms, nav morphing (`Anim.navInit`, pill `:has()`-fond), hover ressort + glow accent, sweep radar, **orbite conique** `@property` sur la carte CPU du Dashboard (`.stat-card.main`, 1.6fr), glow vivant, logo shine, reduced-motion complet ; + responsive réparé (stats 2 col mobile, topbar wrap 2 rangées ≤768px — débordement 375px préexistant éliminé). **Review adversariale finale** = 10 findings dont 1 critique (l'orbite écrasée en lamelle 3px par le `height:3px` du `::before` legacy — fixé `height:auto`) et 3 importants (glow status-badge tué par un reset `!important` ; pill masqué par le bg opaque des onglets ; hover ARM perdu en spécificité) — tous corrigés avant push. **Vérifié en prod** (session Chrome Massii) : v112/sw v125 servis, rethème live des 5 accents (orbite/pill/cartes suivent), console 0 erreur. Spec + plan + mockup committés sous `docs/superpowers/`. Pièges Ion documentés dans la section Design System v6. |
| 2026-06-21 | 🌾🔒 **AI Harvester — CAPTCHA interactif human-in-the-loop (tier stealth), déployé + vérifié live** (branche `feat/harvester-manual-captcha` → `main` `27251ca`, subagent-driven TDD, **790 tests verts**). Gère les rares CAPTCHA interactifs **sans solveur auto, sans payer** — c'est Massii qui résout, le bot attend. **4 features dans le tier stealth** : (A) **auto-click générique** de la case Turnstile (`click_turnstile` = bbox center click, ne résout rien) ; (B) **résolution manuelle** : si un puzzle persiste, au lieu de `PushbackError` → event `awaiting_manual_solve` + pause + poll (titre-only) jusqu'à résolution/timeout(1800s)/stop, sinon repli unblocker ; opt-in (`plan.manual_solve`, OFF par défaut) ; events miroir de `recommend_tier` → `_solve_from_log` (tail) → `/status` `awaiting_solve` → bandeau `#hrv-solve` ; (C) **config Telegram persistante** (modèle `unblocker_config` : `data/harvester_telegram.json` 0o600 atomique, token masqué, endpoints admin) + notifier httpx best-effort (zéro fuite token, zéro nouvelle dép) ; (D) **vue live noVNC** : bridge WS FastAPI admin-gated `/api/bots/harvester/vnc/{job_id}` = pont **WS↔socket Unix** vers **x11vnc** (`-display :100`), noVNC vendored `frontend/js/vendor/novnc/`, canvas dans le dashboard ; refuse hors (admin + job `awaiting_solve`). Le subprocess détaché possède la page Chrome ; uvicorn est un autre process → d'où le bridge. **Revue adversariale (5 dims)** → 4 findings corrigés avant deploy : re-goto en boucle (→ résolution titre-only, ne se bat plus avec les clics noVNC + plus de stall sur le beacon CF), export client embarquait `manual_solve` (→ strippé), x11vnc gardait un listener TCP (→ `-localhost`), test WS non-discriminant (→ accept-puis-1011). **Infra one-shot** : `x11vnc` + unit systemd `omen-harvester-vnc.service` (socket Unix `/run/omen-harvester-vnc/vnc.sock`, **`-localhost` = TCP loopback only**, user massii08). **Vérifié live** : flux `awaiting → resolved → done` reproduit ×2 sur le site démo `challenge-endpoint.lusostreams.com`, x11vnc sert `RFB 003.008` sur le socket, screenshot Chrome `:100` OK. ⚠️ Le challenge démo s'auto-résout (auto-click + signaux) → la résolution humaine ne se déclenche que sur un vrai puzzle persistant. Piège #46. |
| 2026-06-21 | 🔒🔒 **Audit de sécurité complet + remédiation déployée (session autonome nocturne)** (commits `e3eef1e`→`376626d`→`d72769e` sur `main`, TDD, **748 tests verts**). Audit 6-dimensions (6 sous-agents : auth/injection/path/SSRF/XSS/config, exploitabilité tracée + recoupée). **Thème racine** : énormément d'endpoints ne vérifiaient que « est connecté » sans **rôle ni ownership** → corrigé par un garde factorisé `auth/access_control.require_resource_access(user, "server", id, db, min_level)` (admin court-circuite). **Corrigé & déployé** : (1) **IDOR critique** — `game_server/{files,settings,players,access,worlds,database}` + `mods/{install,remove}` gatés (la fuite du mot de passe SFTP `get_sftp_info` fermée) ; (2) **zip-slip** restauration backup (`_safe_extract` filtre les membres + `backup_id` validé/confiné, Python 3.9 n'a pas `filter='data'`) ; (3) **injection shell-in-container** `reset_world` (`world_name` regex `^world[\w-]*\Z`) ; (4) **SSRF** — nouveau `backend/net_guard.py` (`is_public_url`/`assert_public_url` bloquent loopback/privé/link-local ; `host_allowed` = allowlist CDN) appliqué aux downloads mods (+ `follow_redirects=False`), au crawl harvester (same-host+public, guards injectables) et au webhook Discord ; (5) **écriture fichier host arbitraire** confinée (gdrive `dest_path` racine fixe+admin, upload xlsx `basename`) ; (6) **secrets** — invitations (token 128 bits + `max_uses` appliqué + expiration 7j par défaut), `.env`+`nodes_api_key.txt` en 0o600, clé agent en `compare_digest`, rate-limit : `X-Forwarded-For` client non fiable retiré (CF-Connecting-IP sinon `client.host`) ; (7) **XSS** — helper global `esc()` (`window.esc` dans lang.js) + échappement systématique de TOUTES les données en innerHTML (18 fichiers JS), `toast.js` en `textContent`, `onclick`-avec-données → pattern `data-*`+`dataset`, `href/src` validés http(s) ; cache-bust 18× + sw v121. **Console RCON** relevée `view_only`→`manage`. **Verts au déploiement** (versions servies + SW confirmés via Chrome MCP ; rendu app profond non vérifié = pas de session admin sous la main). **⚠️ Suivi (NON corrigé, noté)** : (a) token JWT en query string des WebSocket (le frontend en dépend → migrer sur l'auth-par-1er-message avant de retirer `?token=`) ; (b) `/api/sysdoc/me` renvoie le `SECRET_KEY` JWT global (l'agent sysdoc S'EN SERT pour signer son JWT → nécessite un secret d'agent dédié, refonte) ; (c) bots Python = RCE hôte **par design** (rôle developer ≈ shell) — à sandboxer ; (d) `custom_image` Docker arbitraire (moderator/dev) ; (e) RCON password hardcodé (port non mappé, FAIBLE) ; (f) durcir l'infra : branch-protection GitHub + 2FA (auto-deploy `git pull`/min sur repo PUBLIC = RCE si push sur `main`), HSTS, uvicorn sous user non-root. Fix annexe : **flaky préexistant** mc_agent (race pump-thread cleanup) éliminé proprement (conftest, marqueur `real_pump_cleanup`). **Pattern transverse** : *un endpoint qui agit sur une ressource DOIT appeler `require_resource_access` ; une requête sortante vers une URL d'origine utilisateur DOIT passer `net_guard` ; toute donnée en innerHTML DOIT passer `esc()`.* |
| 2026-06-20 | 🔒 **AI Harvester — durcissement defense-in-depth : `config.json` + `run.log` naissent en 0o600 (création atomique)** (commit `380b749`, TDD). Repéré pendant la revue adversariale du tier débloqueur : le pattern `open()` puis `os.chmod()` laisse une **fenêtre world-readable** (0o644 sous umask 022) et **reste 0o644 si le chmod échoue**. `HarvestConfig.save()` (`harvester/config.py`, porte `feed_key` + d'éventuels creds proxy) et `_open_run_log()` (`harvester_router.py`, traces) alignés sur `unblocker_config.save()` : `os.open(path, O_WRONLY|O_CREAT|O_TRUNC` *(config, overwrite)* `/ O_APPEND` *(run.log)*`, 0o600)` + `os.fchmod` best-effort (`except (AttributeError, OSError)`). Sémantique préservée (config = overwrite, run.log = append vérifié live). **512 tests** (+3 : baseline config + `…is_600_even_if_chmod_unavailable` ×2, modèle = `test_save_is_600_even_if_chmod_unavailable`). Zéro dép, backend-only (pas de cache-bust). **Pattern « secret écrit côté serveur » = TOUJOURS création atomique 0o600 (`os.open` mode + `os.fchmod`), jamais `open()`+`chmod()`.** |
| 2026-06-20 | 🌾 **AI Harvester — champ UI pour la clé du débloqueur (config persistante, plus de `.env`)** (commit `0f8bcae`). On pose **endpoint + clé d'API** du tier unblocker directement dans le bot (panneau dépliable « Réglages débloqueur » du formulaire) → persisté côté serveur, plus besoin de toucher au `.env` ni au code. `harvester/unblocker_config.py` (`load/save/clear/public_view`) : clé dans `data/harvester_unblocker.json` (**gitignoré**), fichier **créé atomiquement en 0o600** (`os.open`+`os.fchmod`, pas de fenêtre world-readable) ; `public_view` **masque** la clé (`····last4`) → la clé brute ne sort JAMAIS de l'API. Endpoints **admin-only** : `GET /unblocker-config` (masqué), `POST` (clé vide ⇒ garde l'existante ; endpoint validé http(s)), `POST /clear`. `_build_fetcher` fusionne **plan > config persistante > env**. UI : panneau (endpoint, clé `password`, méthode, placement clé, param, render_js) + statut configuré/masqué + Enregistrer/Oublier (champ clé jamais pré-rempli) ; i18n FR/EN/IT ; cache-bust lang v235/module v8/sw v120. **509 tests, zéro dép.** Revue adversariale ciblée (2 confirmés : race chmod corrigée ; endpoint-scheme côté plan = nit accepté car admin-only + httpx rejette les non-http). Vérifié live (save clé factice → masquée, fichier prod `600`+gitignoré, GET sans fuite, clear OK). **Pattern réutilisable** « secret posé depuis l'UI » : fichier gitignoré + création atomique 0o600 + vue API masquée + clé vide = conserver. Activer : ouvrir le bot → Réglages débloqueur → coller endpoint+clé. |
| 2026-06-20 | 🌾 **AI Harvester — P3c (tier "débloqueur") + reco déterministe livrés en prod** (branche `feat/ai-harvester`, commit `9fb430f`, TDD). 3e tier de fetch = **API managée de débloquage** (type ZenRows/ScrapingBee/Bright Data Web Unlocker) pour le Cloudflare AGRESSIF. **(1)** `harvester/fetch_unblocker.py` `UnblockerFetcher` — contrat MIROIR de `StealthFetcher` (`.get(url)->str`, lève `PushbackError`/`FetchError`), client **httpx injectable** (test offline), **provider-agnostique** : endpoint/clé/`render_js`/params lus de l'env (`HARVESTER_UNBLOCKER_ENDPOINT`/`HARVESTER_UNBLOCKER_KEY`) **surchargés par le plan** ; placement clé `body`/`header`/`query` ; enveloppe JSON (`result_field` + inspection statut/erreur cible ≥400 → Pushback/FetchError, pas de faux succès). Branché dans `_build_fetcher` (`tier=='unblocker'`, options tolérantes `_as_int/_as_float`). **Non configuré → FetchError clair** (lazy, comme patchright). **ZÉRO nouvelle dép** (httpx). **(2)** UI : checkbox `#hrv-unblocker` (**exclusive** avec stealth, UI + payload), badge tier, bandeau de reco `--warning`, i18n FR/EN/IT, cache-bust (lang v234/module v7/sw v119). **(3) ⭐ Reco déterministe** : `engine.py` compte les `PushbackError` **consécutifs** (`_consec_pushbacks`, reset sur succès ET sur `FetchError`), émet **une fois** `recommend_tier` au seuil **5** (sauf si déjà unblocker) via `on_event`→`_emit`→run.log ; router capte l'event (thread `_capture`) + `_recommend_from_log` (lecture **tail 64KB bornée** + cache positif sur le job) → surfacé par `/status` + `/active` → bandeau UI. **Runtime 100% déterministe (aucun LLM dans la boucle).** 🔒 La clé n'apparaît JAMAIS dans /status/active/dumps NI dans un message d'erreur (`get()` réduit TOUTE exception à `type(e).__name__`, pas `repr` qui fuiterait l'URL+clé si key_in=query) ; `run.log` chmod 600. **492 tests backend** (+38 P3c, tous offline DI). **Revue adversariale multi-agent** (20 agents, 4 dim → vérif par finding) : 11 confirmés corrigés avant deploy. **Vérifié live (Chrome MCP)** : checkbox+exclusion, run unblocker routé (badge "débloqueur", sans clé→erreur propre, pas de crash), bandeau de reco affiché. ⚠️ Ce qui est construit = la **plomberie pluggable** du tier (fetcher vers API managée) + la reco, PAS un solver Turnstile (toujours hors-périmètre). Reste : **P4 export client**. Pour activer : poser `HARVESTER_UNBLOCKER_ENDPOINT`+`HARVESTER_UNBLOCKER_KEY` dans le `.env` de l'Omen. |
| 2026-06-18 | 🌾 **AI Harvester — P2 (setup IA) + P3a (pacing adaptatif) livrés en prod** (suite du P1). **P2** : `harvester/llm.py` (`_claude` porté du Sniper, `subprocess.run` injectable) + `harvester/setup.py` (probe difficulté facile/moyen/dur → prompt → Claude génère `{recipe,plan,pacing}` → run échantillon → preview ; `fetch_full`+`claude` injectés) + `POST /setup` (admin, gate http(s), **gate PII sur la recette générée**, 502 si l'IA échoue) + flux UI « Générer (IA) » (instructions NL → remplit les textareas recette/plan **éditables** + tableau d'aperçu). Le runtime reste 100% déterministe (`/setup` ne produit QUE la config que `/run` consomme ; seule étape LLM). Vérifié live sur la prod (Claude a généré une recette correcte avec sélecteur en chaîne descendante pour le titre). **P3a** : `harvester/pacing.py` `AdaptivePacer` (penalize ×backoff capé / relax vers la base) + `fetch.py` `PushbackError(FetchError)` (levé sur `429`/`503+Retry-After`/challenge, surfacé sans marteler) + `engine.py` pacer optionnel (pushback → penalize + réessaie la MÊME URL borné ; succès → relax + espace par `interval()`) + `__main__` wire (RateLimiter 0). **Rétro-compatible** (sans pacer = comportement P1). **348 tests backend, zéro nouvelle dép.** ⚠️ **P3b/P3c (tier stealth patchright + tier unblocker) = NON construits** : contournement anti-bot = hors-périmètre agent (même ligne que Turnstile/Akamai) ; fetcher `get(url)->str` laissé pluggable pour que Massii les branche lui-même. Reste : **P4 export client** (zip standalone déterministe). Plans `docs/superpowers/plans/2026-06-18-ai-harvester-p2.md` + `…-p3a-pacing.md`. |
| 2026-06-18 | 🌾 **AI Harvester — nouveau bot (module Bots), P1 livré en prod** (branche `feat/ai-harvester`, subagent-driven TDD via superpowers). On donne une URL + une recette → moisson **continue low-and-slow** d'une cible propre (httpx) → records accumulés via **gate no-PII** → exposés par une **API privée** (`/data` gated `X-Feed-Key`). **Runtime 100% déterministe, zéro LLM**. Package moteur `backend/bots/harvester/` : `policy`(no-PII, porté de Feedsmith) · `dom`(mini-DOM **stdlib `html.parser`** — PAS de bs4/lxml → **zéro nouvelle dép** ; httpx déjà présent) · `recipe`(extracteur déterministe, **sélecteurs en chaîne descendante** `[{tag:h3},{tag:a}]`) · `crawl`(sitemap + pagination) · `store`(frontier resumable JSON) · `fetch`(httpx + RateLimiter, DI) · `engine`(boucle pacée, fetch/clock/sleep injectés) · `config`+`__main__`(subprocess détaché `python -m backend.bots.harvester <run_dir>`). Router `harvester_router.py` = mirroir Bond Scanner (`Popen start_new_session=True`, job mémoire + thread logs, `/run /status /active /stop /data`). Front : carte admin-only + `harvester_module.js` + i18n FR/EN/IT. **318 tests backend (tous offline, DI)** + revue adversariale GO. **Smoke live sur omenserver.org** : books.toscrape → 440 records, titres+prix+dispo+rating extraits, clé de feed OK. **Découpage** : P2 = setup intelligent (`_claude` génère recette/plan/pacing + `/setup` + preview) ; P3 = pacing adaptatif + tier stealth (patchright) + tier unblocker (clé) ; P4 = export client (zip standalone). Spec `docs/superpowers/specs/2026-06-18-ai-harvester-design.md`, plan P1 `docs/superpowers/plans/2026-06-18-ai-harvester-p1.md`. ⚠️ Leçon : vérifier la recette démo contre la VRAIE page (le sélecteur simple `{tag:a}` tombait sur l'ancre image avant `<h3><a title>` → titre vide ; corrigé par la chaîne descendante). |
| 2026-06-08 | 🔧 **MC Agent — fix pilier + stuck flottant + mod capture enrichi (events combat/minage)** : (1) **pilier** (`21744a7`) : `Movements.scafoldingBlocks` élargi (défaut = dirt+cobblestone SEULEMENT → un mineur deepslate n'avait aucun bloc utilisable pour tourer/remonter) ; (2) **stuck flottant** : `recoverFloating`/`isFloatingStuck` (CODE MORT, jamais branché) câblés en watchdog 2 s + garde vélocité (pas de faux positif en chute) ; (3) **mod OmenCapture** : events `mob_appear`/`damage`/`attack`/`block_break` détectés **client-side** (chute PV / scan hostiles / `AttackEntityCallback` / bloc visé→air), types alignés sur `mc_capture_distill.py` → **calibration des temps de réaction ENFIN possible** (le trou du piège #44). Jar **1.21.11** rebuildé (13K, 0 réflexion, v0.1.0). Tests **Node 558 + JUnit 10**. ⚠️ Jars ≤1.21.5 figés (source 1.21.6+). Piège #45. |
| 2026-06-08 | 🎭 **MC Agent — humaniser le bot (paquets 1-2, déployés)** : le bot « trop facile à deviner » tournait **humanisation OFF par défaut** (vitesse machine) + ne touchait que le chat. Diagnostic multi-agent (6 lecteurs) + analyse des **vraies captures** (3 joueurs, 478M, 1,5M ticks @20Hz, dans `~/mc-captures-work/`). **P1 anti-tell** (`5c6b70b`) : `humanize.js` `nextLook` (visée marche-aléatoire bornée calée captures p50≈0°/p90≈4.5°) / `sampleReactionDelay` / `mungeChat` (registre minuscules+fautes clavier) ; `reflexes.js` délai de réaction injectable ; `brain.js` posture **ÉVASIVE** (nie être un bot) ; `index.js` boucle bruit-de-visée idle + `allowSprinting` + `stopMotion`+stopDigging ; `mc_agent_manager.start_session humanize=True`. **P2 survie** (`2f35e06`) : `survival.combatDecision` **armor-aware** (sans armure → fuit dès PV≤12/2 hostiles, anti mort-par-combo) + `fleeFrom` hostiles-only (plus les vaches). Mod capture v0.1.0 ne loggue que `tick`/`chat` → réaction non calibrable (défaut ~300ms). Décisions Massii : nie/passe-pour-humain · **gratuit** (0-LLM max) · gains visibles d'abord. Tests **Node 557 + Python 257**. Stoppé la session test de 6 bots d'hier. Worktree `worktree-feat+mc-agent-humanize`. Piège #44. |
| 2026-06-06 | ⛏️ **MC Agent — pipeline cartographes → bots ressources multi-quota** (nuit autonome) : N mappers scannent TOUS les ores des chunks chargés (`scanAllOres` 256 blocs, 5 types quota, flag `exposed`, event batché `ores_found`, cap 800/type) → M bots `resource` en PARALLÈLE avec quotas par bot (15💎/15🟡/64🔴/64🔵/64⚙, `quota.js` banked+inventaire), claims anti-collision (`claims.js` lockfile TTL 120s + refresh post-goto), mémoire LIVE (`--wm-live` re-read), collect borné 90s, nearest-first en mode quota, tossJunk (pas de deposit sous terre). Viz : ores sur la carte groupe (carrés par type, plein=exposé) + barres de quota par worker (`quota_progress` → session → /active). 3 fixes nés du run live parallèle : cap par type (fer évinçait les diamants), freeze collectBlock, claim expirée en tunnel. Fix annexe prod : symlink node_modules cassé purgé + npm install. Tests **Node 452 + Python 241**. Branche `feat/mc-agent-oremap-quota` → main. Piège #42. |
| 2026-06-02 | 🔩 **MC Agent — progression FER (objectif `iron_pickaxe`, zéro→pioche fer)** : nouvelle chaîne `IRON_CHAIN` (12 buts) sélectionnée par `chainFor(objective)` ; étend la chaîne pierre → **four** (8 cobble) → **minerai de fer** (`gather` iron_ore/deepslate, pioche pierre+) → **lingots** (nouveau skill **`smelt`** : `openFurnace`+putInput/putFuel **bois**+poll output, four PORTABLE place/reprend comme la table) → **pioche fer**. Cobble **scindé** pick(3)/four(8) pour rester monotone. Dashboard : **dropdown objectif** (pioche pierre | pioche fer) sous la case autonome → `objective` à `/run` → `start_session` seed le world.json (whitelist anti-injection). **Combustible = bois** (pas de coal-finding). Validé live : **pioche fer obtenue depuis zéro**, four+fonte, 0 LLM, chaîne sans stall. Aucune nouvelle dep. Tests **Node 176 + Python 108**. ⚠️ Suivi : la **recherche profonde de minerai** (branch-mine pour trouver le fer loin en surface) reste un chantier (spec diamant §7 `branchMine`) — validé ici avec minerai à portée. Branche `feat/mc-agent-iron`. |
| 2026-06-02 | 🎛️ **MC Agent — lancement autonome depuis le dashboard** : case « Lancer en autonome » (panneau Lancer) → `autonomous:true` à `POST /run` → `start_session(autonomous=True)` seed un `world.json` temp (objectif stone_pickaxe) + passe `--world` → le bot lance la boucle planner au spawn (0 LLM), nettoyé au stop comme cmds/policy. **AuthMe = géré côté Node** (self-persist `data/mc_agent_secret_<user>.json` chmod 600) — **PAS dans `mc_agent_servers.json`** (exposé par l'API `/servers` → fuite). i18n FR/EN/IT + cache-bust (v212/sw v91). Tests **Python 65** (manager `--world`+cleanup, router `autonomous`). Mergé `main` + déployé. Branche `feat/mc-agent-dashboard-start`. |
| 2026-06-02 | 🤖 **MC Agent — planner autonome MVP validé live (zéro→pioche pierre, 0 LLM)** : le bot va seul de l'inventaire VIDE à une pioche pierre (gather bois toutes essences → planches → table → bâtons → pioche bois → cobble → pioche pierre → `autonomous_done`), **sans qu'on lui fournisse de matières**, **0 appel LLM**. 4 fix robustesse skills×terrain : **table de craft PORTABLE** (posée/reprise à chaque craft 3×3, surface OU sous-sol — fin du stranding après le creusage cobble) ; `gatherLog` toutes essences (`gather` accepte un tableau de noms) ; `placeBlockNear` 3 passes (case vide / creuse une paroi / **remblai sur piédestal**) ; **timeout 90s/skill** (anti-freeze pathfinder/collectblock). `mineDown` écarté pour le cobble (bot off-center ne tombe pas → `void_below`). Validé 4 runs (dont y58 sous-sol) ; **leçon : tester en boucle détruit le terrain de spawn → valider sur arène propre/serveur frais**. Aucune nouvelle dep. Tests **Node 164** verts, subagent-driven TDD. Worktree `worktree-feat+mc-agent-planner-mvp`. Piège #41. |
| 2026-06-02 | 🎮 **MC Agent — couche de commandes directes (zéro-LLM)** : 16 commandes anglaises en `/msg` privé (`take`/`follow me`/`stop`/`afk`/`craft`/`give`/`give all`/`pvp`/`tpa`/`mine down`/`come`/`deposit`/`guard`/`equip`/`eat`/`goto`) parsées (`orders.js`) **AVANT** tout `think()` → exécution déterministe, **0 token** (anti-grillage clé). LLM seulement si (/msg non-commande) ou (bot **nommé**), + **mémoire conversation par joueur** (`memory.js`, fenêtre+TTL). `take` = meilleur outil auto par bloc + auto-défense → `done` ; `stop` = loiter vivant (anti-tell) ; `afk` = inerte (réflexes survie ON). **Langue** FR/EN/IT au profil + **port optionnel**. Retours en whisper privé localisé. Modules purs `orders`/`tools`/`tasks`/`memory` + 8 skills, `taskCtl` annule la tâche active. Aucune nouvelle dep → auto-deploy propre. Tests **Node 132 + Python 61** verts, subagent-driven TDD. Piège #40. |
| 2026-06-01 | 🤝 **MC Agent — gens de confiance** : liste par profil serveur qui gate les ORDRES (`action`/`command` du LLM retirés par `gateDecision` si l'émetteur n'est pas listé ; questions répondues à tous) + **auto-accept /tpa** des gens de confiance (`bot.on('messagestr')` + `parseTpRequest` Essentials EN/FR, gated par `/tpaccept` coché) + **trade** opt-in (pattern+commande configurables). Liste vide = gating off. Garde-fou double (prompt `buildTrustDocs` + `gateDecision` dur). Module pur `mc-agent/trust.js`. Policy `{trusted,trade}` résolue backend (`resolve_policy`) → `--policy`. Note UI « clé LLM commune à tous les bots ». Tests **Node 89 + Python 96** verts. Worktree `feat/mc-agent-trusted-players`, subagent-driven TDD. Piège #39. |
| 2026-06-01 | ⚙️ **MC Agent — onglet Serveurs (profils + commandes)** : profils serveur réutilisables (IP/port/compte/auth + intelligence = profils existants Évident/Intermédiaire/Expert + checklist de commandes dispo) persistés `data/mc_agent_servers.json`. Catalogue prédéfini `mc-agent/commands-catalog.json` (19 cmds Essentials/SMP) + commandes custom. Le bot reçoit la whitelist via `--commands` → injectée dans le system prompt (`brain.js`, champ décision `command`) + garde-fou pur `mc-agent/commands.js` (`isAllowed` bloque toute commande non cochée, double sécurité). Commande = **texte chat** (`bot.chat`), AUCUNE nouvelle skill mineflayer. UI 2-onglets ▶ Lancer / ⚙ Serveurs. Endpoints catalogue + CRUD `/servers` + `/run` via `server_id`, **admin-only**. Tests **Python 89 + Node 77** verts. Worktree `feat/mc-agent-server-commands`, subagent-driven TDD. Piège #38. |
| 2026-06-01 | 👥 **MC Agent — accès REC-testeur + mod multi-version** : nouveau rôle RBAC `rectester` (permission `mc_capture`, **capture-only**, admin-assigné, gate backend `_require_capture`). Download mod depuis le dashboard (`GET /api/mc-agent/mod/{v}`, jars **1.21.4 + 1.20.1** committés `mc-capture-mod/dist/`, whitelist anti-traversal) + tuto install (Fabric Loader + API + F8 ; `INSTALL.md` + panneau dépliable UI 3 langues). Dépôt rec avec **ownership par compte** (sidecar `.owner` ≠ pseudo MC → chacun voit/supprime les siennes, admin voit tout). distill/bot/clé/profils restent **admin strict**. Carte MC Agent réduite pour rectester. Mod multi-version 1 codebase (`options.release`+`-Pjava_version`). Tests backend **74/74**. Pièges #35/#37. |
| 2026-05-30 | 🎥 **MC Agent Phase 1b.1 — pipeline de capture** (branche `feat/mc-agent-phase1b-impl`, worktree isolé, subagent-driven TDD). Mod client **Fabric** OmenCapture (REC/REC-off **F8**, HUD permanent, consentement OFF par défaut + erreur→off, chat in+out, cible 1.21.4) → `.jsonl` local → **upload manuel** dashboard. Backend **stdlib** : `mc_capture_store` (rangé par joueur, attribution par header, anti path-traversal) · `mc_capture_distill` (→ `style.json` stats + `clips/` motricité, spec §7) · `mc_capture_router` (upload/list/distill/style/delete **admin-only**) monté dans `main.py`. Panneau « Captures » UI + i18n FR/EN/IT + cache-bust. Tests **Python 53/53** + **JUnit pur 8/8**. Mod = build-time only (l'Omen ne gagne aucune dép runtime ; JDK/Gradle tarball user-space). Pièges #35/#36. Jalons suivants : 1b.2 calibration, 1b.3 clone hybride, 1b.4 dream team. |
| 2026-05-29 PM | 🎮 **MC Agent Phase 1 — profils calibrés** (branche `feat/mc-agent-phase1`, subagent-driven TDD). 3 profils Évident/Intermédiaire/Expert avec **fiches de tells** (invariant §2 : `tells` non-vide validé à la construction, sinon rejeté — `profiles/index.js`). Réalisme **paramétré** `humanize.js` (latence tirée d'une distribution + fautes occasionnelles, §7.1 — PAS de clonage humain). Skills `mineBlock`/`collectWood` (collectblock), `attackNearest` (pvp), `fleeFrom` (GoalInvert) + réflexes zéro-LLM `reflexes.js` (manger + fuir/défendre). Endpoint `GET /api/mc-agent/profiles` (source unique = `mc-agent/bin/list-profiles.js`) + UI sélecteur de profil & panneau **corrigé formateur** (anti-XSS). ⚠️ 2 deps Node ajoutées (`mineflayer-pvp`, `mineflayer-collectblock`) → `cd mc-agent && npm install` sur l'Omen au déploiement (auto-deploy ne réinstalle pas, cf. #33). Node 49/49 + Python 27/27 ✓. Behavioral cloning isolé en **plan 1b** (cadrage consentement). |
| 2026-05-29 PM | 🛑 **Fix serveur de jeu « se rallume tout seul »** — la boucle auto-restart du `@app.on_event("startup")` de `main.py` rallumait TOUT conteneur arrêté sans lire `srv.status` → les serveurs éteints **volontairement** revenaient à chaque restart uvicorn (et chaque `git push` = auto-deploy/min = un restart) → impression de relance continue. Fix : `container.start()` seulement si état désiré `srv.status == "running"`, sinon laissé éteint. `GameServer.status` est conflaté (désiré+observé) ; `unless-stopped` coopère. Post-deploy : 1 dernier *Arrêter* manuel pour « coller ». Piège #34. |
| 2026-05-29 PM | 🎯 **Rating FITCH PAR ISIN — refonte, Brave abandonné** (demande Massii « fitch only ISIN »). API GraphQL `api.fitchratings.com` `search(term:ISIN,item:IDENTIFIERS)` via **curl_cffi** (empreinte TLS Chrome → passe le mur Cloudflare qui refusait httpx au handshake). ISIN unique → ZÉRO ambiguïté de nom. Note émetteur Long Term IDR (+ note du titre exact dispo). `WD`/non-noté → ∅. Bunds→AAA (étaient BBB faux !), sovereigns OK. Retry anti-challenge Cloudflare (unreachable après 5 échecs). Cellule G Excel = lien `fitchratings.com/search/?query=<ISIN>`. Modules `scanner/fitch_isin.py` + `yield-bot/scraper/rating_fetcher.py` (`select_isin_rating`). ⚠️ `pip install curl_cffi` requis sur l'Omen (auto-deploy ne réinstalle pas). 112 tests ✓ (fixtures Fitch réelles). Validé live : Oncor BBB+, Bund AAA, Mercedes ∅(WD), bidon ∅. Piège #33 (supersede #29/#31/#32). |
| 2026-05-29 | 🩹 Fix rating WRONG-ISSUER (2 bots) — `site:fitchratings.com {issuer}` extrayait le rating d'une AUTRE entité quand Fitch ne notait pas l'émetteur (`Iccrea Banca` → `Fitch Affirms ICBC 'A'` → faux 'A'). Garde-fou d'identité `_issuer_matches_hit` (token identitaire dans titre/URL, match par token). Bond Scanner : rating obligatoire (bond sans rating vérifié exclu) + cellule G = **lien hypertexte Fitch** (vérifiable à la main). Yield Bot : faux positifs → `?`. Fix annexe : réserve Brave lisait `remaining 0` du plan métré (50 req/s, cap mensuel 0) comme épuisée → bloquait les scans ; on ne déclenche la réserve que si limite mensuelle > 0. Caches empoisonnés vidés. 91 tests ✓ (régression fixtures Brave réelles). Validé live : Iccrea→∅, Dominion→BBB+, Bayerische→A-. Pièges #31 + #32. |
| 2026-05-28 PM | 🔍 Bond Scanner V2 — refonte complète validée end-to-end en prod. (1) Pagination headless réparée via dispatch d'événements pointer (piège #30) : 25→1500 bonds. (2) Extraction listing (name.originalValue + coupon + maturité) → 97% sans enrich → ratissage rapide. (3) Best-N trié RATING DESC (slider 1-100). (4) 3 stores dédup : found (permanent) + seen (rejetés 60j) + rating cache (30j) ; overflow revient. (5) Garde-fou réserve Brave ≤50 (réservées au Yield Bot, clé partagée) → arrêt + blocage lancement. (6) Scan détaché (survit aux reloads), timeout 45min, banner Excel partiel. 79 tests ✓. 1er Excel prod : 30 bonds EUR yield≥4% triés AA-→BBB. |
| 2026-05-28 | 🔍 Bond Scanner — port stratégie rating Brave/Fitch-only (mirror Yield Bot). Strip cascade 5-providers (`DeutscheBoerseApiProvider`/`BoerseFrankfurtHtmlProvider`/`IssuerReferenceProvider`/`BoerseStuttgartProvider`/`FitchRatingsProvider`) → single `BraveFitchProvider` async. Cache 30j + negative caching (issuers Fitch-uncovered cachés aussi). Cellule Excel vide si pas de Fitch. Tests 18/18 ✓. Smoke OK : Dominion BBB+, Stryker ∅. |
| 2026-05-28 | ✨ PR 38 — Utilisateurs (admin-only) rejoint le dropdown profil au-dessus de Paramètres ; tab nav supprimé |
| 2026-05-28 | ✨ PR 37 — Topbar dropdown profil : Paramètres + Déconnexion sortent de la nav-tabs et vivent sous le `.user-pill` (caret ▾, aria-haspopup, click-outside, Esc, animation 140ms) |
| 2026-05-28 | 🏦 Activation rating fetcher Yield Bot — Brave Search API ciblée `site:fitchratings.com` (Fitch-only strict). 5/7 ratings "Da papà" extraits (Dominion ×2, Broadcom, IBM, AstraZeneca) ; Hilton/Stryker → cellule intacte (Fitch ne couvre pas). Coût $0/mois (free credits 1000 req). Clé stockée Keychain + `.env` gitignored. |
| 2026-05-27 | 🩹 PR 36 — Fix btn-icon trap (boutons serveur superposés) + Clé API Agents (👁/📋 strippés, labels i18n + 40 bullets placeholder) |
| 2026-05-27 | 🧹 PR 35-bis — Bump `?v=` JS individuels (lang/game_server/sv_files/server_view/app) oublié dans PR35 |
| 2026-05-27 | 🩹 PR 35 — Fix 4 boutons vides post-PR34 : `sharing.share_btn` raw, boutons search Mods/Modpack, loupe modale Partage, actions fichiers (renommer/supprimer) |
| 2026-05-26 PM | 🔇 PR 34 — Nuclear emoji sweep lang.js + tous JS + login.html (~720 bytes strippés) |
| 2026-05-26 PM | 🚪 PR 33 — Login page full Bento (brand `O OMENSERVER` + pill FR/EN/IT, plus de gradient text) |
| 2026-05-26 PM | 🔄 PR 32 — Background bots poller cross-page (`App._activeJobs` + `.tab-badge` + toast notif) |
| 2026-05-26 PM | 🖼️ PR 31 (backend) — CSP `img-src` étendu (forgecdn/modrinth/Steam Workshop/Spigot CDNs) |
| 2026-05-26 PM | 🔇 PR 27-30 — Strip emojis bots / modules / server pages (`YLD`/`SCN`/`CST` tickers, text-only buttons) |
| 2026-05-26 PM | 🏗️ PR 26 — Sidebar → topbar nav horizontale (10 tabs, brand+pill+user-pill à droite) |
| 2026-05-26 PM | 🏠 PR 24 — Real Bento Hub : kill big emoji module-cards, add `.machines-grid` + `.row-list` |
| 2026-05-26 PM | 🧹 PR 17-23 — Sweeps JS automatiques (legacy vars, hex, gradients, glassmorphism, ✅ backend Toast msgs) |
| 2026-05-26 PM | 🩺 PR 12-19 — Bento Tech polish autonome (themes legacy purge, RBAC emojis, login PR15, app.js sweep PR16) |
| 2026-05-26 | 🎨 Refonte frontend Bento Tech v5 — 11 PRs (tokens, composants, Dashboard, Server view, modules) |
| 2026-05-26 | 🎨 PR 11 — Polish autofill / form-input / avatars / danger icons |
| 2026-05-26 | 🎨 PR 10 — Game server list `.server-item` overrides Bento |
| 2026-05-26 | 🎨 PR 9 — Network module `.bento-overview` stats + speedtest |
| 2026-05-26 | 🎨 PR 8 — Bots module HTML refactor en `.bot-card-bento` |
| 2026-05-26 | 🎨 PR 7 — Override massif legacy classes (217 !important) |
| 2026-05-26 | 🎨 PR 6 — 15 composants restants + fix `.diag-item.err` bg |
| 2026-05-26 | 🎨 PR 5 — Server view sidebar 240px (strip inline styles) |
| 2026-05-26 | 🎨 PR 4 — Dashboard refactor + Diagnostic strip API |
| 2026-05-26 | 🎨 PR 3 — Accent switcher 4 dots + legacy theme migration |
| 2026-05-26 | 🎨 PR 2 — 11 composants atomiques + composés (`.badge`, `.role-pill`, etc.) |
| 2026-05-26 | 🎨 PR 1 — Tokens Bento Tech + Geist Mono import (additif) |
| 2026-05-18 | 🌙 Reboot post-suspend pour RAM fraîche (omen-resume.sh v2) |
| 2026-05-18 | 🏦 Fix Bond Scanner : ségrégation devise EUR/USD dans les rapports Excel |
| 2026-05-17 | 🔒 Audit sécurité complet : JWT secret, Docker shell injection, path traversal, RBAC |
| 2026-05-17 | 🛡️ Rate limiter IP-based + clés API masquées dans le dashboard |
| 2026-05-16 | 🏦 Optimisation Bond Scanner : pagination, retry logic, provider errors |
| 2026-05-15 | 🌙 Fix réveil automatique 6h + omen-resume.sh (restart cloudflared/omenserver) |
| 2026-05-15 | 🏦 Bond Scanner : download Excel fiable + slider config UI |
| 2026-05-14 | 🔌 Module Accès : SFTP conteneurisé par serveur + fix JS template literals |
| 2026-05-14 | 🔧 Fix Reboot/Shutdown : body JSON vide + Toast persistent + sudo validation |
| 2026-05-13 | 🖥️ Module Réseau admin-only + nodes grid déplacée + fix ping diagnostic |
| 2026-05-12 | 🖥️ Omen cerveau visible dans la grille machines + stats combinées (CPU/RAM/Temp) |
| 2026-05-12 | 🔌 Boutons reboot/shutdown sur carte Omen + diagnostic crash nodes |
| 2026-05-10 | 🖥️ Multi-machines : stockage combiné + mini-listes + auto-deploy |
| 2026-05-10 | 💾 Extension disque LVM (98→914 Go) + formatage SSD NVMe (469 Go) |
| 2026-05-10 | 🌐 Cloudflare Tunnel → omenserver.org + service systemd |
| 2026-05-08 | 🔧 Fix bcrypt (passlib → bcrypt direct) |
| 2026-05-07 | 🏦 Intégration Bot Yield (backend + frontend + CSS + i18n) |
| 2026-05-07 | 🔒 Security headers + CORS restreint + Swagger désactivé |
| 2026-05-06 | 🖥️ Déploiement Ubuntu Server sur HP Omen |
| 2026-05-05 | 🌍 Internationalisation FR/EN/IT complète |
| 2026-05-04 | 🔧 Navigation fix, logs persistants, stabilisation |
| 2026-05-01 | 🎨 V3 → V4 : Modules Média, Web, Réseau, PWA |
