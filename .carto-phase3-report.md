# Rapport PHASE 3 — Vitesse + mouvement propre, puis monde non-pacifique

> Session autonome `/loop` (07/06, depuis ~03:30 CEST). Branche `feat/mc-agent-phase3-speed`,
> mergée ff sur main en continu (auto-deploy + recyclage workers à chaque deploy).
> **EN COURS** — ce rapport est mis à jour au fil du run.

## PHASE A — livré (déployé sur main)

### A1 — Mode furtif optionnel (OFF par défaut)
- `humanize` (latence/typos chat), `loiter` (« stop = vivant »), jitter `explore` → derrière
  `--stealth 1`. Les bots utilitaires vont à **vitesse machine**.
- Toggle persisté au profil serveur (`stealth` bool, `_clean_server`), propagé `_spawn_bot
  --stealth`, checkbox UI onglet Serveurs + i18n FR/EN/IT (`mcagent.cfg.srv_stealth*`),
  cache-bust v226 / sw v105.
- **Trade-off documenté** : off = plus rapide mais détectable comme bot par un serveur
  anti-bot (mouvements parfaits, réponses instantanées). Assumé : notre serveur.

### A3 — Vitesse minage (profilage code du run 14h30)
| Goulot trouvé | Fix |
|---|---|
| `branchMine` s'arrêtait à `diamond>=1` ABSOLU → inutilisable dès le 1er diamant en quota → tout venait des cibles mappées lointaines (3-6 min de voyage/ore) | `stopOre {items,count}` en **DELTA** depuis le début du call → branch-mine = moteur continu au Y optimal |
| Roche nue minée via `collectBlock` (re-pathfinding vers CHAQUE drop, ~1-2 s/bloc de surcoût × milliers) | `bot.dig` direct, drops aspirés en avançant ; collectBlock réservé aux ORES |
| `cheapestPickFor` = pioche la moins chère sur la roche (stone 1.13 s/deepslate) | **Meilleure pioche partout** (iron 0.75 s, +50%) — le fer abonde, ensureGear re-craft |
| `bot.equip` à chaque bloc (~50-100 ms) | equip avec **cache** (skip si déjà en main) — branchMine/tunnelTo/descendDiagonal |
| Ores muraux non-diamant/fer/charbon IGNORÉS pendant le branch-mine | `ORE_NAMES` étendu : or/redstone/lapis/copper/emerald |
| `junkItems` jetait sticks/planks/coal → impasses pioche (vécu V2Res1) | gardés (+1 stack `cobbled_deepslate` pour le murage = stock infini en profondeur) |
| Cap de tunnel re-tiré du yaw à chaque call → tunnels qui se recroisent | **heading persistant** (`bot._branchHeading`), virage perpendiculaire sur lave/gouffre |

### A2 — Mouvement décisif
- `gotoOreBounded` : verdict `unreachable` en tranches de 120 s (au lieu de 300 s ×12).
- `stop` = immobile net (loiter seulement en furtif). dbg par-dig gaté (`opts.debug`).

### Fixes nés des runs V3 (rounds 1-2)
| Vécu | Fix |
|---|---|
| V3Res2 : 5×`goal_failed iron_ore` (~25 min d'anneaux stériles — fer de surface ≈ inexistant sous anti-xray) | **Kit coupé au FOUR** (stone pick + table + sticks + four) ; le fer vient du branch-mining Y=16 — `resource.js` privilégie `iron` tant que tier<3 (bootstrap by design) |
| V3Res4 : 5 timeouts de descente → 5 warps-surface (progression détruite) → starved | **Descente persistante** : timeout → reprendre de la position courante ; relocate seulement après 3 échecs mineFor consécutifs |
| V3Res2 : `lava_ahead` ×5 sur le MÊME cap | Rotation 90° avant re-descente (4 cardinaux max) |
| V3Res1 : `resource_waiting` ×178 sans pioche (80+ min passifs) | **starved `no_pickaxe` en 2 min** → exit → respawn → kit frais |
| V3Res4 : boucle `no_table:unknown_item` ×13 (table perdue + planks épuisées + pioche bois → amont « met ») | `goals.js` : raccourci pioche conditionné à **woodSafe** (table+sticks en poche) → la chaîne RÉ-OUVRE le bois ; `withCraftingTable` re-craft les planks depuis les bûches |
| Kit raté au spawn DÉFORESTÉ (runs précédents) | Kit-relocate ×3 vers biome **À ARBRES** + pré-check (pas de bûche ≤48 ni en poche → relocate-forêt AVANT le kit) |
| V3Res3 « fell from a high place » (percement de plafond de grotte) | **PONT anti-chute** : vide ≥2 sous la marche → bloc de remblai posé (descendDiagonal/branchMine/tunnelTo) |
| TPS 15.6 (« Running 17s behind ») : worldgen des warps frontière ×3 mappers + 4 mineurs | Mappers STOPPÉS pendant la phase quota (branch-mine = moteur principal, la carte est optionnelle) |

### PHASE B — code prêt (déployé, inerte en peaceful)
- **Riposte** : frappé + hostile mêlée ≤5 blocs → meilleure arme + `pvp.attack` (anti-spam 5 s).
  Creeper / PV ≤6 → FUITE prioritaire (inchangé).
- **Manger-pour-régen** : blessé (≤14 PV) → mange dès que faim <18 (seuil de régen naturelle).
- **Torches** : `ensureTorches` (1 charbon+1 stick=4, stock ≥8) + `branchMine torchEvery=8`
  (pose au sol tous les 8 paliers du tunnel principal). Best-effort.

### Fixes nés des runs V3 (rounds 4-9)
| Vécu | Fix |
|---|---|
| Planner JAMAIS en stall sur un but exploratoire (fingerprint POSITION : gatherLog bouge à chaque tentative → stalls=0 → boucle infinie logs timeout, V3Res2/4 immobilisés des heures) | failStreak : 4 `{ok:false}` consécutifs du même but = stalled, peu importe le déplacement |
| Kit-relocate ne s'armait que pour tier<2 (bot tier 2 SANS table bloqué en chasse au bois) | relocate-forêt sur stall logs/planks/table quel que soit le tier ; gatherLog 480→180 s |
| Cellule « biome forêt » pelée (V3Res2 : 158 waypoints sans un arbre) | relocate-forêt vise les bûches CONFIRMÉES (memory.finds *_log, live), spread 48 |
| Fer 64→51 en 1h (politique « meilleure pioche partout » : 1-2 pioches fer/branchMine, 3 lingots pièce > gain 0.375 s/bloc) | REVERT : roche → moins chère (stone, cobble gratuit), ore → meilleure |
| Noyades ×3 en 7 min (plafonds d'aquifère, seuil watchdog tardif) | watchdog barbotage 2 épisodes/90 s (chaque épisode = quasi-noyade) |
| « suffocated in a wall » (gravier au-dessus de la case creusée) | anti-gravier branchMine (mine la colonne qui retombe, ≤4) |
| Morts → retour au spawn-LAC du monde (~10 min de re-voyage) | /spawnpoint post-kit (bot OP) + skip death-recovery quand keepInventory |
| **Tempête « Connection throttled »** : morts simultanées → respawns à t+15 s pile → throttle Paper 4 s same-IP → 3 collisions comptées crash-on-spawn = respawn_given_up (V3Res3 à terre) | sérialiseur GLOBAL de spawns (≥6 s entre joins), throttle-kick ≠ crash, Timer jitteré |
| Mort pendant un craft = table posée perdue → chaque respawn repart en chasse au bois | rab post-kit : 2e table de rechange + planks ≥12 + sticks ≥16 (3 morts amorties) |

## Mesures (en cours)
- Référence à battre (nuit 06-07/06) : 1er bot 5/5 en **7h14**, 4e en **14h26**.
- Run V3 : t0 nominal 01:57:48Z ; relances sur fixes 02:50Z, 03:25Z, 04:06Z, 04:41Z,
  05:16Z, 06:06Z, 06:52Z, 07:50Z, **08:40Z (round 9, build stabilisé)**.
- Débit post-fix observé : V3Res2 **+50 items quota/45 min** ; V3Res3 fer 64 + lapis 45 +
  redstone 27 + 1💎 en ~30 min (rounds favorables). Les freins restants = morts (table/voyage,
  amorties round 9) et churn de respawn (réglé round 8, 0 throttled).
- Tests : **Node 516 / Python 256** verts. Site 200 après chaque deploy (1 × 502 transitoire
  de restart uvicorn).

## PHASE B — mesures en cours (mobs Normal, t0B = 13:39:03Z)
- Wipe quota VÉRIFIÉ 13:39Z (⚠️ piège : Essentials override /clear — la forme `minecraft:item`
  no-op en silence, utiliser les noms SANS namespace). Trous de mesure (deploys/fixes) :
  14:41-15:10, 16:1x-16:32, 16:5x-17:15, 17:5x-17:56.
- Survie : morts RÉPARTIES (eau + creepers dominent), AUCUNE boucle de mort après les fixes
  anti-camping (2 morts <60s → warp+re-spawnpoint) + épée pierre + escalade water_rescue_warp
  (2e rescue <5 min = warp dur — vérifié live ✓) + anti-jam (saut infini) + chutes PV≥75%.
- Progression (19:15Z) : V3Res1 💎1+or6+rouge9+lapis19+fer62 ; V3Res4 rouge10+lapis54+fer✓ ;
  V3Res3 fer✓ ; V3Res2 churn respawn. TPS 19-20 constant avec 2 mappers.
- Cartographes : spec « autonomes+humains » appliquée (mini-kit, reprise couverture persistée,
  self-healing respawn ✓, --humanize : typos/latence ✓ vérifiés live, stop-pour-répondre déployé,
  jitter déplacement, loiter OFF). Frontière à 4700+ blocs du spawn (carte s'étend ✓, biomes
  455→459+/2 min). Connu/accepté : kit mapper peut s'engluer ≤8 min sur un gather post-warp
  (borné, self-récupère).
- 6 bugs live (Massii) corrigés : voir commit 838b851. Stop-pour-répondre visible : 99469b0.

## Assists opérateur (transparence, même classe que la nuit)
- ~09:30Z : `give 16 oak_log` à V3Res4 et V3Res1 (items NON-quota, précédent documenté de la
  nuit : sticks+planks à V2Res1) — les 2 retardataires enchaînaient les zones sans arbres
  malgré le relocate-forêt (malchance de tirage). TOUS les minerais des quotas restent minés.
- Mappers stoppés à ~03:15Z (TPS) — la carte n'est plus alimentée, le branch-mine est le moteur.

## Décisions notées (autonomie)
- Mesure « nettement plus vite » : même méthodologie que la nuit (fixes en cours de run
  comptés dans l'horloge) — t0 01:57:48Z.
- Comptes FRAIS V3Res1-4 (inventaires vides garantis) + carte d'ores VIDÉE au départ
  (backup `/tmp/8d91a7.phase2-final.bak.json`) = mêmes conditions que la nuit.
- Mappers retirés en cours de run (TPS) : assumé, le pipeline quota n'en dépend plus.
