# MC Agent — Marathon (64× diamant/redstone/lapis/or) — journal de session

> Session autonome /loop démarrée 2026-06-04. Worktree `feat+mc-agent-marathon` (base bf8e276).
> Mission : objectif `marathon` + runs longs live → catalogue des modes de panne long-terme + fixes.

## État
- [x] Baseline : 326 tests Node verts @ bf8e276
- [x] Connectivité : MC 100.108.50.70:25566 OK ; SSH LAN `massii08@192.168.68.75` (clé omen_mc_agent)
      ⚠️ SSH via IP Tailscale 100.108.50.70 = INTERCEPTÉ par Tailscale SSH (re-auth web) → utiliser le LAN
- [x] Module `marathon.js` + 22 tests (nextAction, M(c), miningY, scaffold)
- [x] branchMine : ORE_NAMES ×4 minerais + stopWhen + réserve scaffold deepslate-aware (P1 fixé)
- [x] deposit : depositFiltered (only/surplus, relit le coffre → banked) + 4 tests
- [x] goals.js : MARATHON_KIT (21 buts, coffre gated hasBase, scaffold_buffer) + 5 tests
- [x] index.js : startMarathon + dispatch 10 actions + ancre surface + télémétrie mem 30s
- [x] Backend VALID_OBJECTIVES + dropdown UI (i18n ×3, cache-bust v216/sw95). 364 Node + 73 Py verts
- [x] Run live #1 (MarathonA) → 💥 P2 OOM ; run #2 en cours (heap 768M, monitor armé)
- Harness : `~/mc-marathon-test/` (world.json seedé marathon), logs runN.log, PID en cours run2=70878

## Architecture décidée
- **Phase A (kit)** : runPlanner(MARATHON_KIT) — kit fer complet + coffre en poche + réserves, en surface.
- **Phase B (boucle marathon)** : `nextAction(ctx)` pur (priorités : done > pickaxe > base > deposit >
  restock(food/bois, trip surface) > torches > spare_pickaxe > descend/ascend > mine).
  Dispatch côté index.js avec timeouts + settleSurvivalKit. branchMine par segments de 24, le tunnel
  s'éloigne de la base au fil des segments (stress pathfinder longue distance = voulu).
- **Comptage M(c)** : inventaire + `world.banked` (mis à jour à chaque deposit en lisant le coffre).
  or = raw_gold + gold_ingot. Cible 64 chacun.
- **Y de minage** : -54 tant que diamant|redstone <64, puis -16 (pic or, lapis correct).
- **Base** : coffre posé à la profondeur de minage (deposit courts, supply runs longs = stress voulu),
  `world.home` = pos coffre, `world.surface` = ancre de surface (spawn) pour les restock trips.
- **Retour surface** : pathfinder GoalNear(world.surface) — réutilise l'escalier creusé à la descente.

## Catalogue des modes de panne long-terme (à remplir au fil des runs)
| # | Panne | Run | Repro/test | Fix | Commit |
|---|---|---|---|---|---|
| P1 | wallLava/réserve ne compte QUE `cobblestone` — à Y-54 le minage donne du `cobbled_deepslate` → réserve « épuisée » à tort → stop cobble_low, lave non murable | analyse statique | tests branchMine marathon | compter cobblestone+cobbled_deepslate (SCAFFOLD_NAMES) partout | b40a8dc |
| P2 | **OOM heap 2 Go en 116 s** pendant `cobble_pick` (gather stone, forêt y67). Suspect : A*/collectBlock sur cible inatteignable (open set non borné). N'est PAS marathon-spécifique (kit = même code que les runs Surv) — terrain-dépendant. Run #2 au même endroit passe (trou du run #1 expose la pierre). | run #1 | à reproduire (mem télémétrie ajoutée, heap 768M pour fail-fast) | mitigation indirecte : timeouts par phase (cf. P3b) coupent l'A* via stopMotion avant le ballon | 2a006f7 (télémétrie) |
| P3 | **Boucle infinie de timeouts sur `iron_ore`** : le fer n'est presque jamais exposé en surface → `gather+explore` roame 90 s, timeout, retry, ∞ (~25 min observés). Le « chantier recherche profonde » du piège #41. | run #2 | test goals (skill=gatherIron) | skill `gatherIron` : visible≤32 → gather, sinon descente Y=16 (pic fer) + branchMine stopWhen fer≥n ; timeout dédié 15 min | a0c2a8e* |
| P3b | **gatherIron gelé 15 min sur place** : fer VISIBLE ≤32 mais INATTEIGNABLE → collectBlock/A* pend sans timeout interne (et c'est le mécanisme suspecté de P2 : open set A* qui balloon). | run #3 (21 min à y54) | télémétrie gatherIron_phase | timeout PAR PHASE : visible_gather 2 min / scaffold 2 min / descend 8 min + stopMotion | (commit phase-timeouts) |
| P4 | **`descendDiagonal` throw TypeError au 1er appel** : POJO passés à `bot.blockAt()` qui exige `pos.floored()` — le piège #35 corrigé dans branchMine (Vec3) mais JAMAIS ici (le skill n'avait jamais tourné en prod réelle : `descend:error` immédiat). | run #4 | test fake-bot fidèle (blockAt exige .floored) | Vec3 via P(q) sur tous les blockAt du skill | commit P4 |
| P5 | **descendDiagonal boucle muette sans progrès** : au fond d'un puits 1×1 (l'escalier exige de MARCHER, impossible), le goto pend, la boucle tourne jusqu'au timeout externe 8 min, sans diagnostic. | run #5 | test goto-inopérant → no_progress | garde no-progress (8 paliers même Y → abandon) + goto borné 15 s + fallback mineDown dans gatherIron | commit P5 |
| P6 | **mineDown : double bug.** (a) `{count}` attendu mais 3 call-sites passent `{depth}` (index ×2 + **shelter** : abris nocturnes 1 bloc au lieu de 2 !) → creusait 1 bloc. (b) Piège #41 jamais fixé : position fractionnaire (y=53.75 à cheval sur l'arête) → creuse mais ne tombe pas → void_below. | run #6 (rcon : Pos y=53.75) | tests depth-alias + centrage | depth alias + centrage GoalBlock avant dig + attente de chute après dig (gated sur bot.pathfinder → fake-bots inchangés) | commit P6 |
| P7 | **gather bute en boucle sur une cible incollectable** : bûche de canopée flottante (laissée par les récoltes des runs passés) re-choisie éternellement par findBlock → collect_failed ∞. Mode « terrain dégradé par soi-même ». | run #8 (user frais !) | test 2-cibles (mauvaise puis bonne) | blacklist des cibles mortes + candidats multiples via findBlocks + borne attempts | commit P7 |
| P8 | **Descente le ventre vide** : la branche `!hasBase` de nextAction court-circuitait les checks food/bois → le bot partait à −54 avec 0 nourriture (kit `food_stock` échoue `no_prey` sous terre). Mort observée live. | run #9 | tests priorité restock | restock (food/bois) AVANT toute logique base/descente dans nextAction | commit P8-P10 |
| P9 | **marathonDescend sans fallback** : seul gatherIron avait le fallback mineDown → l'action `descend` du marathon stallait ×5 (no_progress) → shuffle → re-stall. | run #9 (×8 stalls Monitor) | — (couvert P5/P6) | `descendRobust` partagé : escalier ⇄ mineDown alternés ×4 rounds + déplacement entre rounds | commit P8-P10 |
| P10 | **Position de mort en RAM seulement** : un restart du process (crash/redeploy/fix) perdait `lastDeath` → pas de récupération d'items à la reprise (ils despawnent à 5 min). | analyse (mort run #9 + restart) | — | `world.lastDeath` persisté à la mort, relu par startAutonomous | commit P8-P10 |
