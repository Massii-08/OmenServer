# Récolteur 1d — log de progression (session autonome)

> Branche `feat/mc-agent-resource-harvester` (base world-memory @ 999d9e6). Fichier local, jamais commité vers Obsidian directement.

## 2026-06-03 — Orientation initiale

- **Constat clé** : les points 1 et 2 du job sont DÉJÀ dans la base (faits côté world-memory) :
  - Biais dirigé : `explore.js` l.74-95 consulte `directedTarget(bot._worldMemory, bot._worldKey, …)` AVANT les anneaux ; fallback aveugle intact. Testé (`explore.test.js` ×2 directed).
  - Apprentissage : `gather.js` l.76-80 émet `materialFoundEvent` au collect réussi (biome résolu AVANT le dig). Testé (`gather.test.js`).
  - Manager Python route `material_found` → `add_find` sous verrou. Testé.
- Suites au départ : **Node 283 ✅ / Python 143 ✅** (vert avant tout changement).
- **Reste à faire (ma part)** :
  1. Biais CAVES pour les minerais (option 3 du prompt) : `directedTarget` ignore `worlds[w].caves` → étendre (matériau minerai + pas de find appris proche → cave la plus proche). TDD.
  2. Vérifier test « fallback aveugle si mémoire muette » existe, sinon l'ajouter.
  3. Obsidian : section « Récolteur 1d » page concept + entrée Daily (à la FIN, jalons seulement).
- Surface index.js : **zéro** (extension dans worldMemory.js → explore en profite automatiquement).

## 2026-06-03 — Biais caves implémenté (commit 60f74b2)

- TDD : 4 tests worldMemory (rouge→vert) + 2 tests explore.
- `worldMemory.js` : `isOre()` (`*_ore` + `ancient_debris`) ; `directedTarget` 3e étage =
  **finds appris > amorce vanilla > cave connue la + proche** (minerais seulement) → `{x,z,y,biome:null,cave:true}`.
- `explore.js` : 1 ligne — event `explore_directed` taggé `cave`.
- Intégration vérifiée : `index.js:249` passe `explore:true` à tous les gather de la chaîne autonome
  → biais dirigé (et caves) actif en pratique, y compris `iron_ore` (goals.js:72). Zéro modif index.js.
- **Suites finales : Node 289 ✓ / Python 143 ✓** (Python non touché dans mon delta, re-validé).
- **Live** : SSH omen OK (borné 10s) mais `data/mc_agent_world_memory/` ABSENT sur l'Omen → mémoire
  jamais remplie en prod (cartographe pas encore déployé/lancé live). Conformément au mandat :
  validation par mémoire FIXTURE dans les tests (explore directed/cave/fallback) ; end-to-end live
  différé jusqu'à ce que le cartographe ait peuplé le store. NON bloquant.
- Reste : Obsidian (section Récolteur 1d + Daily) puis arrêt de la boucle.

## 2026-06-03/04 — NOUVEAU MANDAT : boucle live test→améliore

Harness live : bot lancé depuis le worktree Mac → `100.108.50.70:25566` (docker omen-minecraft-trusted-test),
stdin pilotable (`tail -f ~/mc-harv-test/cmds | node index.js …`), carte fixture `~/mc-harv-test/memory.json`,
observation rcon (`data get entity <bot> Pos/Inventory`) + `run.log` (events JSONL). Features posées par rcon
(fill logs/red_sand/hay/iron_ore) loin du spawn (zone -2000s) → zéro impact MapT5/Massitom2008.

### Runs live
1. **HarvT2 — chaîne stone_pickaxe complète** : `autonomous_done` depuis zéro, 2× material_found (biome réel). (Directed non exercé : logs trouvés localement.)
2. **HarvT3 — scénario (a) directed-harvest ✓** : take red_sand 3 → `explore_directed{learned:true}` → 250 blocs en ligne droite → 3 récoltes → 3× material_found (biome OBSERVÉ savanna ≠ badlands de la carte = datapack-robuste).
3. **HarvT3 — scénario (b) fallback-aveugle ✓** : take hay_block (carte muette) → 0 directed → `explore_waypoint` r=80 → trouvé → récolté.
4. **Scénario (c) apprentissage ✓** : material_found du run.log appliqués via le VRAI store Python (`apply_event`, dédup 7→3) → carte v3 → HarvT4 frais à 404 blocs → `explore_directed{learned:true}` direct au hay appris → récolté. **Contrat bot→manager→store→bootstrap validé end-to-end.**
5. **HarvT5 — gisement épuisé ✓ (post-fix)** : directed vers faux find (760) → miss → anneaux CENTRÉS POSITION COURANTE (fix) → vrai dépôt trouvé à r=160 → récolté.

### Améliorations issues des passes de revue (commits)
- `stdin {type:'start'}` + `{type:'order'}` (harness/manager) ; take ordonné → `explore:true` ; **emit câblé gather→explore** (explore_directed/waypoint visibles en run.log).
- **Gotos explore bornés** (directed 240s / waypoint 90s, `gotoWithTimeout` + setGoal(null)) — anti-gel pathfinder ; `SKILL_TIMEOUTS.gather=480s` (trajets dirigés ≤1500 légitimes).
- **Anneaux post-directed centrés sur la position courante** (prior local, pas de retraversée).
- `_ids` null (bloc inconnu registry) → `not_found` net, jamais findBlock(matching:null).
- **Directed multi-matériaux = cible la + proche tous noms confondus** (pas le 1er du tableau).
- Suivi noté (pas implémenté, YAGNI/surface manager) : « confirmation négative » d'un find épuisé (event material_missing + vieillissement côté store) → Phase 2.

### Run HarvT6 (cave bias, échecs instructifs ×2)
- take iron_ore : minerai naturel local trouvé ×2 → cave jamais exercée (normal : la roche regorge d'iron ≤64). Leçon : cave bias ne s'exerce que sur les cibles rares.
- take diamond_ore : findBlock(64) a vu un diamant ENTERRÉ inatteignable → collect_failed en 25s SANS tenter explore/directed + **creusage fantôme** après l'échec (goal collectBlock jamais nettoyé). 2 vraies faiblesses.

### Fixes revue #3 (commit) — TDD, 298 tests ✓
- gather : cibles ratées (`failed` set) ignorées par findBlock → l'explore/directed cherche AILLEURS au lieu d'abandonner sur la cible morte locale.
- `stopResidual` après double échec collect (setGoal(null) + controls off) — anti-creusage fantôme.
- explore : goal dirigé d'une CAVE au y de l'entrée (GoalNear 3D précis).

### Run HarvT7 (cave bias ancient_debris)
- `explore_directed{cave:true}` ✓ DÉCLENCHÉ live (700,-1950), trajectoire droite — le 3ᵉ étage marche en jeu.
- ⚠️ **2 morts en voyage** (trajet 500 blocs, nuit puis jour) : `reflex flee` ×N puis dead. **Observation produit** :
  un bot NU ne survit pas aux trajets dirigés longs ; les réflexes fuite sans équipement ne suffisent pas.
  → PAS un défaut du 1d : le mode autonome réel fait son kit AVANT (chaîne stone/iron) ; la survie de
  voyage est le chantier §5.1-5.2 (mini-kit+survie, spécifié pour le mapper, à réutiliser pour le récolteur).
  Suivi noté aussi : reprise d'un ordre take interrompu par la mort (l'autonome reprend déjà, le take non).
- Subtilité harness : `order_done` sans id → l'annulation d'un vieil ordre peut être confondue avec la fin du nouveau (faux signal watcher). Mineur, noté.
- Re-test trajet COURT de jour (850,-2050 → cave 330 blocs) : en cours.

## 2026-06-04 — Clôture : DoD atteint

### Suite des runs (HarvT7→T10) et fixes
- **Cave bias ✓ live** (HarvT7) : `explore_directed{cave:true}` → trajet → ancient_debris récolté à la cave + **anti-noyade validé** (réflexe surface ×3 dans le lac qui avait tué 3 bots).
- **3 morts par NOYADE** (logs serveur `drowned`, pas des mobs !) → fix : réflexe oxygène (`breath` → setGoal(null)+jump, hystérésis) + `liquidCost 20`. Plus aucune mort ensuite.
- **Directed mort-né au spawn frais** (HarvT8/T9, NoPath transitoire = chunks non chargés après tp) → grâce 4s avant retry + **rappel dirigé à chaque nouvel anneau** (≤3). Théorie confirmée live (même bot, chunks chargés → directed parfait).
- **Persistance par progrès** : interruptions en rafale (flee/surface → GoalChanged) tolérées tant qu'on se rapproche >8 blocs.
- Finding revue indépendante : `gatherLog` absent de SKILL_TIMEOUTS (90s ≠ trajets dirigés longs) → aligné 480s.

### TRIPLET FINAL sur code final — DoD (1) ✓
- (a) HarvT7 : take red_sand → directed learned → ligne droite → récolte ✓
- (b) HarvT7 : take hay_block (carte muette) → 0 directed, anneaux → récolte ✓
- (c) HarvT10 : material_found d'un AUTRE bot → store Python (apply_event) → carte → bot FRAIS → directed learned → récolte ✓

### Passes de revue finales — DoD (2) ✓
- Passe A (subagent indépendant : boucles/leaks/consommateurs/contrat backend/interactions explore) : **AUCUN FINDING RÉEL** (306 Node + 53 Py backend verts).
- Passe B (relecture finale) : rien de nouveau dans le périmètre. Limitations documentées hors scope : lenteur terrain lacustre (borné, vivant), reprise take post-mort (choix produit), purge finds épuisés (Phase 2 manager), order_done sans id (harness only).

### Bilan
10 runs live, 8 fixes commités issus du live, tests finaux **Node 306 ✓ / Python 143 ✓**. Arène nettoyée (fills air). Bot stoppé.
