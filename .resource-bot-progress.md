# Récolteur 1d — log de progression (session autonome)

> Branche `feat/mc-agent-resource-harvester` (base world-memory @ 999d9e6). Fichier local, jamais commité vers Obsidian directement.

## 2026-06-03 — Orientation initiale

- **Constat clé** : les points 1 et 2 du job sont DÉJÀ dans la base (faits côté world-memory) :
  - Biais dirigé : `explore.js` l.74-95 consulte `directedTarget(bot._worldMemory, bot._worldKey, …)` AVANT les anneaux ; fallback aveugle intact. Testé (`explore.test.js` ×2 directed).
  - Apprentissage : `gather.js` l.76-80 émet `materialFoundEvent` au collect réussi (biome résolu AVANT le dig). Testé (`gather.test.js`).
  - Manager Python route `material_found` → `add_find` sous verrou. Testé.
- Suites au départ : **Node 283 ✅ / Python 143 ✅** (vert avant tout changement).
- **Reste à faire (ma part)** :
  1. Biais CAVES pour les minerais (option 3 du prompt) : `directedTarget` ignore `worlds[w].caves` → étendre (matériau minerai + pas de find appris proche → cave la plus proche). TDD.
  2. Vérifier test « fallback aveugle si mémoire muette » existe, sinon l'ajouter.
  3. Obsidian : section « Récolteur 1d » page concept + entrée Daily (à la FIN, jalons seulement).
- Surface index.js : **zéro** (extension dans worldMemory.js → explore en profite automatiquement).

## 2026-06-03 — Biais caves implémenté (commit 60f74b2)

- TDD : 4 tests worldMemory (rouge→vert) + 2 tests explore.
- `worldMemory.js` : `isOre()` (`*_ore` + `ancient_debris`) ; `directedTarget` 3e étage =
  **finds appris > amorce vanilla > cave connue la + proche** (minerais seulement) → `{x,z,y,biome:null,cave:true}`.
- `explore.js` : 1 ligne — event `explore_directed` taggé `cave`.
- Intégration vérifiée : `index.js:249` passe `explore:true` à tous les gather de la chaîne autonome
  → biais dirigé (et caves) actif en pratique, y compris `iron_ore` (goals.js:72). Zéro modif index.js.
- **Suites finales : Node 289 ✓ / Python 143 ✓** (Python non touché dans mon delta, re-validé).
- **Live** : SSH omen OK (borné 10s) mais `data/mc_agent_world_memory/` ABSENT sur l'Omen → mémoire
  jamais remplie en prod (cartographe pas encore déployé/lancé live). Conformément au mandat :
  validation par mémoire FIXTURE dans les tests (explore directed/cave/fallback) ; end-to-end live
  différé jusqu'à ce que le cartographe ait peuplé le store. NON bloquant.
- Reste : Obsidian (section Récolteur 1d + Daily) puis arrêt de la boucle.
